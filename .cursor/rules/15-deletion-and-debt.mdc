---
description: Deletion vs. deferral decisions, and the lifecycle of migration code.
alwaysApply: true
---

# When to delete, when to defer

## Default: delete

Code with no live caller, code that handles state that doesn't exist, code
marked `[[deprecated]]` for more than one release cycle — all of it goes. Dead
code is audit surface, attack surface, and review fatigue.

## Three dispositions, and each is defined by its grep signature

Work that has been decided about has exactly one of three dispositions. They
are distinguished by **what a machine can find**, not by what the prose says,
because the next agent does not read the prose — it greps:

| Disposition | Symbols | `FOLLOWUPS.md` rows | Decision log |
| --- | --- | --- | --- |
| **REJECTED** | zero | zero | exactly one entry |
| **DEFERRED** | zero | exactly one, naming an external blocker | not required |
| **STAGED** | present | not required | named consumer in a live plan |

An agent greps for symbols and finds staged work. It greps `FOLLOWUPS.md` and
finds deferred work. Rejected work is invisible to both, and its reasoning
lives where humans look and machines do not retry.

That invisibility is the whole mechanism. **A rejected decision that leaves a
symbol or a FOLLOWUPS row is a standing invitation to re-attempt it**, because
both surfaces read as "someone meant to do this". Such residue does not merely
fail to record the rejection — it contradicts it, on the one surface the
machine actually reads.

What follows from the table, rather than from taste:

- **A row that cannot name a blocker is not a deferral.** It is an *undisposed*
  item sitting on the surface agents grep to find deferred work. Give it a
  blocker, reject it and delete the row, or stage it. It does not get to wait
  in the queue as an unanswered question.
- **A rejection is not recorded by deleting quietly.** Zero symbols and zero
  rows, with no decision-log entry, is indistinguishable from work nobody ever
  considered — so the next agent proposes it again, and the rejection has cost
  more than it saved.
- **Staged work owes a named consumer** (`22-no-lazy-deferral.mdc`,
  callee-without-caller). Symbols with no consumer are not staged; they are
  dead code under *Default: delete* above.

### What this section does not cover

- **Which disposition an item gets.** That is the reviewer's decision; this
  fixes only the form each takes once the decision is made.
- **Work in flight.** An open PR is not disposed, it is in progress.
- **The wording of a FOLLOWUPS row** — `95-documentation-lifecycle.mdc` owns
  the one-liner form and the `Target:` tokens; this owns how many rows exist.
- **What makes a blocker acceptable** — `22-no-lazy-deferral.mdc` owns that,
  and adds the requirement that a deferral be disclosed in the flow that made
  it.
- **Vendored and inherited trees**, which carry symbols nobody here staged.

## When to use `[[deprecated]]`

Almost never. The annotation is a promise to delete by a specific version. If
you can't name the version, you can't deprecate — delete now or schedule the
deletion in `docs/FOLLOWUPS.md`. `[[deprecated]]` without a deletion date is
debt that compounds.

## Migration code is a permanent attack surface for a one-time problem

Code that detects an old format and converts it runs forever to handle state
that exists for a finite period.

- **Pre-V3 launch:** `rm -rf ~/.shekyl` and re-sync is the migration path. No
  in-Shekyl format-detection code is justified. Ruthless about deletion.
- **Post-V3 launch:** real users have real state. Migration code becomes
  load-bearing where it exists, but should be the smallest possible code with
  the clearest possible scope, and should still have a deletion target.

This rule is distinct from `60-no-monero-legacy.mdc`, which says no Monero-era
compatibility code exists at all — that's a permanent fact about genesis, not
a transitional policy.

## FOLLOWUPS.md is not a graveyard

Items in `docs/FOLLOWUPS.md` have a `Target:` of **pre-genesis**,
**post-genesis**, or **V4** (see
[`95-documentation-lifecycle.mdc`](95-documentation-lifecycle.mdc)).
Items without one get one within 30 days or get closed as "won't fix."
`V3.1` / `V3.2` / `V3.x` are not targets.

## "While we're here" is the enemy

Every PR has a stated scope. Changes outside that scope go in a separate PR,
even when they're obvious improvements. Reviewer attention is finite;
bisection requires small commits. When you spot something worth fixing
outside scope, write it down in `FOLLOWUPS.md` and keep going.

However, this does not preclude disciplined "the file I'm editing for
substantive reasons should be left in good shape after my edit." It's entirely
appropriate and responsible to fix mechanical formatting errors in a file that 
is already being modified.

## Three work-item targets (not a V3.x train)

V3 ships with FCMP++ and hybrid PQC from genesis. There is no V3.1 /
V3.2 / V3.x release train — those labels were how work hid from
genesis. Allowed targets:

- **pre-genesis** — default. If it should exist at launch, it lands
  before genesis. Genesis ships as complete as possible.
- **post-genesis** — exceptional deferral with a named blocker why it
  cannot land before genesis. The list stays tiny.
- **V4** — lattice-only transition, **2–5 years**, gated on NIST (or
  successor) actually approving and releasing primitives such as
  lattice threshold signatures. Not a parking lot.

If an item does not fit pre-genesis it needs a named post-genesis
blocker, or it is V4, or it is won't-fix. It does not wait in a fake
intermediate version.
