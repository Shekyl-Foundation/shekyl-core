# Implementation index and phase map

**Purpose.** One page that answers "what does this identifier mean, which doc
owns it, and what has actually landed on `dev`?" The wallet/staking work spans
several plan documents whose numbering schemes evolved independently; this
index is the disambiguation layer. It is a *map*, not a status-of-record —
each track's own plan doc remains authoritative for design detail, and where
a plan doc's status table disagrees with landed code, **code wins** (§6 lists
the known disagreements at the verification date rather than editing those
docs from here).

**Method note (2026-07-18) — gate rows decay at the referenced item's rate.** A gate row
("blocked on X", "X is unbuilt") is a claim about *another* item's state and goes stale
when that item moves, not when its host document does. The **cleared-gate sweep is a
landing obligation of the PR that clears the gate**, not of the docs that name it: a PR
that builds or lands X must grep for rows naming X as a blocker (docs *and* code
comments) and correct them in the same landing. Blocked-when-actually-unblocked is the
expensive direction — nobody re-checks a gate that says it is closed. (Instances swept
2026-07-18: the M1 `K_COVER` gate list, the SP-R0 "SP-6 unbuilt" rows in FOLLOWUPS and
the 2D2 plan §2/§12, the WI-1 "made the chain live" comment — PR #333 and the SP-R0
Round-0 exit.)

**Verification stamp.** Statuses below were verified against landed code on
`dev` = `13c368707` (the PR #277 merge) as of 2026-07-09 for the
reward-emission rows (C-1 landed: whitelist flip at
`cryptonote_format_utils.cpp:706`, connect arm `apply_archival_emission_claim`
in `blockchain_db`) and the M1 / segment-freeze rows (PR #263 `6b6517bfc`
/ #264 `0d224faac` merged); other rows retain their prior code-anchored stamps
unless noted. When you update a status here, re-verify against code
(`git grep`, not plan prose) and move this stamp.

**Maintenance discipline** (institutionalized as
[`.cursor/rules/94-tracking-index.mdc`](../../.cursor/rules/94-tracking-index.mdc) —
new identifier families register here at birth; Phase 3+ / Stage 3+ work
items start with an index row).

- **Row format:** every status cell *leads* with the current status and its
  as-of date (e.g. `Landed (as of 2026-08-01)`); dated `UPDATE YYYY-MM-DD:`
  entries append *below/after* that line as the history chain. Readers get
  the answer without parsing the chain.
- **Two verification classes:** *code-anchored* items cite a `git grep`
  target on `dev`; *decision-anchored* items (round closures, wire freezes,
  contract docs — no code artifact) cite the commit hash or PR number of the
  doc edit that closed them.
- **Section contract:** rule 94 refers to §2 (identifier-family registry),
  §4–§5 (live / built-unwired / missing inventory), and §6
  (known-stale-statuses ledger) by role. Their meanings — and this numbering
  — are load-bearing; a restructuring that moves them updates
  `.cursor/rules/94-tracking-index.mdc` in the same PR.
- This doc never carries design content — link to the owning doc instead.
- New identifier families (a new prefix series in any plan doc) get a §2 row
  in the same PR that introduces them, after the rule-94 prefix-uniqueness
  check.

---

## 1. The collision this index untangles

Two independent schemes both used the bare tokens `2a / 2b / 2c / 2d`, and
one token (`2c-2b`) was reused *within* a single scheme:

| Token | Engine-layer meaning (`WALLET_REWRITE_PLAN.md`) | Bond-construction meaning (`ARCHIVAL_BOND_PR2_CHAIN.md`) |
| --- | --- | --- |
| 2a | Phase 2a — send path | Bond-PR 2a — synthetic-tree round-trip KAT (#156) |
| 2b | Phase 2b — stake lifecycle | Bond-PR 2b — StakeEngine actor, seed-owning model (#157) |
| 2c | Phase 2c — addresses/proofs | Bond-PR 2c-1 / 2c-2a / 2c-2b — real-tree KAT, Model D wiring, request path |
| 2d | Phase 2d — cold bundles | Bond-PR 2d-1 / 2d-2 — `P`-scan layer, `P`-isolated transport |

**Resolution (pinned 2026-06-19, `ARCHIVAL_BOND_PR2_CHAIN.md` §0):** the bond
chain is prefixed **`Bond-PR`** in doc prose; bare **`Phase 2x`** always means
the engine layer. Branch names are immutable history and keep their original
(unprefixed) names.

**The `2c-2b` double-use.** Within the bond chain itself, `2c-2b` names two
different deliverables in sequence:

1. **Bond-PR 2c-2b (request path)** — JoinMarket bond request layer, landed
   `feat/archival-bond-request` #163, plan doc
   [`ARCHIVAL_BOND_REQUEST_2C2B_PLAN.md`](ARCHIVAL_BOND_REQUEST_2C2B_PLAN.md).
2. **2c-2b re-scope (GF-7 scheduler seam)** — block-timed entry-seam planning
   plus GF-7 measurement hooks, landed PR #255 (2026-07-05), plan doc
   [`ARCHIVAL_BOND_2C_GF7_HOOKS.md`](ARCHIVAL_BOND_2C_GF7_HOOKS.md).

When you see `2c-2b` in prose, the linked plan doc disambiguates; new prose
should say "request path" or "GF-7 seam" explicitly.

---

## 2. Identifier families

| Family | Reads as | Owning doc | Notes | Completion Status |
| --- | --- | --- | --- | --- |
| **Phase 0–6** | Engine-layer wallet-rewrite phases: 0 baseline, 1 wallet domain model, 2 core operations, 3 `shekyl-cli`, 4 `shekyl-wallet-rpc`, 5 C++ deletion, 6 tests+docs. Phase 2 splits into 2a send / 2b stake lifecycle / 2c addresses+proofs / 2d air-gapped bundles | [`WALLET_REWRITE_PLAN.md`](WALLET_REWRITE_PLAN.md) | The original scheme; always prefixed `Phase` post-2026-06-19 | |
| **Stage 0–5** | Orchestration/actor architecture stages: 0 harness, 1 per-engine trait extraction, 2 KeyEngine actor, 3 `StakeEngine` implementation, 4 actor swaps (e.g. `PendingTxActor`), 5 `ArchivalEngine` | [`WALLET_REWRITE_PLAN.md`](WALLET_REWRITE_PLAN.md), [`STAGE_0_HARNESS.md`](STAGE_0_HARNESS.md), `docs/completed/STAGE_2_KEY_ENGINE_ACTOR.md` | — | |
| **Stage 1 PR 3–7** | Per-engine trait-extraction PRs: 3 KeyEngine, 4 RefreshEngine, 5 PendingTxEngine, 6 PersistenceEngine, 7 EconomicsEngine | `STAGE_1_PR_{4,5,6,7}_*.md`, [`STAGE_1_PER_PR_TEMPLATE.md`](STAGE_1_PER_PR_TEMPLATE.md) | PR 3's docs (KeyEngine + M3 preflights) archived under `docs/completed/` | |
| **M3a–M3e** | Stage 1 PR 3's architectural-inheritance migration steps | `docs/completed/STAGE_1_PR_3_MIGRATION_PLAN.md` | Referenced from `.cursor/rules/16` | |
| **Bond-PR 0–2d** | Bond-construction chain: 0 `P`-derivation, 1 bond builder, 2a–2c KATs+wiring+request, 2d-1 `P`-scan, 2d-2 transport | [`ARCHIVAL_BOND_PR2_CHAIN.md`](ARCHIVAL_BOND_PR2_CHAIN.md) | The chain's §2 arc table is the per-sub-PR record | |
| **SP-0…SP-7, SP-R0** | `P`-scan pipeline slices (2d-1); SP-R0 is the reconcile-GC follow-on — three arms (#1 spent-funding prune / #2 done-side slot GC / #3 full-scan phantom drop) | [`ARCHIVAL_BOND_2D1_PSCAN_PLAN.md`](ARCHIVAL_BOND_2D1_PSCAN_PLAN.md); SP-R0's own round: [`ARCHIVAL_BOND_SP_R0_PLAN.md`](ARCHIVAL_BOND_SP_R0_PLAN.md) (Round 0 ratified 2026-07-18, DQ-A…F) | SP-0…7 shipped grouped as PR-A / PR-B, not one-PR-per-SP; SP-R0 arms sequence behind the staker-activation round (its own round doc) | |
| **PR-A / PR-B** | The two validation-surface bundles that shipped SP-0…SP-7 (#201, #205) | [`ARCHIVAL_BOND_2D1_PSCAN_PLAN.md`](ARCHIVAL_BOND_2D1_PSCAN_PLAN.md) | Named per rule 19 (validation-surface bundling) | Complete |
| **CW-1a…CW-5** | Credit-wire implementation phases (TJ-B step 3): **CW-1a** coinbase `tx_extra` attestation carrier (tag `0x0B`); **CW-1b-i** Rust `BlockAttestationWitness` codec + pairing rule; **CW-1b-ii** prunable height-keyed witness side table; **CW-1b-iv** cross-language KAT + FFI constant gate; **CW-2** the transport bundle (wire field on `block_complete_entry`, IBD serving, reorg survival, bootstrap export/import) — CW-2a/2b/2c are its sub-phases; **CW-3** settlement writer; **CW-4** equivalence KAT; **CW-5** old-vin suppress → reject → delete | [`ARCHIVAL_CREDIT_WIRE.md`](ARCHIVAL_CREDIT_WIRE.md) §3–§5 | Registered 2026-08-05 at birth (rule 94 §1); prefix `CW-` passes the uniqueness check against Phase/Stage/M/Bond-PR/SP/SP-T/PR-A/PR-B/PR-E/CT/GF/S/R/F/DQ/SCE/WI/M1/CB/TJ. **Supersedes the unregistered `PR-B2` token** used in the CW-2 implementation before this row existed: its `PR-B` prefix collided with the registered PR-A/PR-B family above, so a reader could not tell the `P`-scan bundle from the credit-wire slice. Bare sub-phase tokens (`1b-ii`, `1b-iv`) are likewise retired for the prefixed form per §2 | CW-1a/1b-i/1b-ii/1b-iv/2 land in #402; CW-3…CW-5 pending (the verify + settlement + cutover PR) |
| **P-lane fee uniformity** | Uniform canonical fee on every `P`-attributed tx, paid from persona working capital (cover + earnings): typed `P`-space pool selector, `EXIT_FEE_RESERVE_ATOMIC` + sizing assert (✅ re-grounded 2026-07-22, DS-PR-2: `COVER_RUNWAY_FLOOR_ATOMIC` retired with the runway floor; the 2026-07-21 "re-derive against user-supplied working capital" framing was maintainer-corrected — cover is **always protocol-added**, `U(0, bond_floor)`, a top-up is optional; the assert is now the **corner-fraction bound** `0 < reserve < COVER_RUNG_ATOMIC` in `shekyl-standoff::reserve`; see `ARCHIVAL_BOND_CONSTRUCTION.md` §7.2), `ClaimFeeInputsRequired` destitute conditional, debit-path fee-input battery rows, canonical-floor fee (no knob) | `V3_WALLET_DECISION_LOG.md` "P-lane fees" (2026-07-19); gate-4 §3.2 fee note; [`ARCHIVAL_BOND_CONSTRUCTION.md`](ARCHIVAL_BOND_CONSTRUCTION.md) §7.3 | Design ratified 2026-07-19; implementation rides the `Unbond` wallet constructor (SP-R0 arm-#2 production-discharge family) — FOLLOWUPS "V3.x — staker archival" rider | Design ratified |
| **SP-T0…SP-T5** | `P`-isolated transport slices (2d-2): Tor bootstrap, circuit isolation, fetch, …, broadcast | [`ARCHIVAL_BOND_2D2_TRANSPORT_PLAN.md`](ARCHIVAL_BOND_2D2_TRANSPORT_PLAN.md) | Sub-docs: `_SP_T0_TOR.md`, `_SP_T2_FETCH.md`, `_SP_T4_BROADCAST.md` | |
| **PR-E0…PR-E3** | Reward-emission implementation PRs; C-1 (ML-DSA equality + membership-only verify) is PR-E3 step 8. **C-1 LANDED (as of 2026-07-09, PR #277 merged, `dev` `13c368707`)**: all six commit-blocks on `dev` with the F-B5/F-B1 findings fixed in-PR (verified: whitelist entry `cryptonote_format_utils.cpp:706`; `apply_archival_emission_claim` in `blockchain_db`). UPDATE 2026-07-09: #277 merged; track front moves to the E4-gate pair (wallet-side claim builder → regtest e2e), then E4/E5. History: E0/E1/E2 landed; gating design round CLOSED 2026-07-07 (policy trio Q3/Q11/Q12 resolved, WS-1 + WS-2 ratified); PR-E3 verify body, WS-1 held-sourcing correction, and WS-2 write-side landed via PR #269 | [`REWARD_EMISSION_LEG.md`](REWARD_EMISSION_LEG.md), [`REWARD_EMISSION_VIN_PLAN.md`](REWARD_EMISSION_VIN_PLAN.md), [`REWARD_EMISSION_E3_GATING_ROUND.md`](../completed/REWARD_EMISSION_E3_GATING_ROUND.md) | C-1 was the hard Stage-3 emission blocker; residue = wallet-side claim builder + regtest e2e (E4/E5 gate) | C-1 landed (#277); E4/E5 open |
| **CB-1…CB-5** | Wallet-side emission claim-builder round-1 design questions (recompute sourcing, actor locus, claim cadence, fee sourcing, refusal taxonomy) — the E4-gate part-1 work unit | [`EMISSION_CLAIM_BUILDER.md`](EMISSION_CLAIM_BUILDER.md) | Registered 2026-07-09 at birth (rule 94 §1); prefix `CB-` passes the uniqueness check against Phase/Stage/M/Bond-PR/SP/SP-T/PR-A/PR-B/PR-E/CT/GF/S/R/F/DQ/SCE/WI/M1 | **Round 1 CLOSED (2026-07-09), all five ratified at source.** CB-1: new daemon RPC as `EmissionEpochSource` gather; ratifying reason = the single-evaluator invariant `capped_work_milli` (`consensus_state.rs:182-188`) enforces, lying-daemon bound = claim-liveness, safe-by-recomputation, joins the 2d-2 remote-daemon reopen family. CB-2: StakeEngine actor (`stake_engine.rs:16-50` seed-gone-after-`assemble()`), pin = sign within that discipline (use a derived bundle, never re-borrow the seed). CB-3: routing-not-closure, joint-grading reopen (GF-4 grades cadence + amount + holdings stratum jointly). CB-4: ratified on the **structural three-leg exclusion** (backing in opaque `canonical_bytes` never deserialized into `pseudoOuts`; dispatch + leaf size checks `tx_verification_utils.cpp:175` + `rctSigs.cpp:362`), not on the KAT. Arming KAT was necessary-not-sufficient (arm-1 identity green-by-construction); **fixed in-PR**: arm 1 strengthened to production operand functions (`classify_archival_tx` + `shekyl_checked_sum_amounts`, mirror removed) arming the operand + leaf legs, E3 §2.2 rationale corrected, `50-testing.mdc` coverage-boundary discipline pinned. Remaining blob-boundary invariant arm (drive full dispatch, assert verdict invariant under `canonical_bytes` variation) is harness-gated → homed to §8 PR-4, regtest e2e + blob-boundary arm (`FOLLOWUPS.md` Q11-KAT item). CB-5: cause-blind zero-share (single `K_COVER` site `k_cover.rs:11`/`consensus_state.rs:470`), pin = `SelfCheckFailed` must also refuse cause-blind. **Round 2 CLOSED (2026-07-09), CB-1 RPC surface + PR chain** (`EMISSION_CLAIM_BUILDER.md` §7–§8): single-gather pin recorded as CB-1(b) daemon-side face; window-batched `p_id`-only query (transport cause-blindness); field enumeration with the `k_cover`/`frozen_shard_count` exclusion recorded CB-5-structural; §2 boundaries anchored to the rejection predicates (top-strict corrected; bottom verified single-sourced through `claim_window_floor`, consumption-not-re-derivation pinned). Four-PR chain endorsed, PR 4 gated on #281; implementation underway from PR 1 (daemon RPC) with two grep-checkable watch items named at §8 closure. **PR 1 implemented**: `get_archival_emission_claim_source` (both transports, RPC minor 17→18), marshal helper over the single gather in its windowed one-scan form (`gather_archival_emission_window_snapshots` — review hardening: one serve-credit pass per request, thread-aware `block_rtxn_start` txn selection in the archival read helpers) under one `db_rtxn_guard`, `shekyl_archival_claim_window_floor` FFI delegate, Rust decode to the verify-side views (`emission_source.rs`, loud on non-OK `status` and ordering-invariant violations, views via the single-sourced `EpochCloseInputs::verify_view`); both watch items pinned by tests (`archival_claim_source_rpc.cpp`, fixture shared with the substrate KAT via `archival_lmdb_test_helpers.h`). **PR 2 implemented (2026-07-10)**: pure assembly module `engine/emission_claim.rs` — **four-boundary** claimable-epoch derivation (top/expiry/dedup via the connect predicate on a purity-pinned scratch clone + differential oracle test; **strict finalization** via verify's `epoch_close_height` — a review finding: the connect window admits the youngest epoch one count before verify accepts it, verified at `blockchain_db.cpp:504-514`/`archival_claim_source.cpp:30-32`, closed with `EpochSkip::NotFinalized` + premise-asserted boundary KAT after an earlier "foreclosed" claim was disproved at daemon source; recorded as the §2 step-1 post-closure sharpening), M1 positive-share forward obligation discharged (claimable ⇔ recomputed share `> 0`, no `K_COVER` read), canonical work-claim rows with the `u64→u32` scarcity guard armed at the builder site (const-assert production guarantee + `try_from` refusal + hostile-fixture KAT) and the entry-sum hard check, bound-or-split sizing measured by the production encoder at structural maxima, step-7 `self_check_claims` driving the literal `emission_vin_verify_claims` (CB-5 pin discharged: `SelfCheckFailed` a cause-blind unit variant, verify detail log-local; differential KAT — six vin mutations + three context arms, each flips accept→refuse). `InsufficientBacking` + real backing/auth legs defer to PR 3 with their constructors. **PR 2 review round (2026-07-10)**: three correctness fixes — claims budget re-based on the binding per-tx weight limit (`shekyl_wire::TX_WEIGHT_LIMIT` = 149 400 pins the C++ `get_transaction_weight_limit`; the old `MAX_TX_SIZE`-derived budget could pass a permanently-unsubmittable batch), verify step 2's join bound applied at derivation (`epoch_is_before_join` extracted, one definition for verify + builder; retire-then-rejoin no longer wedges the batch behind `SelfCheckFailed`), and the settled/height pair decode-enforced consistent (`settlement_epoch_at_height`; inconsistent daemon reply is one loud `Malformed`, plus Copilot's overflow-close-height ⇒ `SourceInvalid`). Cleanups: scratch-clone oracle retired (read-only `epoch_is_not_settled` extracted, `check_and_set` rewired through it — the V3.1 FOLLOWUPS item landed on its named reopening trigger), steps-4/5 recompute single-sourced (`claimant_reward_share` shared by verify body + derivation), `ClaimableEpochs<'src>` borrows the bond context and carries rows built in the admitting scope (stale-pairing panic surface gone), one vin construction site (`claims_vin`) with pop-in-place bound-or-split, `size_deferred: Vec<u64>`, item-level staging allows (rule 45), shared KAT fixture shape (`EMISSION_KAT_SHAPE`). **PR 3 implemented (2026-07-11, `feat/emission-claim-handler`)**: wire emission-input arm (`0x04`, closes the latent scanner break), `EmissionReward` scan arm (GF-4b item 2), designated-backing selector (`BackingSet` → `DesignatedBacking`, GF-4b §5 item 1 — exclusivity + Q11 fee-exclusion as type properties), `AssembleEmissionClaim` handler (CB-2 `AssembleBond` sibling: F-C1c assembly order, `prove_backing_membership` in `spawn_blocking`, dual auths from the derived bundle — no seed re-borrow — daemon-side differential KAT over the produced bytes), Engine-side claim orchestrator (`engine/claim_orchestrator.rs`: fetch → reference anchoring, daemon two-sided gate ∩ `REF_ANCHOR_AGE` → provable-record filter → designate → fee sweep → path assembly → handler, reply unbroadcast per CB-3; end-to-end test over a real 30k-block `CurveTreeClient` with daemon-side re-derivation against the real root), and the PR-1/PR-2 staging-allow deletion pass (remaining claim-surface allow: the orchestrator + its reply fields, staged against the CB-3 dispatch seam). The builder half of the E4 gate is complete; PR 4 (regtest e2e + blob-boundary arm) is the chain's only open item, gated on #281. **PR 3 review round (2026-07-11)**: six correctness/invariant fixes — slot filter inside `BackingSet::from_spendable` (the all-slot union let a rotation-overlap foreign record win designation → persona deterministically unable to claim), the designation/same-tip anchor is the gather TIP not the block count, the wire validator gained the `check_money_overflow` parity sum for loud vouts, the §7.4 transport pin became structural (`PersonaIsolatedTransport` marker bound on `orchestrate_emission_claim`), the handler enforces Q11 backing∉fee-set at its boundary (`BackingInFeeSet`), and zero fee inputs refuse structurally (`ClaimFeeInputsRequired`, not `InsufficientFunding{0,0}`). Cleanups: shared `two_sided_reference_height` in shekyl-curve-tree (transfer + claim paths), shared `derive_spend_parts`/`construct_vouts_to_base`/`prove_input_from_spend`, lazy memoized `OwnEmissionSlots` P-scan classifier, single test-side wire encoder (`source_json`), rule-22 CLAUDE.md index row. **UPDATE 2026-07-12 — PR 4 split 4a/4b/4c; PR-4a implemented (`feat/emission-regtest-e2e`)**: driving the harness live proved the daemon's Rust submit engine accepts neither transaction kind the e2e needs (no bond-post Phase-C battery — and `phase_a.rs`'s "bond posts can't clear Phase A" docs are stale, disproven by a live `FeeTooLow`; Phase A rejects `Input::ArchivalRewardEmission` outright), so the daemon submit legs split out per rule 19/06 (`EMISSION_CLAIM_BUILDER.md` §8 "PR 4 split"). PR-4a lands: the CB-4 blob-boundary arm (**Q11-KAT FOLLOWUPS item CLOSED** — fixture made valid under full `ver_non_input_consensus` dispatch, verdict-invariance arm under `canonical_bytes` variation, oversized-`pseudoOuts` control), the FAKECHAIN-only settlement-epoch-blocks env lever + serve-credit-bit injection RPC (e2e enablers, both fail-closed off FAKECHAIN), `LocalNodeRpc` (local-posture `PersonaIsolatedTransport`, DQ-T2.3 choice site), the `SweptFeeInputs`/`ClaimOperands` designation-event witness (PR-3's named forward item, landed with the seam as scoped — both step-2 runtime refusal arms deleted) + the CB-3 dispatch seam (`engine/claim_dispatch.rs`: read-only identity projection → orchestrate → persist-before-dispatch seal → posture→submit choke point; the last claim-surface staging allows deleted), the staker regtest harness up to the daemon submit gap (production transfer → P-scan discovery → bond assembly → dispatch, with the `Malformed` tripwire pinning the gap), and two harness-surfaced production bug fixes with unit KATs (payment path encapsulated to `montgomery(view_pk)` — cross-wallet payments were unrecoverable; P-scan exhaustiveness fetches full tx bodies — pruned FCMP++ spends can never re-hash to their committed hashes). **PR-4a review round (2026-07-12, high-effort workflow review — 29 verified findings, all addressed in one round; record in `EMISSION_CLAIM_BUILDER.md` §8)**: the SEB override converted to explicit **arming** (unarmed processes — every wallet, every public-net daemon — run the genesis schedule and warn once on an ignored lever; the daemon arms only on FAKECHAIN, refuses on the lever's presence off it, refuses invalid values loudly instead of clamping, and pins the effective schedule into the fakechain datadir — reopen-under-a-different-schedule is a loud refusal); the CB-3 seam gained **persist-before-dispatch** (`PendingEmissionClaim`, pending-post block v4: bytes + fee-gindex reservation into the shared `reserved_gindexes` union + claimed epochs; one live claim per persona = in-flight dedup, `ClaimPending` refusal; retirement/resubmit = the named claim-driver slice) and lost its rotation side effect (claimant id via the read-only `PersonaIdentityOf` projection — activation would invalidate in-flight handles and wipe a retired ephemeral persona); typed taxonomies: `NoSpendableFunding` (empty eligible set ≠ zero-value shortfall; the `ClaimFeeInputsRequired` mapping keys on the typed arm, not `available == 0`) and `OutputNotYetDrained{gindex}` (the harness's substring match on a rendered error deleted); the P-scan full-body fetch names the storage-pruned-daemon cause + remedy; credential grammar authority-scoped at the source (`SimpleRequestRpc`) with `LocalNodeRpc`'s pin moved to `IpAddr::is_loopback`; consolidations: single `DaemonEngine::fetch_scannable_block_in_form` override point, `BroadcastSubmitter::local` (three copies deleted), `EpochCloseInputs::close_view` (FFI lockstep retired), `EmissionClaimReceipt` embeds the reply, shared e2e `create_wallet`, mtime-gated `pscan_until`, `tokio::join!`ed seam reads; `CORE_RPC_VERSION` → 3.19. **UPDATE 2026-07-18 — PR-4b bond-post half implemented**: the JoinMarket bond-post Phase-C battery wired in `shekyl-daemon-rpc` `verifier.rs::verify_bond_post` (§8.7.1 BP-rows closure note: O6 → bond CT balance → BP2/BP5/BP3+BP4 as native `shekyl-archival-retention` calls — the same functions the C++ oracle reaches over FFI — → Bp+ → funding-subset FCMP++ → all-input PQC), `SubmitFacts` extended with the BP3 `bond_record_exists` fact (snapshot probe keyed on the vin's claimed id + Phase-D re-probe from the reparsed blob; record-appears-during-C → `DoubleSpendConflict`), the stale "§13 wire reshape / cannot clear Phase A" reachability docs corrected (no wire contradiction existed), non-JoinMarket kinds refused under a named rule-21 reopen, the production-stack bond-post oracle fixture + BP mutation KATs added to `submit_verifier.rs`, and the PR-4a tripwire promoted to accepted-and-applied — surfacing + fixing two latent production bugs (transfer path omitted the `0x07` PQC leaf-hash blob ⇒ every transfer output unspendable; `prepare_handle_incoming_blocks` `bad_variant_access` on archival-vin blocks; `docs/CHANGELOG.md`). **UPDATE 2026-07-19 — PR-4b COMPLETE (emission half)**: §8.7.2 E-rows pinned; `SubmitTxKind::Emission` + Phase-A statics; `SubmitFacts::emission` E6/E7 bundle (C++-owned-handle marshal, one lock scope); native `verify_emission` battery over the shared `emission_vin_verify_*` minters; E6 Phase-D claim-slot re-probe → `DoubleSpendConflict`; FOLLOWUPS "daemon Rust submit engine" item CLOSED. Open: the claim dispatch driver's retirement/resubmit legs, then **PR-4c** (e2e proper — now unblocked on the daemon side; the E4 merge gate closes there). **UPDATE 2026-07-19 — PR-4c LANDED, the E4 merge gate CLOSED**: `e2e_emission_claim_accepted_and_applied` drives the full composition live under the SEB lever (epoch 1 is the first claimable epoch — onset stagger; single claimant ⇒ loud vout == `budget_atomic(E)` byte-exact over real RPC — the §9.5 item-8 conservation identity; real ToKey fee inputs, fee sized over the production `EMISSION_NON_CLAIMS_RESERVE_BYTES` envelope; production P-scan re-discovers the reward as rung-1 `EmissionReward`), plus three pop legs (depth-1 idempotency; epoch-straddling re-close byte-identical from the pop-surviving credit bits, floor above the non-pop-symmetric inject; deep pop through the claim's reference — chain re-converges, stranded claim inert, replacement claim = the retire/resubmit FOLLOWUPS item). Composition findings fixed/pinned in-PR: the daemon **arm-order bug** (fresh-datadir genesis add latched the settlement-epoch schedule before the `Blockchain::init` SEB arm ⇒ every levered spawn died `ArmedTooLate`; the gate moved above the genesis add) and the **market-bond wallet entry gap** (`first_stake`'s CompleteTree genesis posture is foundation-flagged by the gather (`bond.is_complete_tree()`) and market-excluded by E-2 ⇒ Σwork 0 forever, no wallet-postable bond can earn; the e2e claims via the composed production steps `FixtureStake::MarketBond`, and the entry is a named FOLLOWUPS item gated on gate-6 serving / SA-R2). E4/E5 (§4 item 5) closed docs-only — zero `C_stake` code residue; the `staker_emission_decay` leg stays unit-pinned (14 KATs, year-scale horizon); fee-pool-half positivity is regtest-infeasible by the integer volume-mean law, pinned executably as `total_burned == 0` with B5 carrying positive-pool coverage |
| **CT-1…CT-5 (5a–5d)** | Curve-tree client series | [`CURVE_TREE_CLIENT.md`](CURVE_TREE_CLIENT.md); closeouts in `docs/completed/CT*` | Series closed (`CT5_SERIES_CLOSEOUT.md`) | Complete |
| **GF-1…GF-12** | Gate-6 firewall findings; **GF-7** = the funding-seam (principal↔`P`) unlinkability property, a genesis gate | [`ARCHIVAL_FIREWALL_GATE6.md`](ARCHIVAL_FIREWALL_GATE6.md) | GF-7's measurement hooks: [`ARCHIVAL_BOND_2C_GF7_HOOKS.md`](ARCHIVAL_BOND_2C_GF7_HOOKS.md). GF-4b's backing-lineage design + landing record (incl. the **GF4b-1…GF4b-6** review-round findings, sub-tokens scoped to that doc's §6): [`ARCHIVAL_GF4B_BACKING_LINEAGE.md`](ARCHIVAL_GF4B_BACKING_LINEAGE.md) | |
| **S-1…S-6** | Gate-6 §10.12 seam-scenario rows (adversary fusion analysis) | [`ARCHIVAL_FIREWALL_GATE6.md`](ARCHIVAL_FIREWALL_GATE6.md) §10.12 | Not to be confused with S6 the certify-draw plan (`docs/completed/ARCHIVAL_BOND_S6_CERTIFY_DRAW_PLAN.md`) | |
| **F-D1…F-D6** | Gate-6 R4 drain-event findings: F-D1 amount taint-carve, F-D2 UI-default, F-D3 exit-standoff mechanism, F-D4 a-priori exit window, F-D5 quantization (disposition round ran 2026-07-17: no grid at genesis — see status), F-D6 anti-drift derive | [`ARCHIVAL_FIREWALL_GATE6.md`](ARCHIVAL_FIREWALL_GATE6.md) §12 | Minted 2026-07-11 (R4 draft); registered 2026-07-15 (backfill — the minting PR omitted the row). Prefix `F-D` is distinct from `F-N`'s `F<digits>` tokens (no parse collision) | F-D5 **disposition round RAN — width does not derive, NO GRID at genesis** (as of 2026-07-17, same day as the charter; Gate-6 §12.7 OUTCOME: the harm survives F-D1 in one sentence — the grid targets §18.12's lifetime-aggregate drain-all floor, not the subsum match F-D1 closes — but the structural derivation fails at three source-anchored population entry points: `r_market` inside `scarcity_milli` (no population-free lattice below the `Curve`'s shape constants), the `budget(E)/Σwork(E)` value spacing (a quotient of two economics quantities; X-3's path had no such quotient), and reachable-collision ≠ delivered cover (epoch sets published per claim, `reward_P(E)` a loud `P`-attributed mint vout, so collisions among published sums are population occupancy); the grid's cost side is derivable, its benefit side is not; pass-4's cost half transfers intact; Gate-6 close condition (iv) **discharged**, the band registered as the first R5 S-2 ledger row; any future grid is a hard fork per the §12.7 reopen clause). Charter context: F-D5 **disposition round CHARTERED** (as of 2026-07-17 — the "→ §14.4 economics" routing was a dangling pointer: WI-4 §14.4 is the closed partition arm, and the phrase was a summary-layer conflation of WI-4's strata/lifetime riders (discharged, PR #291) with its quantization GF-4-candidate routing (§18.10 R-3); Gate-6 close condition (iv) re-worded by dated amendment (§12.9), charter at §12.7: a fresh measurement round refused per method note 3 (the grid's privacy benefit is behavioral, testnet-unproducible), the round runs a **structural-derivation attempt first** (grid width from reward-curve structure, rate-independent, X-3-shaped) with the genesis-frozen consequence named (no derivation ⇒ no grid at genesis; post-genesis = hard fork) and the §10.12 pass-4 banding rejection reconciled explicitly; the lifetime-aggregate band registers as an R5 S-2 ledger row either way); F-D3/F-D4 **DELETED — deletion PR landed** (as of 2026-07-17, at Gate-6 §12.9 decision 5's scope: `exit.rs` wholesale + both features + consumer acknowledgment lines + conformance/test arms; F-D6 and `release_cooldown_elapsed` confirmed out of scope; inventory at FD4 §15.4 item 1's landing update); F-D6 **named-const half STANDS; anchor helper DELETED** (as of 2026-07-17 — the deletion PR orphaned `release_cooldown_anchor_height`, its only production consumer was `draw_exit_gap`; deleted per rule 15 by its own decision rather than inheriting decision 5's exclusion, keep-as-derivation-home declined as speculative generality; reopen note at `release_cooldown.rs`, disposition at Gate-6 §12.7); F-D1/F-D2 **UNBLOCKED, re-homed** (§15.5 answered — Gate-6 §12.9: the exit seam is the principal↔user crossing, WI-4 §18.13; F-D1/F-D2 are its class-3 mistake-set closure, build/arms unchanged; F-D2 additionally carries the accepted FD4 §16.4 funding default). UPDATE 2026-07-17 (later): **F-D1 BUILT** (`shekyl-engine-core/src/engine/{drain_orchestrator,drain_amount,drain_select}.rs`; (a)+strip — the orchestrator is the sole trust boundary naming/holding `PFundingOutputRecord` (which carries `MintLineageOutput`), projecting to the aggregate scalar + stripped `{output_id,amount,spendable_height}` vector; M1 grep-arm landed on a skeleton first, proven-to-bite on a deliberate forbidden import (exit 101), reverted, then armed as `fd1_arm_*` unit tests; Gate-6 §12.3 build note) and **F-D2 core-side surface LANDED** (aggregate-only `DrainBalance`/`drain_balance`, scalar-only `plan_drain`; Gate-6 §12.4 build note) — `plan_drain` lands as a correctly-carved planner with **no data source and no consumer yet**. **F-D2's remaining half is NOT a pending UI default and NOT gated on RPC** (the GUI links the engine in-process; `shekyl-engine-rpc` is an FFI library, not a wallet-RPC server): it is a whole **unbuilt `P`-value-out (drain-send) subsystem** in `shekyl-gui-wallet` — a `P`-scan data source (`PScanState.funding_outputs`) feeding the planner + a drain tx assemble→sign→broadcast path + the round-number/random-split default on top. **R4 open on that subsystem.** UPDATE 2026-07-17 (follow-up): F-D6's anchor helper `release_cooldown_anchor_height` deleted (verified: `git grep release_cooldown_anchor_height` → doc references + the `release_cooldown.rs` reopen note only) — the mechanism deletion removed its only production consumer, and the orphaning got its own rule-15 decision; F-D6's named-const half stands. UPDATE 2026-07-17: the mechanism-deletion PR landed (verified: `git grep draw_exit_gap` on the branch → doc references only); R4 close condition (iii) satisfied. UPDATE 2026-07-16 (night — R4 decision round, Gate-6 §12.9, RATIFIED): §15.5 answered as above; joint grade dissolved by **four-axis** attrition (ratification added F-W10 — output-count phantom, the drain is not an identifiable transaction under FCMP++; GF-10 grades standalone); harness fork resolved deleted (not resurrected). UPDATE 2026-07-16 (round 4 ratified): the exit-timing channel is phantom (F-W7 — no principal-side observable within the seam, FD4 §2.1/§15; F-W8 retracted X-3's harm model); delete-mechanism-keep-tripwire ratified, `RELEASE_CHECKLIST.md` seal entry removed, harness sequenced behind the §15.5 gate question, sentinel never shipped a value (K_COVER pattern vindicated, FD4 §15.7). UPDATE 2026-07-16 (later same day): the F-D4 §9 step-4 sweep was built and run (`shekyl-staking-sim --exit-standoff`; findings at FD4 §14) — the instrument **never landed on `dev`**; preserved at `archive/feat/fd4-exit-sweep-2026-07-16` per the FD4 §15.4 landing disposition, its fate riding the §15.5 answer. UPDATE 2026-07-16: `draw_exit_gap` landed against the F-D4 sentinel (verified: `shekyl-standoff/src/exit.rs` — typed draw, golden vector + seal tripwire, shared-trigger negative control); F-D6 anchor derivation landed (`release_cooldown_anchor_height`, boundary-tested against `release_cooldown_elapsed`; staking-sim `20_000` doc fossil purged). UPDATE 2026-07-15: F-D3/F-D4 gate FIRED, open for build; F-D4 derivation committed (`ARCHIVAL_EXIT_STANDOFF_FD4_WINDOW.md`) |
| **X-1…X-3** | Exit-seam standoff bounds/regimes: X-1 steady-state lone exit (rate-driven lower bound), X-2 L17 crisis cohort (intra-cohort mixing upper bound), X-3 anchor-merge (rate-independent lower bound `W ≥ 2 × SEB`, derived from the quantization lemma) | [`ARCHIVAL_EXIT_STANDOFF_FD4_WINDOW.md`](../completed/ARCHIVAL_EXIT_STANDOFF_FD4_WINDOW.md) §4–§5 (X-3 at §5.2a) | Minted + registered 2026-07-15 (X-3 added same day, review round 3). Prefix `X-` unused by any other family | **CLOSED by round-4 premise audit** (as of 2026-07-16) — X-3 harm model retracted (F-W8: cohort membership is `last_served_epoch`, already public; the partition only fed assignment, a function of the phantom `T`); X-1/X-2 conditional on `T` and archived as the FD4 §15.4 tripwire's re-run template; no seal. UPDATE 2026-07-16 (round 4 ratified): deletion-with-tripwire per FD4 §15.4; the geometry (quantization lemma, coverage boundary) survives as public-height arithmetic, the harms do not. UPDATE 2026-07-16: the §8 sweep was built and run (`shekyl-staking-sim --exit-standoff`, `exit_standoff.rs` — never landed on `dev`; archived at `archive/feat/fd4-exit-sweep-2026-07-16`) — X-1 N-sweep, X-2 cohort grid, X-3 coverage boundary certified (measured overlap = `(W − SEB)/W` at all three windows), §8.3 negative control caught, thin regime unbiased; pre-seal findings at FD4 §14 (delivered X-1 cover is `ρ_x · σ_L`, latency-gated; X-2 trough needs `σ_L` on the `W/2` scale — 71/72 rows reported failing per GF7 §5.1, the §5.3 costing surface). UPDATE 2026-07-15: review rounds 1–3 run (X-1 bound reframed as a ~19× planning box per F-W3; X-3 folded into the §5.4 rule per F-W6); sweep pre-registered |
| **F-W1…F-W10** | F-D4 window-derivation review findings. Round 1: F-W1 candidate-fails-own-bound (blocking), F-W2 `σ_L`-is-a-design-lever (load-bearing; §5.3 joint constraint, lever-vs-queue costing). Round 2: F-W3 value-not-derivable-pre-measurement (blocking; `K_COVER`-pattern sentinel + frozen decision rule, sealed by the Phase 7.7 stressnet read), F-W4 stacked-marginal-conservatism (10th percentile of the joint `(N_P, c)` read committed a-priori), F-W5 exit-seam `N_t` re-derivation owed before the seal. Round 3: F-W6 structural-argument-is-a-derived-bound (X-3 anchor-merge folded into the rule as `max((N_t−1)/ρ_x, 2 × SEB)`, cost stated: queue-predicate LHS hard floor makes the F-W2 costing load-bearing in every measured world). Round 4: F-W7 phantom-`T` premise finding (blocking — the correlated observable was never named; no population within the seam), F-W8 X-3 harm-model retraction (cohort membership already public). Post-ratification: F-W9 repetition-premise correction (§13.3's `m` is a bounded lifecycle count, not a growth variable — one mandatory observable crossing; the cross-persona `q^k` relocation raised and withdrawn in the same section for want of a linking key); F-W10 output-count-axis phantom (Gate-6 §12.9 decision 2 at ratification — under FCMP++ the drain is not an identifiable transaction, so its output count is not an observable; the §2.4 pin was a CryptoNote-lineage carry never re-walked across the crypto change) | [`ARCHIVAL_EXIT_STANDOFF_FD4_WINDOW.md`](../completed/ARCHIVAL_EXIT_STANDOFF_FD4_WINDOW.md) §10–§12, §15–§16; F-W10 at [`ARCHIVAL_FIREWALL_GATE6.md`](ARCHIVAL_FIREWALL_GATE6.md) §12.9 | Minted + registered 2026-07-15 (F-W3–F-W5 round 2; F-W6 round 3, same day); F-W7/F-W8 minted round 4, 2026-07-16; F-W9 minted post-ratification same day (doc §16); F-W10 minted at the §12.9 ratification same day (Gate-6 doc). Prefix `F-W` distinct from `F-`/`F-D` (no parse collision) | **All resolved (F-W1–F-W10, as of 2026-07-16) — round 4 RATIFIED: deletion-with-tripwire (doc §15.4); F-W9 rode the §15.5 hand-forward, ANSWERED at Gate-6 §12.9 (seam re-homed; §16.4 default accepted); F-W10 added at the §12.9 ratification (fourth attrition row — output-count phantom; Gate-6 §2.4 discipline retired; method note 5 adopted)**. UPDATE 2026-07-16 (F-W9, post-ratification): §13.3's repetition premise corrected by lifecycle enumeration (doc §16) — `m` = 1 mandatory observable crossing (`JoinMarket`; `bond_post.rs:540` one-per-life), 2 wallet-default-closeable optional classes (`HoldingsUpdate`-add, credit-bearing `Rebond`), 0 observable exits (voluntary refund CT-hidden in-tx; terminal slash emits nothing), rejoin unmodeled (self-harm, slot-indexed derivation); geometric-collapse clause and the residual's `σ_L` routing fall, S-2 routing survives narrowed (its composition question now finite); cross-persona intersection withdrawn (§16.3 — no linking key across `P_id`s; surviving surfaces are the T-4 counterparty ledger and the entry standoff's shared-trigger case); §16.4 GF-4b-filter proposal handed to R4 with its growth-gating cost; Gate-6 §11.8 method note 4 adopted (*enumerate the events that compose*); WI-4 §13.5 + FOLLOWUPS carry-4 citations swept. UPDATE 2026-07-16 (ratification): F-W7/F-W8 resolved — deletion ratified with the harness sequenced behind the §15.5 gate question (handed forward to Gate-6 R4, audit attached: does GF-4's exit seam re-home to §18.13, and what does the joint grade become without its timing axis); `RELEASE_CHECKLIST.md` seal entry removed; observer-as-code adopted as method posture (doc §15.6 + Gate-6 §11.8 method note 2). UPDATE 2026-07-16 (round 4): premise audit run (doc §2.1/§15) — no population for the principal-side re-appearance `T` within the seam (refund structural: in-tx hidden outputs, `bond_connect.rs`; rotation dead by S-5/T-A1 + in-place `HoldingsUpdate`; network spent as §10.9 conditioning; off-chain crossing re-homed to Gate-6 §18.13/S-2). §5–§14 outputs conditional; sentinel holds the frozen state by construction. UPDATE 2026-07-16: F-W5 resolved a-priori before the sweep grades (derivation doc §13) — **exit-seam `N_t = 10`, by derivation, not inheritance** (seam-variant facts land on `W`/graded arms/regime rows; the repetition asymmetry cannot be priced into a per-event anchor and routes to `σ_L` + the S-2 exposure ledger as a named residual; `EXIT_TARGET_ANON_SET` minted at the sweep-harness PR). Same day: `draw_exit_gap` **landed** against the sentinel (verified: `shekyl-standoff/src/exit.rs`). UPDATE 2026-07-15: F-W1–F-W4, F-W6 resolved by same-day amendments (F-W1's round-1 fix superseded by F-W3's sentinel reshape); F-W5 pinned, derivation owed pre-seal |
| **Round N / R N** | Per-doc adversarial design rounds; each design doc numbers its own | Owning doc's round table | Gate-6 **R1–R2 closed** (R2 network/transport closed 2026-07-11, `ARCHIVAL_FIREWALL_GATE6.md` §10.13; its subject = the 2d-2 transport track, still partial-built/inert per §4); **R3 designed + adversarial pass run** (timing/rotation, §11 — GF-10 claim-jitter pinned as mechanism + structural window; grading routed to the R4/GF-4 joint grade; §11.8 pass folded 4 findings incl. one retraction); **R4 drafted** (§12, the drain-event firewall — GF-7 graded PROVISIONAL-PASS; GF-4 value-out: F-D1 drain-amount taint-carve complete pre-code pin (a)+strip, F-D2 UI-default, F-D3/F-D4 exit standoff — **FSM gate FIRED 2026-07-15**, F-D5→~~§14.4~~ *(dangling — disposition round ran 2026-07-17, no grid; see the F-D row)*, F-D6 anti-drift; joint-graded); R5 planned (cross-layer sign-off — S-2 ledger + S-3 sim). UPDATE 2026-07-15: the F-D3/F-D4 activation criterion (Gate-6 §12.5: a verify path reads the cooldown as a spendability gate) FIRED — `release_cooldown_elapsed` is enforced consensus with two verify consumers (verified: `bond_post.rs:369` `HoldingsUpdate`-drop per-shard anchor, `:627` `Unbond` whole-record anchor; FSM landed via PR #303/#307). The R4 pre-seal blocker chain (FSM frictions → age-stratified sim reconciliation) was already discharged (`STAKER_ARCHIVAL_SIM.md` §L18; P2B-7 Pin-4/Pin-5 closed PR #303 2026-07-14). F-D3/F-D4 are open for build (`draw_exit_gap` unbuilt — `shekyl-standoff` `draw.rs` carries only `draw_entry_gap`); Gate-6 §12.5–12.8 + `PHASE_2B_FSM_RETOOL.md` P2B-7 residuals row swept same day. The F-D4 a-priori window derivation is committed (`ARCHIVAL_EXIT_STANDOFF_FD4_WINDOW.md`, 2026-07-15); rate-model review rounds 1–2 run same day — round 1: F-W1 (candidate failed its own X-1 bound), F-W2 (`σ_L` reclassified as design lever; §5.3 joint-constraint costing, lever vs queue); round 2: F-W3 (no pre-measurement value derivable — the bound is a ~19× planning box; `DEFAULT_EXIT_GAP_WINDOW` converted to a `K_COVER`-pattern provisional sentinel with the decision rule frozen, sealed by the Phase 7.7 stressnet rate read per `RELEASE_CHECKLIST.md`), F-W4 (joint-percentile conservatism committed), F-W5 (exit-seam `N_t` re-derivation owed pre-seal); round 3: F-W6 (X-3 anchor-merge bound `W ≥ 2 × SEB` derived from the quantization lemma, folded into the frozen rule as a two-term `max` with its queue-predicate cost stated). `draw_exit_gap` **unblocked**, written against the sentinel (doc §5.4/§9/§11–§12). UPDATE 2026-07-16: `draw_exit_gap` **BUILT** against the sentinel (`shekyl-standoff/src/exit.rs` — `ExitGapWindow`/`ExitGap` newtypes, compile-refusal wiring, golden vector + seal tripwire, shared-trigger negative control) and the F-D6 anchor derivation landed (`release_cooldown_anchor_height` in `shekyl-archival-retention`, boundary-tested against the consensus predicate; staking-sim `20_000` doc fossil purged). R4's timing channel now awaits the F-W5 `N_t` re-derivation, then the X-1/X-2 sweep into the joint grade. UPDATE 2026-07-16 (same day): F-W5 resolved (F-D4 doc §13, `N_t(exit) = 10` derived) — the X-1/X-2 sweep is unblocked and is what remains before the R4 joint grade; the seal stays a Phase 7.7 obligation. UPDATE 2026-07-16 (same day): the **§8 sweep instrument built and run** (`shekyl-staking-sim --exit-standoff`; `EXIT_TARGET_ANON_SET` minted; frozen rule encoded as `frozen_rule_window` with planning-box KAT; instrument never landed on `dev` — archived at `archive/feat/fd4-exit-sweep-2026-07-16`) — pre-seal findings at F-D4 §14 (X-1 delivered cover latency-gated `ρ_x · σ_L`; required-`σ_L` surface now prices the §5.3 lever-vs-queue decision; X-2 trough rows reported failing per GF7 §5.1 as the pre-committed costing surface). R4's timing channel has its joint-grade input; **R4 close awaits the four-axis joint grade + F-D1/F-D2 arms**; the seal stays Phase 7.7. UPDATE 2026-07-16 (later same day): **F-D4 round 4 run and RATIFIED — the premise audit (F-W7/F-W8, FD4 §2.1/§15)**: the exit-timing channel's correlated observable `T` is unpopulated within the seam (refund structural, rotation policy+economics, network conditioning, off-chain crossing re-homed); X-3's harm model retracted; **deletion-with-tripwire ratified** (no seal — the checklist entry is removed; the sentinel never shipped a value). R4's close condition re-forms: the FD4 §15.5 **hand-forward** (Gate-6 §12.8 — seam re-homing to §18.13 + the joint grade's axis set without its on-chain timing axis) now precedes the grade; F-D1/F-D2 arms pend the same answer. Method posture adopted: observer-as-code, pre-registered (Gate-6 §11.8 method note 2). UPDATE 2026-07-16 (post-ratification): second method posture adopted — **mechanism-vs-economics measurement filter** (FD4 §15.8 + Gate-6 §11.8 method note 3 + `RELEASE_CHECKLIST.md` queueing rule): two of the three FD4 seal inputs were economics numbers a testnet cannot produce in principle (true before F-W7); every future stressnet-seal candidate classifies first; PF-9 queue audited — `K_COVER` passes (mechanism), GF-7's cover-level residual caveated at Gate-6 §12.8. UPDATE 2026-07-16 (same day): the GF-7 caveat's follow-through — the effective-cover *level* reclassified as the **fifth WI-4 §13.5 standing conditional** (conditions the number itself, not a channel; the `1.86` is stacked marginals — pessimistic adversary panel × optimistic nominal cover), and its shape measured via the new `shekyl-staking-sim --gf7-breakeven` sensitivity arm (`gf7_breakeven.rs`, mechanism-class): worst-arm `r` clears the bound at every row of `N ∈ [2, 16]`, never trending toward the bar as cover thins ⇒ **`r < 2` is structurally blind to cover** (cover was never gated by it; the ~7% margin is relative-leak margin, not cover margin) and **no per-event monitoring threshold exists** — the candidate `P(link) ≤ 0.2` is the nominal-cover assumption restated by identity (parity `N ≈ nominal · r/bound` forced by flat `r`), refused as an absolute bar; the conditional's instrument is the unbuilt R5 S-2 lifetime exposure ledger (WI-4 §13.5 pins the register as the aspiration's ledger and records S-2's permanent deferral as itself a finding). UPDATE 2026-07-16 (evening — F-W9, FD4 §16): §13.3's repetition premise corrected before R4 takes the §15.5 hand-forward — `m` is a bounded lifecycle count (1 mandatory observable crossing = `JoinMarket`; 2 wallet-default-closeable optional classes; 0 observable exits; rejoin unmodeled), so the on-chain composition a re-homed grade would inherit is finite and GF-7-shaped (at `m = 1` the per-event grade *is* the on-chain lifetime grade, per observer class, at nominal cover); cross-persona `q^k` raised and withdrawn (no linking key — FD4 §16.3); the §16.4 GF-4b-filter wallet-default proposal rides the hand-forward with its growth-gating cost stated; third method posture adopted — **enumerate the events that compose** (Gate-6 §11.8 method note 4). UPDATE 2026-07-16 (night — **R4 DECISION ROUND run, Gate-6 §12.9**): the §15.5 hand-forward **answered** — (1) GF-4's exit seam **re-homes** to the principal↔user crossing (WI-4 §18.13; both halves reduce — timing phantom, amount already class-3; F-D1/F-D2 re-labeled as that seam's mistake-set closure, build/arms unchanged; the on-chain `P`↔principal surface is now the entry seam alone); (2) the four-axis joint grade **dissolves by axis attrition** — CB-3's obligation discharged (one live axis left), **GF-10 grades standalone** against its §11.5 pre-committed advantage claim (R3's close re-formed accordingly); (3) F-D4 §16.4 funding default **ACCEPTED** as F-D2-class (self-fund default via GF-4b lineage; external growth-funding = loud override routed through the entry standoff); (4) harness fork resolved **deleted** (rate model over counterparty arrivals fails method-note-3; never landed, not resurrected) and the mechanism-**deletion PR scoped** (`exit.rs` wholesale; F-D6 + `release_cooldown_elapsed` out of scope). R4's re-formed close: F-D1 + F-D2 (incl. funding default) + deletion PR + F-D5 §14.4 disposition; S-2 is R5's opening item with F-W9's finite domain. UPDATE 2026-07-16 (night — **§12.9 RATIFIED at review-at-source**): decisions 1, 4, 5 as drafted; the dissolution pair (2–3) **amended and ratified** — the draft's axis walk had silently omitted output-count, and ratification added it as the fourth attrition row (**F-W10**: phantom — the drain is not an identifiable transaction under FCMP++, so its output count is not an observable; Gate-6 §2.4's lump-sweep discipline retired as a CryptoNote-lineage carry never re-walked across the crypto change; §11.8 method note 5 adopted: *"carry from X" is a re-walk trigger, not an exemption*). The dissolution stands strengthened; the round is closed as ratified. UPDATE 2026-07-17: the decision-5 **mechanism-deletion PR landed** (`exit.rs` wholesale + features + acknowledgment lines + conformance/test arms; F-D6 anchor re-anchored, out of scope) — R4's re-formed close condition (iii) is satisfied; what remains of the close is F-D1 + F-D2 (incl. the funding default) + the F-D5 §14.4 disposition. UPDATE 2026-07-17 (later): **close condition (iv)'s "§14.4" was a dangling pointer** (WI-4 §14.4 = the closed partition arm; the phrase conflated WI-4's discharged strata/lifetime riders with its quantization GF-4-candidate routing) — re-worded by dated amendment (Gate-6 §12.9): the **F-D5 disposition round** (charter at Gate-6 §12.7) runs a structural-derivation attempt first (grid width from reward-curve structure, mechanism-class; fresh measurement round refused per method note 3), names the genesis-frozen consequence (no derivation ⇒ no grid at genesis), reconciles the §10.12 pass-4 banding rejection, and registers the lifetime-aggregate band as an R5 S-2 ledger row either way. UPDATE 2026-07-17 (same day): **the F-D5 disposition round RAN — width does not derive, NO GRID at genesis; condition (iv) DISCHARGED** (Gate-6 §12.7 OUTCOME: harm survives F-D1, derivation fails at three population entry points — `r_market` in `scarcity_milli`, the `budget(E)/Σwork(E)` spacing, reachable-collision ≠ delivered cover; cost side derivable, benefit side not). **R4 is open on F-D1 + F-D2 only**; the band is the first registered R5 S-2 ledger row | |
| **F-N** | Findings within a specific design doc's review (e.g. F40 `AlreadyInChain`, F41 `Conceal` constant-work in the daemon-submit series) | The doc that numbered them, e.g. [`DAEMON_SUBMIT_VERDICT.md`](DAEMON_SUBMIT_VERDICT.md) | F-numbers are doc-scoped, not global | |
| **DQ-N** | Design questions in the 2d-2 transport plan | [`ARCHIVAL_BOND_2D2_TRANSPORT_PLAN.md`](ARCHIVAL_BOND_2D2_TRANSPORT_PLAN.md) | — | |
| **SCE-N** | Serve-credit C++-decision equivalence-audit findings (D-SC-A/B/C mirror + standing KAT; SCE-1 = the `(P,s,E)` key BE/native-endian encoding split) | [`ARCHIVAL_SERVE_CREDIT_EQUIVALENCE_AUDIT.md`](ARCHIVAL_SERVE_CREDIT_EQUIVALENCE_AUDIT.md) | Registered 2026-07-09 at birth (rule 94 §1); prefix `SCE-` passes the uniqueness check against Phase/Stage/M/Bond-PR/SP/SP-T/PR-A/PR-B/PR-E/CT/GF/S/R/F/DQ/WI/M1 | |
| **WI-1…WI-4** | Post-#255 archival-bond work items: 1 `start_pscan` lifecycle wiring, 2 production bond assembly + pending-post persistence, 3 block-timed dispatch driver, 4 GF-7 measurement round | This index §4 (rows) + the carrying design docs per row | Registered 2026-07-05; prefix `WI` passes the rule-94 uniqueness check against Phase/Stage/M/Bond-PR/SP/SP-T/PR-A/PR-B/PR-E/CT/GF/S/F/DQ. **Ephemeral family (rule 21 retire criterion):** one plan's work breakdown over already-durable §5 gaps, not a durable cross-doc family — the row retires when WI-4 lands (its items live on as the §5 gap closures and per-doc rows they map to) | W2 Pending |
| **MS-1…MS-8** | V3.1 multisig **Rust engine integration** Round 1 design questions (trait identity, C++/Rust boundary, feature gate, receive/spend/transport, persistence; **MS-8 RETIRED** 2026-07-14 → MSW-2) | [`V3_1_MULTISIG_RUST_ENGINE.md`](V3_1_MULTISIG_RUST_ENGINE.md) | Registered 2026-07-14 (rule 94 §1); prefix `MS`. Track B only. | **Round 1 CLOSED 2026-07-16.** All blockers discharged: F-3 DELETE (Option A fossil) + F-6 CI (`--features multisig` green) in **#310** (merged 2026-07-15); **F-7** (`EngineSignerKind::SigningCeremony`, `SoloSigner = Infallible`) in **#317**. Track B (MS-1…MS-7) → Round 2 / MS-5 (Option E′) on explicit go-ahead. Pre-MS-5 gate no CI can turn green: the `MultisigSigner` `N`/`K` decision. |
| **TJ-A…TJ-G** | Test≡job + pruning-is-upstream sequencing round (two maintainer rulings 2026-07-29): **R1** the retention challenge must demand what a client demands — the SHARD, never a ~3 KB opening (the payload asymmetry is what manufactured A5's free-rider margin); **R2** the market's product is set-B scarcity, which exists only when pruned-daemon mode ships — today every daemon retains every leaf forever (segment-freeze fact §2.5, and that retention is *consensus-required by the current verify path*, which reads arbitrary local leaf scalars — the coupling runs both ways). Two-legged necessity: pruning without a real test pays proxies re-fetching from the foundation; a real test without pruning pays everyone for bytes they already hold. Sequencing resolution: challenge-VERIFY semantics are consensus (genesis-frozen; must verify responder-supplied leaf+path against `R_k`, which set A gives every node) — the pruning MODE is node-local and ships post-genesis without coordination (rule 75). Re-classifies W10/A5 as a pre-pruning artifact (machinery retained, re-runs at shard payload); dissolves reopen (d) as premature; pauses the PD probe | [`ARCHIVAL_TEST_EQUALS_JOB_SEQUENCING.md`](ARCHIVAL_TEST_EQUALS_JOB_SEQUENCING.md) | Registered 2026-07-29 at birth (rule 94 §1); prefix `TJ-` passes the uniqueness check (no family mints `TJ<letter/digit>` tokens) | Round 0 CLOSED; **Round 1 OPEN on the witness problem** (§8, 2026-07-29). Ledger's three legs now LOCATED, not pooled: leg 1 price-contingent (within-class per F-1 — decisive ~11× metered-retail, MARGINAL ~1.1× in the cheap-transit watch-cell the §5.3 reopen aims at, inverted flat-rate); leg 2 `T_risk` structural and **MEASURED** (`q_risk* ≈ 0.1011` +reward / 0.2792 bond-only, break-even fetch $0.0004/GB, PR #379); leg 3 **build cost SPLIT into two incompatible quantities** (§5.7, an equivocation cleared): **3a acquisition BYTES** — the free-rider pays perpetually what the honest archiver sank once (`honest = B + N·S` vs `free-rider = N·B`), a horizon correction that converts the cheap-transit coin-flip into a standing asymmetry, but priced in transferred bytes so it goes QUIET at flat-rate; **3b engineering/operational LABOR** — the honest path's marginal engineering is zero while the free-rider builds a subsystem that does not exist (retention suppression, challenge interception, live source map over a churning bonded set, deadline-bound fetch with failover), engineer-months against single-digit dollars per bond-year, denominated in labor so flat-rate does NOT zero it — but explicitly payoff-PROPORTIONAL, commoditizable by one published implementation, and no defense against a determined builder, so it raises the threshold at which tooling gets written and must NOT be leaned on. Flat-rate cell therefore rests on `T_risk` + TJ-C's deadline + a labor threshold nobody should lean on ⇒ §8.3's gate is LOAD-BEARING. Gates landed (PR #378): `tj_g_red_challenge_names_every_segment_leaf` red on challenge scope (rejects every `k` < 25,992), `tj_f_forged_material_does_not_verify` green on soundness, `work_sums_over_exactly_the_answered_pairs` welding the ledger's per-`(P,s,E)` granularity; TJ-F's liveness face carried as a **type-level obligation** on TJ-A's output (entry point takes response + `R_k`, no store handle), not a test. Round 1 inherits the branch asymmetry as **CLOSED** — sampling fails three independent ways (cost ledger §5.6 at ~1:260, 8C §4's Θ(m) zero-storage+reacquire text, and CI) — and puts its design questions at **CHALLENGER INTEGRITY**: **WITNESS POPULATION RULED 2026-07-29: MINERS, not the bonded set** — the bonded-set draw and its two rounds of mitigation (co-holder exclusion, denial-tolerant quorum, `1/r` denial-to-monopolize) are RETRACTED as design and RETAINED as diagnosis: they engineered around a threat the population choice manufactured (the `reground-the-whether` failure — two review rounds verified the choice's execution, never the choice). Miners carry both required properties ALREADY PROVEN: **indifference** by §12.11.1 Leg 1 (`miner_fee_income = total_fees − burned_amount` is invariant to the staker share across the whole `n` domain, pinned by `escalating_the_share_never_touches_miner_income`; serve credit only redistributes a budget it cannot resize) and **liveness** by PoW (selection IS proof of being online — the only population with the property; an offline witness's stall is indistinguishable from a denial, which was the prior scheme's failure mode). **TJ-A3 COLLAPSES**: verification is deterministic (recompute sub-root, compare `R_k`), honest witnesses cannot disagree and `P` cannot serve two valid responses, so disagreement is EVIDENCE (witness lied, or `P` served selectively — newly named and detectable), majority resolves, outlier is a signal. Remaining: TJ-A1 miner selection window (set now enumerable from block headers), TJ-A2 miner/bonder overlap (NOT excludable — G-1 forbids linking miner identity to persona; quorum now defends only the overlap FRACTION, a far weaker job), TJ-A2b witness obligation (paid = compatible; **block-validity = RULED OUT, it puts a 3.33 MB Tor transfer on the mining critical path — the invalid-blocks class**), TJ-A2c quorum size where the trade **INVERTS** (raising `n` DILUTES the deterrent: honest pays `n` egress, free-rider `n` egress + ONE ingress, so the differential is constant while shared cost grows — three-way tension, two of three push `n` small), TJ-A2d the escape to evaluate FIRST: `k`-of-`n` over SEQUENTIAL challenges (one witness each) preserves the ratio, spreads miner bandwidth, and **multiplies the deterrent** since `CHALLENGES_PER_EPOCH = 1` today (`constants.rs:11`) — a measurable claim re-runnable through `tj_shard_payload_report` by changing one operand, and the lever that could lift §5.7c's marginal cheap-transit cell. **MECHANISM RULED (§9, 2026-07-29): THE TEST IS A READ** — miners use the same mechanism any wallet uses, `P` cannot distinguish test from read, so passing requires *actually being able to serve* (a distinguishable challenge would let `P` fast-path tests while real requests rot — teaching-to-the-test, the anti-shim principle at the protocol layer). ARCHAEOLOGY: the ~3 KB opening was **forced, not chosen** — `P` self-attested in its own tx, and an artifact that fits in a tx can only prove CAPABILITY, never SERVICE; service needs a counterparty, so the rulings are FORCED IN SEQUENCE (test≡job ⇒ off-chain serve ⇒ third-party attester ⇒ indifferent+live = miners ⇒ indistinguishable request). L14's read-credit **SUBSUMED** (it bridged two events test≡job collapses into one; its 65% saving was on artificial tests and there are none left) ⇒ cadence **UNIFORM by construction**, which is correct not fallback (paid to be ABLE to serve; popularity-weighted testing would under-check exactly the unchecked holdings). Remaining = **TWO items**: (i) the client-facing read/serve protocol is TJ-B's real surface — **unblocks SP-T3** (its deferral was 'consumer-less infrastructure, no byte-defined payload'; this supplies payload=shard + consumer=drawn miner) and promotes response semantics to genesis-frozen; SP-T4b's absent producer changes hands from `P`-assembles-opening to witness-submits-receipt; (ii) **TJ-A5 DISSOLVED (2026-07-29)**, three findings: the evidence had NO CONSUMER (no tribunal; the m-of-n window over independent draws IS the adjudication — statistical over repetition, not forensic over one event; ephemeral-key candidate dropped, the read stays plain); the timely-commitment cost-binding is **RETRACTED** (the response is deterministic so ANY co-holder computes the commitment — ~100 bytes across the free-rider's link, and honest/free-riding commitments are byte-identical ⇒ zero adjudication information; the keyed HMAC variant fails on HORIZON — one key-share unlocks unlimited generation, §5.7a inverted — and its second-order effect is OUTSOURCING, since sharing `k_P` = handing over rewards/unbond = the helper IS `P`'s storage); what binds is **TOPOLOGY** — the witness reaches `P` only via `P`'s onion rendezvous, so the bytes traverse `P`'s link even as a dumb pipe (`n·I − S` enforced by path position, no artifact from `P`; the 'inseparable signature' question is answered no-and-unneeded — bytes are self-authenticating against `R_k`, relaying at full freight IS priced service). **WINDOW ASYMMETRY INVERTED (§9.6)**: denying honest `P` needs ~11/13 hostile (`f ≈ 0.85`), sustaining zero-service `P` needs ~3/13 friendly (`f ≈ 0.23`/shard) — outage-absorption ≡ attestation-forgiveness (`n − m + 1`), so the `(m,n)` re-pin gains objective 3: **ATTESTATION-RESISTANCE**, bribe priced against the zero-service reward take, portfolio (4,096 independent windows) swept via PD-D's predicate. SIZING (§9.5): honest egress CANCELS (validating deliverable 2's model), reward cancels and dwarfs costs 389–2598× so there is **no viability ceiling** (headroom to `n ≈ 747`), `n ≈ 10` buys a 10× deterrent margin, and miner bandwidth is an aggregate ÷ population (~9 KB/s per miner across 1,000) whose real constraint is miner-set concentration. **ONE OPEN GATE the round does not own:** is `q ≥ 0.10` forceable on a 3.33 MB Tor-borne fetch inside `CHALLENGE_RESOLUTION_BLOCKS` — PD-F-2's dispersion measurement; forceable ⇒ the fork has two live options, not ⇒ receipt is the only branch and Round 1 is a design of the attestation (§8.3) |
| **PD-A…PD-F** | Reopen-(d) feasibility probe design questions (honest-`q` family form, verdict exposure-end, `(m,n)` grid, parameterized window predicate, `φ`-endogeneity, plausible-`ρ` band) + Round-0 review findings PD-1…PD-3 — the pre-stressnet sim arm that breaks the joint round's near-circularity: A5's `q* ≈ 0.098–0.278` + the Round-1 false-slash 0.002 are known; only the honest-`q` coupling is missing, and it is **parameterized instead of awaited**. Output = a **conditional map** `ρ_max(m,n) = q_h_max/q*` ("feasible for honest-q below X"), never a collapsed verdict, so the Round-2 stressnet measurement is a lookup, not a re-run. Empty admissible region ⇒ **PoRep promotes BEFORE testnet build-out** (the fork is asymmetric: grace is a dial, PoRep is architecture). Pins NOTHING — grace/`m`/`n` stay provisional; the amended §12.6 handoff still precedes any pin | [`ARCHIVAL_REOPEN_D_FEASIBILITY_PROBE.md`](ARCHIVAL_REOPEN_D_FEASIBILITY_PROBE.md) | Registered 2026-07-28 at birth (rule 94 §1); prefix `PD-` passes the uniqueness check (no family mints `PD<letter/digit>` tokens; distinct from `P2B-`, `DQ-`, `DS-`) | Round 0 OPEN, source-reviewed same day (2026-07-28) — a-priori commitments stated in-doc (false-slash bound inherited from Round-1, headline verdict at the conservative 0.278 end, `n ≤ 25` structural ceiling); review findings folded: PD-1 (i.i.d. layer is the zero-correlation FLOOR — a lower bound on false-slash, correlation is what makes an `m`-near-`n` slash reachable), PD-2 (three-valued verdict: marginal = non-empty region below the plausible-`ρ` band, carries infeasible's action pending stressnet; band itself an a-priori input, PD-F), PD-3 RULED (parameterize production `failure_window_slashable` via `FailureWindowParams` + delegate — parity structural across the grid, `EscalationParams` precedent; twin+single-point-parity demoted to named fallback). Dispositions PD-A…PD-F awaiting ratification. **⏸ PAUSED 2026-07-29** — premise superseded by the TJ sequencing round (row above): the `q*` band was a pre-pruning artifact at the mis-specified payload. PD-D's parameterized predicate, the conditional-map form, and PD-F-2's dispersion framing survive for reuse; resumption = TJ spec ratified + pruned-daemon mode designed + A5 re-measured |
| **DS-1…DS-7** | F-D2 drain-send subsystem design questions (assembly locus, GUI data source, destination pin, fee/change mechanics, UI default, F-D4 §16.4 funding-default rider, shape-era doc sweep) — Gate-6 R4's last open item (former `K_COVER` seal seat removed 2026-07-19, M1 §4/F-W10) | [`ARCHIVAL_DRAIN_SEND_FD2.md`](ARCHIVAL_DRAIN_SEND_FD2.md) | Registered 2026-07-19 at birth (rule 94 §1); prefix `DS-` passes the uniqueness check against the registered families (no family's tokens parse as `DS<digits>`; distinct from `DQ-`) | Scoping (opened 2026-07-19) — review rounds 1–4 applied (rounds 1–2 review + Round 3 threat-model addenda, rule 26 A3: T-DS-1…T-DS-5 graded + Round 4 structural-distinguishability pass, **closed maintainer-accepted**: T-DS-6 absorbed via the consensus 2-out floor, T-DS-3 change-output arm forward-actioned to DS-PR-1, and T-DS-6 ∧ T-DS-7 folded into a single composite wire-shape arm (drain serialization byte-identical to a modal transfer via the shared `sign_bridge` path — `0x07` leaf-hash confirmed unconditional at `sign_bridge.rs:283`), GF-7 disposition + local-adversary scope boundary added — all settled at source), under review. Impl-time pre-flight (rule 26 Part B) **core-side run 2026-07-19** (`ARCHIVAL_DRAIN_SEND_FD2_AUDIT.md` Round 0 — DS-PR-1/DS-PR-2 substrate re-verified at pin, 1 B6 errata, 24 artifacts green); GUI-side (DS-PR-3/4/5) carried to each GUI sub-PR's open. Composition-discipline pass 2026-07-19 (`ENGINE_COMPOSITION_DECOMPOSITION.md` alignment, doc §4): drain workflow pinned to a free-function orchestrator over a `DrainCtx` (no new `Engine` generic/inherent method), own `drain_*` module set (not the flagged god-files), DS-PR-4 entry = thin façade delegate — armed as an F-D1-sibling self-grep. **UPDATE 2026-07-20: DS-PR-1 (drain assembly, `shekyl-core`) LANDED** — `AssembleDrain` actor handler + free-function `assemble_drain_tx` (`engine/drain_assembly.rs`) + `PendingDrain` record (schema v4→v5, snapshot updated); the T-DS-6 ∧ T-DS-7 composite wire-shape arm, T-DS-3 change→`P`, T-DS-4 crossing-class mapping, and the DS-4 exit-reserve disposition all discharged (doc §5 post-closure pin + round-log 2026-07-20). Substrate finding: the ratified zero-value-change mechanism is unrealizable (shared prover rejects `ZeroOutputAmount`), so **drain-all splits the payment into two nonzero principal outputs** — same two-nonzero-output wire shape, T-DS-6 strengthened. Transfer-parity is now **compile-time-enforced**: the whole `WireEncodeInput` is built by a single shared constructor (`sign_bridge::assemble_transfer_wire`) that `sign_tx` also calls; a whole-tx **normalized byte-diff** (`drain_partial_and_drain_all_are_wire_identical`) lands in-slice. Gates green (4 drain-shape tests + `sign_bridge`; schema snapshot; fmt/clippy `-D warnings`). Remaining DS-PR-2…6 open (drain orchestrator/dispatch, persona-transport self-grep, the **full transfer-vs-drain** byte-diff e2e, GUI surfaces). **UPDATE 2026-07-21: DS-PR-2 (drain orchestration + dispatch, `shekyl-core`) LANDED** — free-function `orchestrate_drain` over a `DrainCtx` (never `&Engine`; `engine/drain_orchestrator.rs`); `EXIT_FEE_RESERVE_ATOMIC` real constant (`shekyl-standoff::reserve`, `50_000_000` atomic, compile-time bounded) enforced in the orchestrator (T-DS-4/DS-4: partial live-persona drain refused below reserve via `DrainOrchestrationError::ReserveBreached`, retired sweep reserve-moot); `Engine::submit_drain` dispatch seam (`engine/drain_dispatch.rs`, `claim_dispatch` sibling — engine-resolved principal destination T-DS-3, persist-`PendingDrain`-before-dispatch, one live drain per persona, persona-transport choke point `BroadcastSubmitter::local`→`submit_bound` T-DS-2, comment-stripped self-grep tripwire); and the composite arm's **final leg** — the real transfer-vs-drain e2e byte-diff (`regtest_e2e::e2e_drain_wire_shape_matches_a_real_transfer`, both daemon-accepted, normalized skeletons byte-identical / raw bytes differ). No new schema (dev's v6 already carries `PendingDrain`). Deferred (named, WI-3 sibling): drain retirement wiring. Gates: fmt/clippy `-D warnings` clean. DS-PR-3…6 (GUI) remain open |
| **WI-RPC-1…WI-RPC-N** | Wallet-RPC parity work items: 1 receiving + staking-read + fee surface projection (first tranche; enabler for GUI work + the CLI migration off wallet2); 2 the CLI migration itself under ratified Shape B (CLI always an RPC client; 2a transport swap + core commands, 2b new surface + wallet2-era deletions); 3 Phase 2c proofs tranche — tx proofs + reserve proofs projected from `rust/shekyl-proofs/` primitives through the Engine, contract, RPC server, and the CLI RESERVED stubs (closes the plan's "Engine::tx_proof / Engine::reserve_proof not wired" fossil-sweep gap; **in-scope prerequisite:** the send-path retention writer for `TxMetaBlock.tx_keys` — the schema predated the tranche with no production writer, so OUTBOUND tx proofs had no secret to consume; the writer landed first, ordered before the proof wrapper (mirrors the contract's `GetTxProofParams` OUTBOUND-PREREQUISITE note, which also pins the retention lifecycle incl. the spend-quadruple — `TransferDetails.spending_tx_hash` + `KeyImageObserved` growing the containing-tx hash — that closes the exact-spend I-2 corner); un-stubs the proof commands per the FOLLOWUPS "WI-RPC-2b deferrals" reopen shape) | Contract: [`../api/wallet_rpc.yaml`](../api/wallet_rpc.yaml); per-item plan docs are the work-item briefs | Registered 2026-07-19 at birth (rule 94 §1); prefix `WI-RPC-` passes the uniqueness check — the existing `WI-N` family mints only bare-digit tokens (`WI-1`…`WI-4`), so token sets cannot collide (the `SP-N`/`SP-TN` coexistence shape). Ephemeral like `WI-N` (rule 21 retire criterion): the family retires when wallet-RPC parity closes and the CLI is off wallet2 | WI-RPC-3 implemented (2026-07-24, `feat/wallet-rpc-proofs` — spec round ratified 2026-07-23 then the full tranche landed: retention writer for `TxMetaBlock.tx_keys` at dispatch (review-hardened 2026-07-24 from submit-ACCEPTED: crash-window closure + reconciliation-only death, see the contract's OUTBOUND-PREREQUISITE pins) with the spend-quadruple (`TransferDetails.spending_tx_hash`, `KeyImageObserved.containing_tx_hash`) + I-2 extension + `reconcile_tx_key_retention`; `TxSecretKeys.additional` deleted (rule 15, `TX_META_BLOCK_VERSION` 2); tx-proof wire format now carries per-entry vout indices (non-prefix subsets verify; FFI + wallet2 caller rewired); Engine workflow `engine/proofs.rs` per `ENGINE_COMPOSITION_DECOMPOSITION.md` (explicit `ProofsCtx`, thin Engine delegators, KeyActor `GenerateInboundProof`/`GenerateReserveProof` messages); `shekyl-wallet-rpc/src/proofs.rs` serving `get_tx_proof`/`check_tx_proof`/`get_reserve_proof`/`check_reserve_proof` with wallet-less `check_*`; CLI un-stubbed with disclosure warnings, `get/check_tx_key` deleted with parse-time guidance per the ratified rule-21 rejection; end-to-end generate→check tests over a mock daemon incl. the negative surface. Verified: `cargo test -p shekyl-proofs -p shekyl-engine-core -p shekyl-wallet-rpc -p shekyl-cli`). WI-RPC-3 opened (2026-07-23, `feat/wallet-rpc-proofs` — spec round: contract additions + rule-21 dispositions reviewed before implementation). WI-RPC-2b implemented (2026-07-22, `feat/cli-rpc-client-surface` — CLI receiving/staking/fee commands over the WI-RPC-1 methods; wallet2-era account/subaddress/key-image/secret-display/sweep commands deleted with parse-time guidance; `HELP_TEXT` rewritten; `CLI_PARITY_MATRIX.md` reconciled; RESERVED-stub carriers in `FOLLOWUPS.md` §"WI-RPC-2b deferrals"; verified: `cargo test -p shekyl-cli`). WI-RPC-2a implemented (2026-07-22, `feat/cli-rpc-client-core` — `shekyl-cli` is off wallet2: native RPC client over private-UDS in-process spawn or `--rpc-url`; `restore_wallet` added; `shekyl-engine-rpc` dep deleted with a grep-gate test; verified: `cargo test -p shekyl-cli` → `tests/rpc_session_e2e.rs`). WI-RPC-1 implemented (2026-07-19, `feat/wallet-rpc-receive-staking-fee`) |
| **SJ-DQ-1…SJ-DQ-8** | Send-journal design round: the post-dispatch send-record lifecycle — W-D outgoing history (record shape/disclosure ledger, write point, state machine, prune, schema, projections) + the `abandon_tx` presumed-dead arm (safe entry, RPC contract) | [`WALLET_SEND_RECORD.md`](WALLET_SEND_RECORD.md) | Registered 2026-08-05 at birth (rule 94 §1); prefix `SJ-` is unminted — no collision with `SP-`/`SA-`/`WI-` token sets | R1 pass 1 FOLDED (2026-08-05): C1 (premise corrected — the address book is an existing user-curated counterparty store), C2 (journal owns dispatch facts; F14 derived, never stored twice), C3, C5, C7 (NEW — `LedgerBlock` scan-derived-only, marker-trait-enforced), §5, and the PR decomposition RATIFIED; §1 replaced by the source-verified per-field recoverability table (replay recovers txid/height/fee/total/change via the spend-quadruple + cleartext wire fee — only counterparty identity and the split are journal-unique); SJ-DQ-4 REFRAMED (journal carries the dispatched input set across the rescan wipe, markings re-applied post-rescan; `PresumedDead` becomes a display state, evidence bar collapses to show-the-user — abandon arm 2 self-contained); SJ-DQ-1/5/6 re-posed; force path pulled from SJ-DQ-8 into R1. R1 pass 2 FOLDED (2026-08-05): **C1 REPLACED** — the envelope is the boundary (`WALLET_FILE_FORMAT_V1.md`: the journal lands AEAD-sealed beside the transfers cache, the keys file already holds the seed, and `tx_meta.tx_notes` is an existing contract-promised counterparty-bearing surface — privacy-by-omission displaces the record to unprotectable storage; discipline moves to the boundary crossings: RPC wire incl. the TCP listener, rules-35/36 memory hygiene, logs/dumps/backups); **SJ-DQ-1 DECIDED** — full row, addresses stored, no knob (the reduced-row GUI mock + its ratification gate WITHDRAWN, findings M-1..M-4 die with it; address-book-reference demoted to normalization); SJ-DQ-5 re-scoped to user control (envelope does seizure defense); scope ruling per rule 19: the dormant `tx_notes` annotation surface (persisted+preserved+promised, zero consumers — WI-RPC-3-F-1 dormancy class) folds into SJ-DQ-7 and ships with PR-SJ-2; the wallet2-lineage `attributes` bag gets its own deletion-leaning disposition (rules 60/81) ratifying at pass 3. Pass-2 AMENDMENT (2026-08-05, FA-10): the recoverability table gains the label row — `LABEL_KIND_REQUEST` rid linkage is CONDITIONALLY replay-recoverable (retained tx_key ∧ candidate recipient address, via the OUTBOUND `rederive_combined_ss` → `derive_output_secrets().k_label` chain); SJ-DQ-1 unaffected (the decided full row is what satisfies the condition); PR-SJ-1 stores the echoed rid directly at dispatch. ROADMAP FOLD (2026-08-06): SJ-DQ-1 gains the R-4 fee-provenance clause (realized fee of the built tx, never an estimator value — correct through the stub-estimator era); SJ-DQ-3 ratification GATED on the C++-retirement roadmap's A4 cold-bundle decision (R-2: in-scope bundles add exported-unsigned/signed-elsewhere states and an export path on `build_pending_tx`, the function PR-SJ-1 modifies; A4+B2 resolve before pass 3 freezes the machine). B2 DECIDED (2026-08-06): hardware-device C++ deletes with Phase 5, not re-homed — `src/device_trezor/` 6,348 lines + `tests/trezor/` harness, wallet2-only consumers verified, currently-impossible hardware (no vendor hybrid-PQ firmware); rule-21 reopen = vendor ships hybrid PQ signing, landed as a Rust `Signer` impl (FOLLOWUPS entry supersedes the Trezor-harness V3.1 migration item; base `src/device/` explicitly NOT in scope — cryptonote_basic-threaded, separate daemon-side retirement). Pass-3 gate is now A4 ALONE. A4 DECIDED (2026-08-06, descope — clause in FOLLOWUPS): GATE CLEARED, pass 3 UNBLOCKED; V3.0 machine ships without `ExportedUnsigned`; PR-SJ-1 gains the clause-(2) obligation — pin additive-variant admissibility of the persisted encoding with a test. PASS 3 RATIFIED (2026-08-06, P3-1..P3-5): rescan re-application idempotent, `-29202` splits (unconfirmed half retires, reservations half stays); P3-1a SPLIT PRE-COMMITTED (PR-SJ-1 = reclassification + both-agree equivalence invariant, C7 exception provably vacuous; PR-SJ-1b = the 91-ref field retirement, own PR, before PR-SJ-2); two-tier deletion (tx_keys independent; tier-2 full-scrub warned); dedicated `send_journal` block v1 + append-only state enum; NO force path (reopen: a blocked flow discard cannot clear) — abandon reframed to the I-2 reference migration; `attributes` deletion ratified. Round 0 opened 2026-08-05 (questions posed with C1–C6; PR-SJ-1 journal, PR-SJ-2 projections closes the W-D FOLLOWUP, PR-SJ-3 abandon closes the rescan-escape FOLLOWUP) UPDATE 2026-08-07: PR-SJ-2 landed on `feat/wallet-rpc-outgoing-sj2` — OUTGOING projection closes W-D FOLLOWUP (verified: `cargo test -p shekyl-wallet-rpc`). |
| **MSW-1…MSW-8** (+ **MSW-G=5**; product **Option E′**) | V3.0 wire: MSW-6 scheme_id; MSW-G=5; MSW-1…5; **MSW-8** delete vestigial address `hybrid_sign_pubkeys`. MSW-7 retracted. Product **E′** (§0.5). | [`V3_1_MULTISIG_RUST_ENGINE.md`](V3_1_MULTISIG_RUST_ENGINE.md) §0.3–§0.5 | **MSW-1/2/3/4/5/8 code LANDED 2026-07-16** (branch `feat/msw-wire-max5`, atop MSW-6): `MAX=5` across crypto-pq/wire/address/C++, F-1 fossil `2 + N·LEN` deleted at all four sites, crypto-pq↔address caps pinned at compile time, cross-language KAT `fcmp.cpp::msw1_pqc_constants_match_rust` green (F-1 catcher); MSW-3 was cleared by MSW-6. Per-item closeouts in the wire-work table. Product **E′** = Track B (pending). | MSW-1/2/3/4/5/6/8 landed; E′ (Track B) pending |
| **DRS-*** | Daemon chain-store: **§0.1 Tier A/B success** (mission-ordered); **P0a–P0d** multi-PR; **BENCH** (resource/privacy/IBD/pop); **C** surfaces S-TXN…S-PRUNE (97 methods); **D10 reconstructible mandatory** for redb genesis; D2-reopen = **first-class Tier-A LMDB genesis**. | [`DAEMON_REDB_STORE.md`](DAEMON_REDB_STORE.md) | Registered 2026-07-27; gap-close 2026-07-27. Supersedes V4 heed destination | Engine swap not started; **P0/BENCH/C may open** |
| **RP-1…RP-N** (relay-privacy) | Daemon Dandelion++ relay-timing work items. **RP-1** = the `shekyl-relay-privacy` crate (this row's birth registration, rule 94): the measurement instrument *and* the corrected primitives — exact embargo derivation, memoryless draws, stem-map port, goodness-of-fit conformance. Standalone, wired to nothing. **RP-2** = FFI + `connection_map` cut; **RP-3** = epoch/fluff scheduler cut (`levin_notify.cpp` → transport shim); **RP-4** = `tx_pool.cpp` embargo. RP-2…RP-4 gate on the design doc converging. | [`DAEMON_RELAY_PRIVACY.md`](DAEMON_RELAY_PRIVACY.md) (Round 3 dispositioned; D-1…D-7 as amended, §10 = RD-1…RD-4 dispositions, §7 = Round-3 agenda) | **Port arc COMPLETE; parameter/selection rounds OPEN (as of 2026-08-06)** — RP-1…RP-4 all merged (#370 / #372 / #374 / #377 / #380): Rust owns every D++ timing decision behind `shekyl_relay_zone_*` / `shekyl_dandelionpp_*`; embargo 190 s, wallet propagation timeout 874 s, configuration B deleted. Open: Q-10 (`g_max` — p2p round, gates ρ/selection/reshape), Q-11 Unit 2 (covert cadence), Q-12 (forward delay, the last inherited timing draw at `tx_pool.cpp:312`), the §64 anonymity-zone-stem round, and **landing the §80-decided `hop` parameter set in code** (decided in prose+tests, not yet shipped — see UPDATE 2026-08-06). History and UPDATE chain follow. Landed 2026-07-23 (measurement + design in one PR). Four defects, all asserted in code: **F-1** the 39 s embargo does not follow from its own stated derivation (paper formula over `k=5, ep=0.10, hop=175ms` gives 16.61 s; 39 s reproduces only under `log10` for `ln`); **F-2** the embargo draws a Poisson while its derivation assumes an *exponential* survival function — measured, the backstop **never fires** (full-travel 1.0000 at both 39 s and a corrected 17 s, against a stem completing in ~700 ms); **F-3** the closed form substitutes `E[K]` into an expression in `K(K-1)` and stem length is geometric (`E[K(K-1)] = 2·E[K](E[K]-1)`), so it under-provisions at every `q` — exact solve gives **144 s** (RD-1 return-term + RD-4 stem-support corrections; cross-checked at 147 s); the pre-RD-1 31 s was a loose lower bound; **F-4** the **fluff delay** carries the same distribution defect on the timer every node applies to every tx (Bitcoin Core's cited `PoissonNextSend` is an exponential inter-arrival, not `std::poisson_distribution`) — measured ~1.96× more invertible, and 93% invertible for a tx arriving late in a flush window; **F-5** the same defect makes the fluff flood ~7× slower (13.75 s vs 2.25 s p90 first passage) — the flood-return term that lifts the RD-1 embargo to 112 s, before RD-4's stem-support fix takes it to the adopted **144 s** (F-3). F-2/F-4 are the high-severity pair; correcting F-1 alone changes nothing measurable. Relay timing is node-local policy, **not consensus**: no fork, not genesis-blocking, but being fixed pre-ship because it is wrong. | RP-1 landed (crate + design Round 2: RD-4 stem-support fix → 144 s, black-hole + first-spy instruments, §11 source-anchoring, §6 adversary-position + quantified clearnet↔Tor delta [supernode π₀ 0.45→0.000] + measured clearnet passive channel [mean-dependent, 0.011@144s, zero on Tor]); Round-3: BOTH levers live (ε clearnet-passive, Q-8a black-hole); §12 wargame adopts re-stem (cap=STEMS−1=1 derived), rejects two-class; §13 resolves Q-9 in form (δ=precision increment, RE-RUN reproduces the table); §14 REFRAME — fluff-on-expiry (embargo→fluff, cryptonote_core:1087) spends privacy for recovery-latency (backwards from priority order) → adopt reshape UNCONDITIONALLY, bound δ only on W3 residual; recovery cost measured (p99 954s = 0.37% of 3-day mempool lifetime = cost not break); ≤0.3% worst-case exposure reachable via reshape [2.24%→0.026% at 1 retry, no embargo change] not embargo-lengthening [needs 1050s = liveness break]; §15 block-time seam (embargo held block-time-unaware, reconciled at the integration layer); §§12.6–12.11 selection mechanism — W3 residual `W3(g)`, Q-10 `g_max` reframed as select-toward-demonstrated-work, epsilon-greedy anti-ossification, all measured in-crate; §6.9/§6.10 passive-observer scope boundary + economic deterrent. **Design round CLOSED; crate decomposed pre-RP-2 (measurement surface split into focused modules).** RP-2 split into **RP-2a** (faithful `connection_map` port to Rust — logic to `stem_map.rs`, `dandelionpp.{h,cpp}` become opaque-handle FFI glue with removal target RP-3, `levin_notify`/gtests unchanged; §16 design round) + **RP-2b** (W3c churn-stable alternate + Q-10 selection — own round, own repeated-churn sibling test + `g`-bound, the three §12.11 obligations); the two are separable because the gtests pin outcome-invariants not the fresh-draw mechanism. §12.11 re-census DONE: C++ RP-2/3/4 surface unmoved (staking landed in Rust); port anchors re-verified at source. RP-2a **MERGED** (#370, merge `d4bb9b117`; archived `archive/rp2a-connection-map-rust-2026-07-27`): connection_map logic ported to Rust `StemMap` via `shekyl-ffi`; C++ is now an opaque-handle forwarding wrapper (removal target RP-3). Verified — the 6 `dandelionpp_map` gtests pass **unchanged** against the Rust map (migration oracle), all 33 `levin_notify` gtests pass (end-to-end), + the 3 §16.1 seam micro-tests + the Rust gtest translations; the two silent-corruption contracts are closed *by construction* (copy-assign `= delete`d, snapshot emits every slot nil-inclusive). **RP-4 in progress** on `feat/rp4-embargo-rust` (§17 design round): the embargo cut — F-1/F-2/F-3 → Rust-owned draw (`EmbargoTimer`, one export, no handle), 39 s constant + Poisson draw deleted, derivation guarded at three sites per the `40667e701` STEMS=2 precedent; the two named obligations (§10.7 loop handling, §15.4 `MIN_RELAY_TIME` reconciliation) **discharged by verification at source** — stem-on-stem already fluffs, and the relay loop gates stem/forward on the embargo deadline disjointly from `get_relay_delay`. Scope excludes the §14 reshape (RP-2b-entangled). **RP-4 MERGED** (#372, merge `41ee2c41f`; archived `archive/rp4-embargo-rust-2026-07-27`): embargo value+distribution to Rust (`EmbargoTimer`, 144 s memoryless, one export, no handle); the 39 s constant and the Poisson draw are gone from the tree (only guard comments remain); the wallet's propagation timeout now derives from a stated false-fail rate (1-in-100 → 664 s) instead of 3/2× the mean; deadline rounds away from `now` so the FFI's `div_ceil` is not undone one layer down; embargo/`MIN_RELAY_TIME` disjointness armed by test and negative-control verified. Six review rounds. **RP-3 in progress** on `feat/rp3-levin-scheduler-rust` (§18 design round): the scheduler cut — the loop and the zone state move to Rust, closing **F-4/F-5** (the fluff-delay distribution defect and the ~7× slower flood). Re-census correction: the 33 `levin_notify` gtests **do** survive the cut — they force each step via the `run_epoch`/`run_stems`/`run_fluff` hooks (36 calls, zero timer manipulation), so the oracle is timing-independent by construction. Proposed 3a (Dandelion++ path, findings close here) / 3b (noise channels). **RP-3a built** (branch green: 33/33 `levin_notify`, full unit suite 1011 passed / 0 failed): `levin_notify.cpp` is a transport shim over `shekyl-relay::{Zone, Driver}` — `contexts`/`map`/`connection_count`/`fluffing` are Rust-owned behind one handle, two asio timers collapse to one armed from `next_wake()` (Rust owns the deadline value, asio owns the sleep — no reactor added, so the crate's reason-2 seal holds and the earlier seal-break was **withdrawn** as mispriced: F-4/F-5 close without it). Two regressions the port introduced and the round caught: the i2p/tor **outbound-only fluff** rule was dropped (caught by the 8 `private_*` gtests — an inbound peer on a hidden service is a stranger who dialled us), and the **epoch rollover merged instead of rebuilding** the stem map, freezing the stem graph and every source's pinning for the life of the process (*not* caught — all 33 gtests passed both before and after the fix, because they assert pinning holds *within* an epoch, which a frozen map satisfies more strongly). **RP-2a seam retired here** on the option §16.1 acceptance item 2 reserved: `src/net/dandelionpp.{h,cpp}`, `dandelionpp_ffi.h`, the `shekyl_dandelionpp_map_*` exports and the 6 `dandelionpp_map` gtests are deleted (five Rust twins verified present in `stem_map.rs`; census by grep). Of the three §16.1 contracts, two are moot and **index-order-with-nils survived** as the `SlotsCb` contract — its successor witness was found sealed-but-empty and was negative-controlled before the deletion landed. **RP-3a MERGED** (#374, merge `0a27d519e`; archived `archive/rp3-levin-scheduler-rust-2026-07-28`). **RP-2b MERGED** (#377, merge `ab8a0987e`; archived `archive/rp2b-churn-stable-alternate-2026-07-28`): each source's stem candidates freeze at first pin, closing the W3c mid-epoch re-roll; the Q-10 ρ obligation shrinks to a single draw **per epoch** (still blocked on the `g_max` grounding). **RP-3b CLOSED 2026-07-29** (§20, branch `feat/rp3b-covert-channels-rust`; **MERGED #380 2026-07-30**): the covert-channel port — per-channel noise timers cut N→**zero** (deadlines Rust-owned, folded into `next_wake()`, zone strand sole FFI caller); the slot-push seam **inverted then deleted** (`SlotsCb`/`update_channel`/`Effect::StemSlots` gone; the binding travels with each `CovertSend`, a slot's loss crosses as `CovertUnbind` derived per due tick — commands return nothing, all effects ride `poll`/force hooks); the RP-3a seal test retired into three named, negative-controlled successors; four invariants witnessed with run-and-observed-to-fail controls (CV-1 repoint-discards-remainder as a gtest sequenced *after* the second discard site died, CV-2 binding identity + hole, CV-3 deadline stability, CV-4 payload-blindness structural — no discriminant in the callback signature); the noise gtests rewritten under the `gtest_dropped_connection` standard after the inherited expectations were shown to assert hook-synchronized emission (the negation of constant-rate cover); `zone::noise` split into `covert_payload` + Rust-owned `covert_enabled` (one birth site); `run_stems` renamed `run_next_wake`. Round outcome in §20.10 records the cost honestly: four unanticipated findings (CV-4 at grounding, the oracle-drive coupling, the synchronization fiction, the `CovertUnbind` dormant-queue leak at the deletion) and **four of ten acceptance criteria wrong as written** (items 1/4/5/8), every one caught at execution rather than review. Remaining in the arc: **Q-11 IN FLIGHT** (Unit 0 landed 2026-07-31, §22 — feasible-region census: `FORWARD_DELAY_*` decoupled from `NOISE_*`, dead epoch mirrors deleted, `NOISE_BYTES`/`NOISE_CHANNELS` admitted to the ledger, the `MAX_FRAGMENTS` capacity constraint restated family-survivingly, the cadence oracle re-aimed at production with controls run-and-failed; Unit 1 = adversary-and-capability next), **Q-12 registered and live** (§22.2 — forward-delay mean underived + Poisson family at `tx_pool.cpp:311`; fired by Unit 0's own decoupling per its reopen criterion (a)), Q-10 selection (blocked on `g_max`). The `force_fluff` pass was discharged clean in #382 (`fluff_via_scheduled_drive`). **Q-11 census corrections (#384): F-7 CLOSED at 190 s** — `fluff_return_ms` had been measured under `EveryPeer` (undirected) while anonymity-zone fluff is outbound-only; re-measured under `OutboundOnly` at degree 12 → **F′ = 3250 ms**, embargo re-derived **144 → 190 s**, wallet timeout **664 → 874 s** (every 144 s / 664 s / 2250 ms figure earlier in this row is the pre-F-7 value, correct under the old input). **F-8b**: outbound-degree floor 12 (`shekyl_relay_zone_min_provisioned_out_peers`, Rust-owned) enforced at the `--tx-proxy` parser, at `set_max_out_peers` (covers `--out-peers` init), and clamped on the runtime `out_peers` path. **Configuration B deleted**: the covert/noise channel was an origin oracle (precision ~1 toward covert-slot peers); `proxy::noise` removed, `disable_noise` a warn-and-ignore no-op, cover-traffic absence warned at startup; the wire-observer defense is Q-11 Unit 2 (§§25, 29-31, 41). UPDATE 2026-08-04: **R-1 mixed eligibility MERGED (#389)** — `shekyl_relay_zone_divert_relayed_tx`, one 2 %/hop eligibility roll at clearnet entry (§59; dual-stack `select_anonymity` fixes rode along); the anonymity-coherence arm stays dormant while hidden-service zones force fluff. **StemWatch + `/get_stem_tallies` live** (§§38, 47–48, 55) — per-successor stem outcomes recorded via `shekyl_relay_zone_record_stem`/`record_arrival`, snapshot through `net_node.inl:2200`; records only, thresholds deliberately open (§36.1/§37.3), so Q-10's selection round has its input signal already flowing. **§63 discovery (same round): the anonymity zone has never run a stem** — the D++ dispatch is clearnet-gated (`levin_notify.cpp:1222`); i2p/tor diffuse to their outbound set with the wire marked `dandelionpp_fluff` while the txpool is told `stem`, so the embargo arms with no stem existing. **F-12 RETRACTED** on its own instrument (sign backwards — the shipped 190 s over-provisions the anonymity path by ≥ 75 % even at 10× clearnet latency); "should the anonymity zone stem?" scoped as its own round (§64). UPDATE 2026-08-06: **hop verification surface + decisions MERGED (#408 surface, #409 decisions; §§72–87)** — `hop` defined forward-to-forward (§71); the 48-cell `f(n_in ∈ 1..=8, depth_tier ∈ 1..=6)` surface enumerated on both arms; the Pi/x86 ratio measured flat (5.36–5.75, pinned as the re-measurement invariant, §80.6); the §84 pre-registration resolved to the flat branch (§85). Decisions: **minimum supported spec = Raspberry Pi 4** (promoted to rule `76-device-provisioning-floor.mdc`), **option D adopted** — per-shape `f` lookup table, decoupling `MAX_INPUTS` from the embargo (§80.3), **refuse-don't-clamp** below-spec guard (§83), **`Provenance` field + scaled-row tripwire** after §80.3's own correction (§87.2). **Code-anchored gap (verified 2026-08-06, `git grep time_between_hop_ms` → `params.rs:354`): the adopted `hop` parameter set is NOT shipped** — `175` sits only in `DandelionParams::inherited()` (whose doc says "to be measured against, not to be shipped") and no adopted counterpart exists; the §80 decisions live in `spec_decision_matrix.rs` and prose. Landing the adopted set is the next RP cut. UPDATE 2026-08-06: row refreshed after a same-day roadmap audit reproduced its staleness downstream — "RP-3b PR pending" and the pre-#408 "bench not yet run" state were read as current and produced a wrong Phase-1 work item ("run the bench" instead of "ship the adopted params"); recorded per rule 94 because this is the blocked-when-actually-unblocked failure mode the row format exists to prevent, demonstrated on the audit that proposed fixing it. |
| **LV-1…LV-N** (levin) | Levin p2p Rust-migration work items. **LV-1** = the `shekyl-levin` framing crate (this row's birth registration, rule 94): 33-byte bucket header, notification/request/response builders, noise + noise-shaped fragmentation, zstd `COMPRESSED` flag path (feature-gated, `HAVE_ZSTD` parity), incremental `BucketReader` mirroring `handle_recv`. Byte-identity KATs mirror the `tests/unit_tests/levin.cpp` gtests assertion for assertion, and `ci/levin-constant-parity` fails the build if any hand-copied wire constant stops matching its C++ definition (`HEADER_SIZE` anchored by a `static_assert(sizeof(bucket_head2) == 33)` in `levin_base.h`). Divergences from the oracle are enumerated **once**, in the crate docs (`rust/shekyl-levin/src/lib.rs`) — this row deliberately does not restate them. Standalone, wired to nothing — the C++ path (`contrib/epee`, `levin_notify.cpp`, `src/p2p/`) stays live. **LV-2** = portable_storage payload codec + p2p/cryptonote command schemas (build-or-vendor decision, cf. `DAEMON_RELAY_PRIVACY.md` §8 on Cuprate). **LV-3** = connection-path cutover (the `levin_notify` / `net_node` seam; the two-dependency inventory below is the cost basis). LV-2/LV-3 gate on the p2p migration track being scheduled — see the FOLLOWUPS "Levin p2p migration" entry | [`../LEVIN_PROTOCOL.md`](../LEVIN_PROTOCOL.md) (wire spec); `rust/shekyl-levin/src/lib.rs` crate docs (authoritative divergence census); `rust/shekyl-levin/README.md` (scope) | Registered 2026-08-05 at birth (rule 94 §1); prefix `LV-` passes the uniqueness check against the families above | LV-1 landed (as of 2026-08-05; code-anchored: `rust/shekyl-levin/`, KATs `tests/oracle_kats.rs`). LV-2/LV-3 not started, deferral recorded in FOLLOWUPS |

---

## 3. Phase map (engine layer, `WALLET_REWRITE_PLAN.md`)

| Phase | Scope | Status (per plan doc + code, 2026-07-05) |
| --- | --- | --- |
| 0 | Close out previous plan; align baseline | Closed |
| 1 | Wallet domain model (`shekyl-engine-core::Engine`) | Closed 2026-06-10 (`docs/completed/PHASE_1_ORCHESTRATOR_STATUS.md`) |
| 2a | Send path | Done for phase scope (2026-07-01) — curve-tree client wired into production sends; residue in `PHASE_2A_SEND_PATH.md` §10 |
| 2b | Stake lifecycle | Design rounds 0–4 closed / largely closed; implementation = Stage 3, **partially landed** (see §5) |
| 2c | Addresses / proofs | **Partial — receiving + proofs landed; message signing remains (as of 2026-07-24)** — receiving/payment-requests landed with FA-8 → WI-RPC-1; `Engine::get_tx_proof`/`get_reserve_proof`/`check_tx_proof`/`check_reserve_proof` landed with WI-RPC-3 (`engine/proofs.rs`, closing the plan's "not wired" fossil-sweep gap). Remaining 2c residue: `sign_message`/`verify_message` (own hybrid-key design decision; FOLLOWUPS §"WI-RPC-2b deferrals" carrier). UPDATE 2026-07-24: WI-RPC-3 proofs tranche landed on `feat/wallet-rpc-proofs` (see §2 WI-RPC row) |
| 2d | Air-gapped flow (`UnsignedTxBundle`/`SignedTxBundle`) | **A4 DECIDED 2026-08-06: DESCOPED from V3.0** (capability kept, tractability confirmed — SA/L separable by construction, membership re-anchors hot, PQC view-tier re-signable; verified display + envelope-sealed bundle are the unstarted halves; rule-21 clause in FOLLOWUPS "A4 DECIDED", two items: SA/L-split form load-bearing / `ExportedUnsigned` additive, verified at PR-SJ-1; posture: V3.0 ships cold STORAGE (seed custody complete), not cold SIGNING — the earlier joint-with-B2 framing is withdrawn, the decisions are independent). Target V3.1+. Per plan doc — **not** the bond chain's 2d-1/2d-2 |
| 3 | `shekyl-cli` binary | **Off wallet2 — native RPC client under Shape B, target command set landed incl. proofs + rescan (as of 2026-08-04, WI-RPC-2a+2b+3 + Phase 4c)** — `rpc_client.rs` replaces `EngineContext`/`Wallet2` (the `shekyl-engine-rpc` dep is deleted; a grep-gate test holds the line); REPL/one-shot self-host `shekyl-wallet-rpc` in-process over a private `0600` UDS, `--rpc-url` targets an external daemon. Remaining RESERVED stubs (sign/verify, offline signing, wallet info, unattributed receives) name their gates — carriers in `FOLLOWUPS.md` §"WI-RPC-2b deferrals", parity in `CLI_PARITY_MATRIX.md`. UPDATE 2026-08-04 (Phase 4c, `feat/wallet-rpc-phase-4c-rescan`): CLI `rescan` un-stubbed over `rescan_blockchain`; `hard` accepted and reported as equivalent (one rescan, not two); failure path distinguishes pre-reset refusals from a post-reset failure so the user is told when history was already cleared (verified: `cargo test -p shekyl-cli`). UPDATE 2026-07-24 (WI-RPC-3, `feat/wallet-rpc-proofs`): proof commands un-stubbed — `get_tx_proof`/`check_tx_proof`/`get_reserve_proof`/`check_reserve_proof` are real commands with disclosure warnings, proof strings excluded from readline history (bearer artifacts); `get_tx_key`/`check_tx_key` deleted with parse-time guidance naming the DLEQ proof replacement (ratified rule-21 rejection). UPDATE 2026-07-23 (WI-RPC-2a high-review fixes, `feat/cli-rpc-client-core`): added non-interactive `create`/`restore` subcommands (`--seed-out`/`--seed-file`, `--password-file`/`--password-stdin`) for scripting; interactive `create` now refuses non-TTY seed output (no pipe/file leak) by preflighting before wallet creation; RPC request/response secret buffers zeroized (rule 35); `--rpc-url` sessions sync open-state from the server; transfer aborts release the reservation (discard-before-abort); chunked-response decode is byte-safe. UPDATE 2026-07-22: WI-RPC-2b landed the surface commands + deletions on `feat/cli-rpc-client-surface` (verified: `cargo test -p shekyl-cli`). UPDATE 2026-07-22: WI-RPC-2a landed on `feat/cli-rpc-client-core` (verified: `cargo test -p shekyl-cli` → `tests/rpc_session_e2e.rs`, incl. the no-wallet2 grep gate) |
| 4 | `shekyl-wallet-rpc` binary | **Phase 4b SPECIFIED methods + WI-RPC-1 tranche + `restore_wallet` + WI-RPC-3 proofs + Phase 4c `rescan_blockchain`; in-process spawn hardened to private UDS; submit verdict projection landed (as of 2026-08-04)** — lifecycle + read queries + `refresh` + send lifecycle (`build`/`submit`/`discard_pending_tx`) on `Engine::<SoloSigner>`. `rescan_blockchain` landed with Phase 4c (see its UPDATE). The transitional `shekyl-engine-rpc` (wallet2 FFI) it used to be distinguished from was **deleted** 2026-08-06 (roadmap B1, `chore/delete-shekyl-engine-rpc`): it had no Cargo dependents, no `rust/` consumers outside itself, and no place in the C++ link graph (`BuildRust.cmake` builds only `shekyl-ffi` / `shekyl-daemon-image`), so this crate is now the only Rust wallet RPC server. Its `rust-scanner` read-side cache feature retired with it (FOLLOWUPS entry closed); its `multisig` feature named no code in the crate, so dropping it from the `ci/multisig-feature` lane subtracted a no-op arm, not coverage. The C++ `wallet2_ffi` surface it wrapped is Phase 5's to delete. UPDATE 2026-08-04 (`feat/w4b-submit-verdict`, W-B step 1): `build_pending_tx` no longer stalls read RPCs — `Engine::build_pending_tx{,_async}` relaxed to `&self` (the FOLLOWUP's `&mut`-is-structural premise was wrong at source; trait + impl were already `&self`), builds serialized on the engine-owned `LocalPendingTx::build_permit` (Semaphore, 1 permit — network observable unchanged; raising it is a rule-21 reopen gated on anonymized segment fetch, see FOLLOWUPS "build concurrency permit stays 1"), `send::build_pending_tx` holds `read()`; pinned by `concurrent_builds_serialize_on_the_build_permit` (proven-to-bite at permit=2). UPDATE 2026-08-04 (`feat/w4b-submit-verdict`): submit verdict projection — `Engine::submit_pending_tx{,_async}` returns the identity-bearing `SubmitOutcome` (`Accepted`/`AlreadyInPool`/`AlreadyInChain { height }`) and `send::submit_pending_tx` maps it onto `SubmitVerdictView`, making the contract's `ALREADY_IN_POOL`/`ALREADY_IN_CHAIN` verdicts + verdict-scoped `confirmed_height` reachable (previously always `ACCEPTED`; FOLLOWUPS "Phase 4b: `submit_pending_tx` verdict is flattened" closed, audit-trail entry retained; the §2.5 disposition partition is untouched — the pool bit rides `SubmitSuccess::Broadcast` as the inform-only `BroadcastKind`, so no lock-lifecycle behavior changed; CLI renders the three verdicts; verified: `cargo test -p shekyl-engine-core -p shekyl-wallet-rpc -p shekyl-cli`). UPDATE 2026-08-04 (Phase 4c, `feat/wallet-rpc-phase-4c-rescan`): Engine `reset_scan_derived_state` + `start_rescan` (`engine/rescan.rs`; single-flight slot extracted to `engine/refresh_slot.rs`), wallet-rpc `sync::rescan_blockchain`, CLI `rescan`. Reset preserves `tx_keys` / payment-request rows / restore height / staking bond records and leaves the chain-global curve tree alone; refuses before touching the wallet on daemon-unreachable (-29201) or in-flight transactions (-29202, newly allocated). UPDATE 2026-08-07 (PR-SJ-2): OUTGOING projection closed the W-D FOLLOWUP; `get_transfers` projects send-journal rows as OUTGOING; W-D FOLLOWUP closed. (build write-lock + submit verdict closed on `feat/w4b-submit-verdict`, see the two UPDATEs above; reconciled at the merge) (verified: `cargo test -p shekyl-engine-core rescan`; `cargo test -p shekyl-wallet-rpc`; `git grep start_rescan` → `engine/rescan.rs`). UPDATE 2026-07-24 (WI-RPC-3, `feat/wallet-rpc-proofs`): landed the four proof methods — `get_tx_proof` (INBOUND/OUTBOUND selected by tx-key availability; OUTBOUND needs the retained `TxMetaBlock.tx_keys` secret, INBOUND works VIEW_ONLY), `get_reserve_proof` (all-balance or amount-bounded, largest-first selection), and wallet-less `check_tx_proof`/`check_reserve_proof` (daemon-only; no open wallet required); contract finalized in `wallet_rpc.yaml` same PR with error codes -29300…-29303 (verified: `cargo test -p shekyl-wallet-rpc`; `git grep get_tx_proof` → `shekyl-wallet-rpc/src/proofs.rs`). UPDATE 2026-07-22: WI-RPC-2a landed `restore_wallet` (BIP-39 mainnet/stagenet or testnet raw-seed hex + optional `restore_height`; contract in `wallet_rpc.yaml` same PR) and hardened `spawn_in_process` from auth-less loopback TCP to a `0600` UDS in a private per-spawn `0700` dir (priority-1 fix; verified: `cargo test -p shekyl-wallet-rpc`; `git grep restore_wallet` → `shekyl-wallet-rpc/src/lifecycle.rs`). UPDATE 2026-07-23 (WI-RPC-2a high-review fixes, same branch): `get_height` returns `wallet_height` with a nullable `daemon_height` (offline node no longer hides wallet status); testnet `restore_wallet` accepts the raw seed; internal error messages no longer echo `io`/persistence paths; `UdsCleanup` guards the socket before `restrict_socket_perms`. UPDATE 2026-07-19: WI-RPC-1 landed nine methods — receiving (`create_payment_request`/`list_payment_requests`/`make_uri`/`parse_uri`, FA-8d projection; rid-on-URI, no subaddress model), fees (`estimate_tx_size_and_weight`/`get_default_fee_priority`, single-source `predict_weight` projection), staking reads (`get_staked_balance`/`get_staked_outputs`/`staking_info`, authoritative `staking_read_view` — never `bonded_slots`); contract updated in `wallet_rpc.yaml` same PR (verified: `cargo test -p shekyl-wallet-rpc`; `git grep create_payment_request` → `shekyl-wallet-rpc/src/receiving.rs`). UPDATE 2026-07-09: Phase 4b send + refresh + reads (verified: `cargo test -p shekyl-wallet-rpc`; `git grep build_pending_tx` → `shekyl-wallet-rpc/src/send.rs`). UPDATE 2026-07-09: Phase 4b lifecycle. UPDATE 2026-07-09: Phase 4a scaffold. UPDATE 2026-07-06: OpenAPI contract landed #265 (decision-anchored) |