# Implementation index and phase map

**Purpose.** One page that answers "what does this identifier mean, which doc
owns it, and what has actually landed on `dev`?" The wallet/staking work spans
several plan documents whose numbering schemes evolved independently; this
index is the disambiguation layer. It is a *map*, not a status-of-record —
each track's own plan doc remains authoritative for design detail, and where
a plan doc's status table disagrees with landed code, **code wins** (§6 lists
the known disagreements at the verification date rather than editing those
docs from here).

**Method note (2026-07-18) — gate rows decay at the referenced item's rate.** A gate row
("blocked on X", "X is unbuilt") is a claim about *another* item's state and goes stale
when that item moves, not when its host document does. The **cleared-gate sweep is a
landing obligation of the PR that clears the gate**, not of the docs that name it: a PR
that builds or lands X must grep for rows naming X as a blocker (docs *and* code
comments) and correct them in the same landing. Blocked-when-actually-unblocked is the
expensive direction — nobody re-checks a gate that says it is closed. (Instances swept
2026-07-18: the M1 `K_COVER` gate list, the SP-R0 "SP-6 unbuilt" rows in FOLLOWUPS and
the 2D2 plan §2/§12, the WI-1 "made the chain live" comment — PR #333 and the SP-R0
Round-0 exit.)

**Verification stamp.** Statuses below were verified against landed code on
`dev` = `13c368707` (the PR #277 merge) as of 2026-07-09 for the
reward-emission rows (C-1 landed: whitelist flip at
`cryptonote_format_utils.cpp:706`, connect arm `apply_archival_emission_claim`
in `blockchain_db`) and the M1 / segment-freeze rows (PR #263 `6b6517bfc`
/ #264 `0d224faac` merged); other rows retain their prior code-anchored stamps
unless noted. When you update a status here, re-verify against code
(`git grep`, not plan prose) and move this stamp.

**Maintenance discipline** (institutionalized as
[`.cursor/rules/94-tracking-index.mdc`](../../.cursor/rules/94-tracking-index.mdc) —
new identifier families register here at birth; Phase 3+ / Stage 3+ work
items start with an index row).

- **Row format:** every status cell *leads* with the current status and its
  as-of date (e.g. `Landed (as of 2026-08-01)`); dated `UPDATE YYYY-MM-DD:`
  entries append *below/after* that line as the history chain. Readers get
  the answer without parsing the chain.
- **Two verification classes:** *code-anchored* items cite a `git grep`
  target on `dev`; *decision-anchored* items (round closures, wire freezes,
  contract docs — no code artifact) cite the commit hash or PR number of the
  doc edit that closed them.
- **Section contract:** rule 94 refers to §2 (identifier-family registry),
  §4–§5 (live / built-unwired / missing inventory), and §6
  (known-stale-statuses ledger) by role. Their meanings — and this numbering
  — are load-bearing; a restructuring that moves them updates
  `.cursor/rules/94-tracking-index.mdc` in the same PR.
- This doc never carries design content — link to the owning doc instead.
- New identifier families (a new prefix series in any plan doc) get a §2 row
  in the same PR that introduces them, after the rule-94 prefix-uniqueness
  check.

---

## 1. The collision this index untangles

Two independent schemes both used the bare tokens `2a / 2b / 2c / 2d`, and
one token (`2c-2b`) was reused *within* a single scheme:

| Token | Engine-layer meaning (`WALLET_REWRITE_PLAN.md`) | Bond-construction meaning (`ARCHIVAL_BOND_PR2_CHAIN.md`) |
| --- | --- | --- |
| 2a | Phase 2a — send path | Bond-PR 2a — synthetic-tree round-trip KAT (#156) |
| 2b | Phase 2b — stake lifecycle | Bond-PR 2b — StakeEngine actor, seed-owning model (#157) |
| 2c | Phase 2c — addresses/proofs | Bond-PR 2c-1 / 2c-2a / 2c-2b — real-tree KAT, Model D wiring, request path |
| 2d | Phase 2d — cold bundles | Bond-PR 2d-1 / 2d-2 — `P`-scan layer, `P`-isolated transport |

**Resolution (pinned 2026-06-19, `ARCHIVAL_BOND_PR2_CHAIN.md` §0):** the bond
chain is prefixed **`Bond-PR`** in doc prose; bare **`Phase 2x`** always means
the engine layer. Branch names are immutable history and keep their original
(unprefixed) names.

**The `2c-2b` double-use.** Within the bond chain itself, `2c-2b` names two
different deliverables in sequence:

1. **Bond-PR 2c-2b (request path)** — JoinMarket bond request layer, landed
   `feat/archival-bond-request` #163, plan doc
   [`ARCHIVAL_BOND_REQUEST_2C2B_PLAN.md`](ARCHIVAL_BOND_REQUEST_2C2B_PLAN.md).
2. **2c-2b re-scope (GF-7 scheduler seam)** — block-timed entry-seam planning
   plus GF-7 measurement hooks, landed PR #255 (2026-07-05), plan doc
   [`ARCHIVAL_BOND_2C_GF7_HOOKS.md`](ARCHIVAL_BOND_2C_GF7_HOOKS.md).

When you see `2c-2b` in prose, the linked plan doc disambiguates; new prose
should say "request path" or "GF-7 seam" explicitly.

---

## 2. Identifier families

| Family | Reads as | Owning doc | Notes | Completion Status |
| --- | --- | --- | --- | --- |
| **Phase 0–6** | Engine-layer wallet-rewrite phases: 0 baseline, 1 wallet domain model, 2 core operations, 3 `shekyl-cli`, 4 `shekyl-wallet-rpc`, 5 C++ deletion, 6 tests+docs. Phase 2 splits into 2a send / 2b stake lifecycle / 2c addresses+proofs / 2d air-gapped bundles | [`WALLET_REWRITE_PLAN.md`](WALLET_REWRITE_PLAN.md) | The original scheme; always prefixed `Phase` post-2026-06-19 | |
| **Stage 0–5** | Orchestration/actor architecture stages: 0 harness, 1 per-engine trait extraction, 2 KeyEngine actor, 3 `StakeEngine` implementation, 4 actor swaps (e.g. `PendingTxActor`), 5 `ArchivalEngine` | [`WALLET_REWRITE_PLAN.md`](WALLET_REWRITE_PLAN.md), [`STAGE_0_HARNESS.md`](STAGE_0_HARNESS.md), `docs/completed/STAGE_2_KEY_ENGINE_ACTOR.md` | — | |
| **Stage 1 PR 3–7** | Per-engine trait-extraction PRs: 3 KeyEngine, 4 RefreshEngine, 5 PendingTxEngine, 6 PersistenceEngine, 7 EconomicsEngine | `STAGE_1_PR_{4,5,6,7}_*.md`, [`STAGE_1_PER_PR_TEMPLATE.md`](STAGE_1_PER_PR_TEMPLATE.md) | PR 3's docs (KeyEngine + M3 preflights) archived under `docs/completed/` | |
| **M3a–M3e** | Stage 1 PR 3's architectural-inheritance migration steps | `docs/completed/STAGE_1_PR_3_MIGRATION_PLAN.md` | Referenced from `.cursor/rules/16` | |
| **Bond-PR 0–2d** | Bond-construction chain: 0 `P`-derivation, 1 bond builder, 2a–2c KATs+wiring+request, 2d-1 `P`-scan, 2d-2 transport | [`ARCHIVAL_BOND_PR2_CHAIN.md`](ARCHIVAL_BOND_PR2_CHAIN.md) | The chain's §2 arc table is the per-sub-PR record | |
| **SP-0…SP-7, SP-R0** | `P`-scan pipeline slices (2d-1); SP-R0 is the reconcile-GC follow-on — three arms (#1 spent-funding prune / #2 done-side slot GC / #3 full-scan phantom drop) | [`ARCHIVAL_BOND_2D1_PSCAN_PLAN.md`](ARCHIVAL_BOND_2D1_PSCAN_PLAN.md); SP-R0's own round: [`ARCHIVAL_BOND_SP_R0_PLAN.md`](ARCHIVAL_BOND_SP_R0_PLAN.md) (Round 0 ratified 2026-07-18, DQ-A…F) | SP-0…7 shipped grouped as PR-A / PR-B, not one-PR-per-SP; SP-R0 arms sequence behind the staker-activation round (its own round doc) | |
| **PR-A / PR-B** | The two validation-surface bundles that shipped SP-0…SP-7 (#201, #205) | [`ARCHIVAL_BOND_2D1_PSCAN_PLAN.md`](ARCHIVAL_BOND_2D1_PSCAN_PLAN.md) | Named per rule 19 (validation-surface bundling) | Complete |
| **SP-T0…SP-T5** | `P`-isolated transport slices (2d-2): Tor bootstrap, circuit isolation, fetch, …, broadcast | [`ARCHIVAL_BOND_2D2_TRANSPORT_PLAN.md`](ARCHIVAL_BOND_2D2_TRANSPORT_PLAN.md) | Sub-docs: `_SP_T0_TOR.md`, `_SP_T2_FETCH.md`, `_SP_T4_BROADCAST.md` | |
| **PR-E0…PR-E3** | Reward-emission implementation PRs; C-1 (ML-DSA equality + membership-only verify) is PR-E3 step 8. **C-1 LANDED (as of 2026-07-09, PR #277 merged, `dev` `13c368707`)**: all six commit-blocks on `dev` with the F-B5/F-B1 findings fixed in-PR (verified: whitelist entry `cryptonote_format_utils.cpp:706`; `apply_archival_emission_claim` in `blockchain_db`). UPDATE 2026-07-09: #277 merged; track front moves to the E4-gate pair (wallet-side claim builder → regtest e2e), then E4/E5. History: E0/E1/E2 landed; gating design round CLOSED 2026-07-07 (policy trio Q3/Q11/Q12 resolved, WS-1 + WS-2 ratified); PR-E3 verify body, WS-1 held-sourcing correction, and WS-2 write-side landed via PR #269 | [`REWARD_EMISSION_LEG.md`](REWARD_EMISSION_LEG.md), [`REWARD_EMISSION_VIN_PLAN.md`](REWARD_EMISSION_VIN_PLAN.md), [`REWARD_EMISSION_E3_GATING_ROUND.md`](REWARD_EMISSION_E3_GATING_ROUND.md) | C-1 was the hard Stage-3 emission blocker; residue = wallet-side claim builder + regtest e2e (E4/E5 gate) | C-1 landed (#277); E4/E5 open |
| **CB-1…CB-5** | Wallet-side emission claim-builder round-1 design questions (recompute sourcing, actor locus, claim cadence, fee sourcing, refusal taxonomy) — the E4-gate part-1 work unit | [`EMISSION_CLAIM_BUILDER.md`](EMISSION_CLAIM_BUILDER.md) | Registered 2026-07-09 at birth (rule 94 §1); prefix `CB-` passes the uniqueness check against Phase/Stage/M/Bond-PR/SP/SP-T/PR-A/PR-B/PR-E/CT/GF/S/R/F/DQ/SCE/WI/M1 | **Round 1 CLOSED (2026-07-09), all five ratified at source.** CB-1: new daemon RPC as `EmissionEpochSource` gather; ratifying reason = the single-evaluator invariant `capped_work_milli` (`consensus_state.rs:182-188`) enforces, lying-daemon bound = claim-liveness, safe-by-recomputation, joins the 2d-2 remote-daemon reopen family. CB-2: StakeEngine actor (`stake_engine.rs:16-50` seed-gone-after-`assemble()`), pin = sign within that discipline (use a derived bundle, never re-borrow the seed). CB-3: routing-not-closure, joint-grading reopen (GF-4 grades cadence + amount + holdings stratum jointly). CB-4: ratified on the **structural three-leg exclusion** (backing in opaque `canonical_bytes` never deserialized into `pseudoOuts`; dispatch + leaf size checks `tx_verification_utils.cpp:175` + `rctSigs.cpp:362`), not on the KAT. Arming KAT was necessary-not-sufficient (arm-1 identity green-by-construction); **fixed in-PR**: arm 1 strengthened to production operand functions (`classify_archival_tx` + `shekyl_checked_sum_amounts`, mirror removed) arming the operand + leaf legs, E3 §2.2 rationale corrected, `50-testing.mdc` coverage-boundary discipline pinned. Remaining blob-boundary invariant arm (drive full dispatch, assert verdict invariant under `canonical_bytes` variation) is harness-gated → homed to §8 PR-4, regtest e2e + blob-boundary arm (`FOLLOWUPS.md` Q11-KAT item). CB-5: cause-blind zero-share (single `K_COVER` site `k_cover.rs:11`/`consensus_state.rs:470`), pin = `SelfCheckFailed` must also refuse cause-blind. **Round 2 CLOSED (2026-07-09), CB-1 RPC surface + PR chain** (`EMISSION_CLAIM_BUILDER.md` §7–§8): single-gather pin recorded as CB-1(b) daemon-side face; window-batched `p_id`-only query (transport cause-blindness); field enumeration with the `k_cover`/`frozen_shard_count` exclusion recorded CB-5-structural; §2 boundaries anchored to the rejection predicates (top-strict corrected; bottom verified single-sourced through `claim_window_floor`, consumption-not-re-derivation pinned). Four-PR chain endorsed, PR 4 gated on #281; implementation underway from PR 1 (daemon RPC) with two grep-checkable watch items named at §8 closure. **PR 1 implemented**: `get_archival_emission_claim_source` (both transports, RPC minor 17→18), marshal helper over the single gather in its windowed one-scan form (`gather_archival_emission_window_snapshots` — review hardening: one serve-credit pass per request, thread-aware `block_rtxn_start` txn selection in the archival read helpers) under one `db_rtxn_guard`, `shekyl_archival_claim_window_floor` FFI delegate, Rust decode to the verify-side views (`emission_source.rs`, loud on non-OK `status` and ordering-invariant violations, views via the single-sourced `EpochCloseInputs::verify_view`); both watch items pinned by tests (`archival_claim_source_rpc.cpp`, fixture shared with the substrate KAT via `archival_lmdb_test_helpers.h`). **PR 2 implemented (2026-07-10)**: pure assembly module `engine/emission_claim.rs` — **four-boundary** claimable-epoch derivation (top/expiry/dedup via the connect predicate on a purity-pinned scratch clone + differential oracle test; **strict finalization** via verify's `epoch_close_height` — a review finding: the connect window admits the youngest epoch one count before verify accepts it, verified at `blockchain_db.cpp:504-514`/`archival_claim_source.cpp:30-32`, closed with `EpochSkip::NotFinalized` + premise-asserted boundary KAT after an earlier "foreclosed" claim was disproved at daemon source; recorded as the §2 step-1 post-closure sharpening), M1 positive-share forward obligation discharged (claimable ⇔ recomputed share `> 0`, no `K_COVER` read), canonical work-claim rows with the `u64→u32` scarcity guard armed at the builder site (const-assert production guarantee + `try_from` refusal + hostile-fixture KAT) and the entry-sum hard check, bound-or-split sizing measured by the production encoder at structural maxima, step-7 `self_check_claims` driving the literal `emission_vin_verify_claims` (CB-5 pin discharged: `SelfCheckFailed` a cause-blind unit variant, verify detail log-local; differential KAT — six vin mutations + three context arms, each flips accept→refuse). `InsufficientBacking` + real backing/auth legs defer to PR 3 with their constructors. **PR 2 review round (2026-07-10)**: three correctness fixes — claims budget re-based on the binding per-tx weight limit (`shekyl_wire::TX_WEIGHT_LIMIT` = 149 400 pins the C++ `get_transaction_weight_limit`; the old `MAX_TX_SIZE`-derived budget could pass a permanently-unsubmittable batch), verify step 2's join bound applied at derivation (`epoch_is_before_join` extracted, one definition for verify + builder; retire-then-rejoin no longer wedges the batch behind `SelfCheckFailed`), and the settled/height pair decode-enforced consistent (`settlement_epoch_at_height`; inconsistent daemon reply is one loud `Malformed`, plus Copilot's overflow-close-height ⇒ `SourceInvalid`). Cleanups: scratch-clone oracle retired (read-only `epoch_is_not_settled` extracted, `check_and_set` rewired through it — the V3.1 FOLLOWUPS item landed on its named reopening trigger), steps-4/5 recompute single-sourced (`claimant_reward_share` shared by verify body + derivation), `ClaimableEpochs<'src>` borrows the bond context and carries rows built in the admitting scope (stale-pairing panic surface gone), one vin construction site (`claims_vin`) with pop-in-place bound-or-split, `size_deferred: Vec<u64>`, item-level staging allows (rule 45), shared KAT fixture shape (`EMISSION_KAT_SHAPE`). **PR 3 implemented (2026-07-11, `feat/emission-claim-handler`)**: wire emission-input arm (`0x04`, closes the latent scanner break), `EmissionReward` scan arm (GF-4b item 2), designated-backing selector (`BackingSet` → `DesignatedBacking`, GF-4b §5 item 1 — exclusivity + Q11 fee-exclusion as type properties), `AssembleEmissionClaim` handler (CB-2 `AssembleBond` sibling: F-C1c assembly order, `prove_backing_membership` in `spawn_blocking`, dual auths from the derived bundle — no seed re-borrow — daemon-side differential KAT over the produced bytes), Engine-side claim orchestrator (`engine/claim_orchestrator.rs`: fetch → reference anchoring, daemon two-sided gate ∩ `REF_ANCHOR_AGE` → provable-record filter → designate → fee sweep → path assembly → handler, reply unbroadcast per CB-3; end-to-end test over a real 30k-block `CurveTreeClient` with daemon-side re-derivation against the real root), and the PR-1/PR-2 staging-allow deletion pass (remaining claim-surface allow: the orchestrator + its reply fields, staged against the CB-3 dispatch seam). The builder half of the E4 gate is complete; PR 4 (regtest e2e + blob-boundary arm) is the chain's only open item, gated on #281. **PR 3 review round (2026-07-11)**: six correctness/invariant fixes — slot filter inside `BackingSet::from_spendable` (the all-slot union let a rotation-overlap foreign record win designation → persona deterministically unable to claim), the designation/same-tip anchor is the gather TIP not the block count, the wire validator gained the `check_money_overflow` parity sum for loud vouts, the §7.4 transport pin became structural (`PersonaIsolatedTransport` marker bound on `orchestrate_emission_claim`), the handler enforces Q11 backing∉fee-set at its boundary (`BackingInFeeSet`), and zero fee inputs refuse structurally (`ClaimFeeInputsRequired`, not `InsufficientFunding{0,0}`). Cleanups: shared `two_sided_reference_height` in shekyl-curve-tree (transfer + claim paths), shared `derive_spend_parts`/`construct_vouts_to_base`/`prove_input_from_spend`, lazy memoized `OwnEmissionSlots` P-scan classifier, single test-side wire encoder (`source_json`), rule-22 CLAUDE.md index row. **UPDATE 2026-07-12 — PR 4 split 4a/4b/4c; PR-4a implemented (`feat/emission-regtest-e2e`)**: driving the harness live proved the daemon's Rust submit engine accepts neither transaction kind the e2e needs (no bond-post Phase-C battery — and `phase_a.rs`'s "bond posts can't clear Phase A" docs are stale, disproven by a live `FeeTooLow`; Phase A rejects `Input::ArchivalRewardEmission` outright), so the daemon submit legs split out per rule 19/06 (`EMISSION_CLAIM_BUILDER.md` §8 "PR 4 split"). PR-4a lands: the CB-4 blob-boundary arm (**Q11-KAT FOLLOWUPS item CLOSED** — fixture made valid under full `ver_non_input_consensus` dispatch, verdict-invariance arm under `canonical_bytes` variation, oversized-`pseudoOuts` control), the FAKECHAIN-only settlement-epoch-blocks env lever + serve-credit-bit injection RPC (e2e enablers, both fail-closed off FAKECHAIN), `LocalNodeRpc` (local-posture `PersonaIsolatedTransport`, DQ-T2.3 choice site), the `SweptFeeInputs`/`ClaimOperands` designation-event witness (PR-3's named forward item, landed with the seam as scoped — both step-2 runtime refusal arms deleted) + the CB-3 dispatch seam (`engine/claim_dispatch.rs`: read-only identity projection → orchestrate → persist-before-dispatch seal → posture→submit choke point; the last claim-surface staging allows deleted), the staker regtest harness up to the daemon submit gap (production transfer → P-scan discovery → bond assembly → dispatch, with the `Malformed` tripwire pinning the gap), and two harness-surfaced production bug fixes with unit KATs (payment path encapsulated to `montgomery(view_pk)` — cross-wallet payments were unrecoverable; P-scan exhaustiveness fetches full tx bodies — pruned FCMP++ spends can never re-hash to their committed hashes). **PR-4a review round (2026-07-12, high-effort workflow review — 29 verified findings, all addressed in one round; record in `EMISSION_CLAIM_BUILDER.md` §8)**: the SEB override converted to explicit **arming** (unarmed processes — every wallet, every public-net daemon — run the genesis schedule and warn once on an ignored lever; the daemon arms only on FAKECHAIN, refuses on the lever's presence off it, refuses invalid values loudly instead of clamping, and pins the effective schedule into the fakechain datadir — reopen-under-a-different-schedule is a loud refusal); the CB-3 seam gained **persist-before-dispatch** (`PendingEmissionClaim`, pending-post block v4: bytes + fee-gindex reservation into the shared `reserved_gindexes` union + claimed epochs; one live claim per persona = in-flight dedup, `ClaimPending` refusal; retirement/resubmit = the named claim-driver slice) and lost its rotation side effect (claimant id via the read-only `PersonaIdentityOf` projection — activation would invalidate in-flight handles and wipe a retired ephemeral persona); typed taxonomies: `NoSpendableFunding` (empty eligible set ≠ zero-value shortfall; the `ClaimFeeInputsRequired` mapping keys on the typed arm, not `available == 0`) and `OutputNotYetDrained{gindex}` (the harness's substring match on a rendered error deleted); the P-scan full-body fetch names the storage-pruned-daemon cause + remedy; credential grammar authority-scoped at the source (`SimpleRequestRpc`) with `LocalNodeRpc`'s pin moved to `IpAddr::is_loopback`; consolidations: single `DaemonEngine::fetch_scannable_block_in_form` override point, `BroadcastSubmitter::local` (three copies deleted), `EpochCloseInputs::close_view` (FFI lockstep retired), `EmissionClaimReceipt` embeds the reply, shared e2e `create_wallet`, mtime-gated `pscan_until`, `tokio::join!`ed seam reads; `CORE_RPC_VERSION` → 3.19. **UPDATE 2026-07-18 — PR-4b bond-post half implemented**: the JoinMarket bond-post Phase-C battery wired in `shekyl-daemon-rpc` `verifier.rs::verify_bond_post` (§8.7.1 BP-rows closure note: O6 → bond CT balance → BP2/BP5/BP3+BP4 as native `shekyl-archival-retention` calls — the same functions the C++ oracle reaches over FFI — → Bp+ → funding-subset FCMP++ → all-input PQC), `SubmitFacts` extended with the BP3 `bond_record_exists` fact (snapshot probe keyed on the vin's claimed id + Phase-D re-probe from the reparsed blob; record-appears-during-C → `DoubleSpendConflict`), the stale "§13 wire reshape / cannot clear Phase A" reachability docs corrected (no wire contradiction existed), non-JoinMarket kinds refused under a named rule-21 reopen, the production-stack bond-post oracle fixture + BP mutation KATs added to `submit_verifier.rs`, and the PR-4a tripwire promoted to accepted-and-applied — surfacing + fixing two latent production bugs (transfer path omitted the `0x07` PQC leaf-hash blob ⇒ every transfer output unspendable; `prepare_handle_incoming_blocks` `bad_variant_access` on archival-vin blocks; `docs/CHANGELOG.md`). **UPDATE 2026-07-19 — PR-4b COMPLETE (emission half)**: §8.7.2 E-rows pinned; `SubmitTxKind::Emission` + Phase-A statics; `SubmitFacts::emission` E6/E7 bundle (C++-owned-handle marshal, one lock scope); native `verify_emission` battery over the shared `emission_vin_verify_*` minters; E6 Phase-D claim-slot re-probe → `DoubleSpendConflict`; FOLLOWUPS "daemon Rust submit engine" item CLOSED. Open: the claim dispatch driver's retirement/resubmit legs, then **PR-4c** (e2e proper — now unblocked on the daemon side; the E4 merge gate closes there) |
| **CT-1…CT-5 (5a–5d)** | Curve-tree client series | [`CURVE_TREE_CLIENT.md`](CURVE_TREE_CLIENT.md); closeouts in `docs/completed/CT*` | Series closed (`CT5_SERIES_CLOSEOUT.md`) | Complete |
| **GF-1…GF-12** | Gate-6 firewall findings; **GF-7** = the funding-seam (principal↔`P`) unlinkability property, a genesis gate | [`ARCHIVAL_FIREWALL_GATE6.md`](ARCHIVAL_FIREWALL_GATE6.md) | GF-7's measurement hooks: [`ARCHIVAL_BOND_2C_GF7_HOOKS.md`](ARCHIVAL_BOND_2C_GF7_HOOKS.md). GF-4b's backing-lineage design + landing record (incl. the **GF4b-1…GF4b-6** review-round findings, sub-tokens scoped to that doc's §6): [`ARCHIVAL_GF4B_BACKING_LINEAGE.md`](ARCHIVAL_GF4B_BACKING_LINEAGE.md) | |
| **S-1…S-6** | Gate-6 §10.12 seam-scenario rows (adversary fusion analysis) | [`ARCHIVAL_FIREWALL_GATE6.md`](ARCHIVAL_FIREWALL_GATE6.md) §10.12 | Not to be confused with S6 the certify-draw plan (`docs/completed/ARCHIVAL_BOND_S6_CERTIFY_DRAW_PLAN.md`) | |
| **F-D1…F-D6** | Gate-6 R4 drain-event findings: F-D1 amount taint-carve, F-D2 UI-default, F-D3 exit-standoff mechanism, F-D4 a-priori exit window, F-D5 quantization (disposition round ran 2026-07-17: no grid at genesis — see status), F-D6 anti-drift derive | [`ARCHIVAL_FIREWALL_GATE6.md`](ARCHIVAL_FIREWALL_GATE6.md) §12 | Minted 2026-07-11 (R4 draft); registered 2026-07-15 (backfill — the minting PR omitted the row). Prefix `F-D` is distinct from `F-N`'s `F<digits>` tokens (no parse collision) | F-D5 **disposition round RAN — width does not derive, NO GRID at genesis** (as of 2026-07-17, same day as the charter; Gate-6 §12.7 OUTCOME: the harm survives F-D1 in one sentence — the grid targets §18.12's lifetime-aggregate drain-all floor, not the subsum match F-D1 closes — but the structural derivation fails at three source-anchored population entry points: `r_market` inside `scarcity_milli` (no population-free lattice below the `Curve`'s shape constants), the `budget(E)/Σwork(E)` value spacing (a quotient of two economics quantities; X-3's path had no such quotient), and reachable-collision ≠ delivered cover (epoch sets published per claim, `reward_P(E)` a loud `P`-attributed mint vout, so collisions among published sums are population occupancy); the grid's cost side is derivable, its benefit side is not; pass-4's cost half transfers intact; Gate-6 close condition (iv) **discharged**, the band registered as the first R5 S-2 ledger row; any future grid is a hard fork per the §12.7 reopen clause). Charter context: F-D5 **disposition round CHARTERED** (as of 2026-07-17 — the "→ §14.4 economics" routing was a dangling pointer: WI-4 §14.4 is the closed partition arm, and the phrase was a summary-layer conflation of WI-4's strata/lifetime riders (discharged, PR #291) with its quantization GF-4-candidate routing (§18.10 R-3); Gate-6 close condition (iv) re-worded by dated amendment (§12.9), charter at §12.7: a fresh measurement round refused per method note 3 (the grid's privacy benefit is behavioral, testnet-unproducible), the round runs a **structural-derivation attempt first** (grid width from reward-curve structure, rate-independent, X-3-shaped) with the genesis-frozen consequence named (no derivation ⇒ no grid at genesis; post-genesis = hard fork) and the §10.12 pass-4 banding rejection reconciled explicitly; the lifetime-aggregate band registers as an R5 S-2 ledger row either way); F-D3/F-D4 **DELETED — deletion PR landed** (as of 2026-07-17, at Gate-6 §12.9 decision 5's scope: `exit.rs` wholesale + both features + consumer acknowledgment lines + conformance/test arms; F-D6 and `release_cooldown_elapsed` confirmed out of scope; inventory at FD4 §15.4 item 1's landing update); F-D6 **named-const half STANDS; anchor helper DELETED** (as of 2026-07-17 — the deletion PR orphaned `release_cooldown_anchor_height`, its only production consumer was `draw_exit_gap`; deleted per rule 15 by its own decision rather than inheriting decision 5's exclusion, keep-as-derivation-home declined as speculative generality; reopen note at `release_cooldown.rs`, disposition at Gate-6 §12.7); F-D1/F-D2 **UNBLOCKED, re-homed** (§15.5 answered — Gate-6 §12.9: the exit seam is the principal↔user crossing, WI-4 §18.13; F-D1/F-D2 are its class-3 mistake-set closure, build/arms unchanged; F-D2 additionally carries the accepted FD4 §16.4 funding default). UPDATE 2026-07-17 (later): **F-D1 BUILT** (`shekyl-engine-core/src/engine/{drain_orchestrator,drain_amount,drain_select}.rs`; (a)+strip — the orchestrator is the sole trust boundary naming/holding `PFundingOutputRecord` (which carries `MintLineageOutput`), projecting to the aggregate scalar + stripped `{output_id,amount,spendable_height}` vector; M1 grep-arm landed on a skeleton first, proven-to-bite on a deliberate forbidden import (exit 101), reverted, then armed as `fd1_arm_*` unit tests; Gate-6 §12.3 build note) and **F-D2 core-side surface LANDED** (aggregate-only `DrainBalance`/`drain_balance`, scalar-only `plan_drain`; Gate-6 §12.4 build note) — `plan_drain` lands as a correctly-carved planner with **no data source and no consumer yet**. **F-D2's remaining half is NOT a pending UI default and NOT gated on RPC** (the GUI links the engine in-process; `shekyl-engine-rpc` is an FFI library, not a wallet-RPC server): it is a whole **unbuilt `P`-value-out (drain-send) subsystem** in `shekyl-gui-wallet` — a `P`-scan data source (`PScanState.funding_outputs`) feeding the planner + a drain tx assemble→sign→broadcast path + the round-number/random-split default on top. **R4 open on that subsystem.** UPDATE 2026-07-17 (follow-up): F-D6's anchor helper `release_cooldown_anchor_height` deleted (verified: `git grep release_cooldown_anchor_height` → doc references + the `release_cooldown.rs` reopen note only) — the mechanism deletion removed its only production consumer, and the orphaning got its own rule-15 decision; F-D6's named-const half stands. UPDATE 2026-07-17: the mechanism-deletion PR landed (verified: `git grep draw_exit_gap` on the branch → doc references only); R4 close condition (iii) satisfied. UPDATE 2026-07-16 (night — R4 decision round, Gate-6 §12.9, RATIFIED): §15.5 answered as above; joint grade dissolved by **four-axis** attrition (ratification added F-W10 — output-count phantom, the drain is not an identifiable transaction under FCMP++; GF-10 grades standalone); harness fork resolved deleted (not resurrected). UPDATE 2026-07-16 (round 4 ratified): the exit-timing channel is phantom (F-W7 — no principal-side observable within the seam, FD4 §2.1/§15; F-W8 retracted X-3's harm model); delete-mechanism-keep-tripwire ratified, `RELEASE_CHECKLIST.md` seal entry removed, harness sequenced behind the §15.5 gate question, sentinel never shipped a value (K_COVER pattern vindicated, FD4 §15.7). UPDATE 2026-07-16 (later same day): the F-D4 §9 step-4 sweep was built and run (`shekyl-staking-sim --exit-standoff`; findings at FD4 §14) — the instrument **never landed on `dev`**; preserved at `archive/feat/fd4-exit-sweep-2026-07-16` per the FD4 §15.4 landing disposition, its fate riding the §15.5 answer. UPDATE 2026-07-16: `draw_exit_gap` landed against the F-D4 sentinel (verified: `shekyl-standoff/src/exit.rs` — typed draw, golden vector + seal tripwire, shared-trigger negative control); F-D6 anchor derivation landed (`release_cooldown_anchor_height`, boundary-tested against `release_cooldown_elapsed`; staking-sim `20_000` doc fossil purged). UPDATE 2026-07-15: F-D3/F-D4 gate FIRED, open for build; F-D4 derivation committed (`ARCHIVAL_EXIT_STANDOFF_FD4_WINDOW.md`) |
| **X-1…X-3** | Exit-seam standoff bounds/regimes: X-1 steady-state lone exit (rate-driven lower bound), X-2 L17 crisis cohort (intra-cohort mixing upper bound), X-3 anchor-merge (rate-independent lower bound `W ≥ 2 × SEB`, derived from the quantization lemma) | [`ARCHIVAL_EXIT_STANDOFF_FD4_WINDOW.md`](ARCHIVAL_EXIT_STANDOFF_FD4_WINDOW.md) §4–§5 (X-3 at §5.2a) | Minted + registered 2026-07-15 (X-3 added same day, review round 3). Prefix `X-` unused by any other family | **CLOSED by round-4 premise audit** (as of 2026-07-16) — X-3 harm model retracted (F-W8: cohort membership is `last_served_epoch`, already public; the partition only fed assignment, a function of the phantom `T`); X-1/X-2 conditional on `T` and archived as the FD4 §15.4 tripwire's re-run template; no seal. UPDATE 2026-07-16 (round 4 ratified): deletion-with-tripwire per FD4 §15.4; the geometry (quantization lemma, coverage boundary) survives as public-height arithmetic, the harms do not. UPDATE 2026-07-16: the §8 sweep was built and run (`shekyl-staking-sim --exit-standoff`, `exit_standoff.rs` — never landed on `dev`; archived at `archive/feat/fd4-exit-sweep-2026-07-16`) — X-1 N-sweep, X-2 cohort grid, X-3 coverage boundary certified (measured overlap = `(W − SEB)/W` at all three windows), §8.3 negative control caught, thin regime unbiased; pre-seal findings at FD4 §14 (delivered X-1 cover is `ρ_x · σ_L`, latency-gated; X-2 trough needs `σ_L` on the `W/2` scale — 71/72 rows reported failing per GF7 §5.1, the §5.3 costing surface). UPDATE 2026-07-15: review rounds 1–3 run (X-1 bound reframed as a ~19× planning box per F-W3; X-3 folded into the §5.4 rule per F-W6); sweep pre-registered |
| **F-W1…F-W10** | F-D4 window-derivation review findings. Round 1: F-W1 candidate-fails-own-bound (blocking), F-W2 `σ_L`-is-a-design-lever (load-bearing; §5.3 joint constraint, lever-vs-queue costing). Round 2: F-W3 value-not-derivable-pre-measurement (blocking; `K_COVER`-pattern sentinel + frozen decision rule, sealed by the Phase 7.7 stressnet read), F-W4 stacked-marginal-conservatism (10th percentile of the joint `(N_P, c)` read committed a-priori), F-W5 exit-seam `N_t` re-derivation owed before the seal. Round 3: F-W6 structural-argument-is-a-derived-bound (X-3 anchor-merge folded into the rule as `max((N_t−1)/ρ_x, 2 × SEB)`, cost stated: queue-predicate LHS hard floor makes the F-W2 costing load-bearing in every measured world). Round 4: F-W7 phantom-`T` premise finding (blocking — the correlated observable was never named; no population within the seam), F-W8 X-3 harm-model retraction (cohort membership already public). Post-ratification: F-W9 repetition-premise correction (§13.3's `m` is a bounded lifecycle count, not a growth variable — one mandatory observable crossing; the cross-persona `q^k` relocation raised and withdrawn in the same section for want of a linking key); F-W10 output-count-axis phantom (Gate-6 §12.9 decision 2 at ratification — under FCMP++ the drain is not an identifiable transaction, so its output count is not an observable; the §2.4 pin was a CryptoNote-lineage carry never re-walked across the crypto change) | [`ARCHIVAL_EXIT_STANDOFF_FD4_WINDOW.md`](ARCHIVAL_EXIT_STANDOFF_FD4_WINDOW.md) §10–§12, §15–§16; F-W10 at [`ARCHIVAL_FIREWALL_GATE6.md`](ARCHIVAL_FIREWALL_GATE6.md) §12.9 | Minted + registered 2026-07-15 (F-W3–F-W5 round 2; F-W6 round 3, same day); F-W7/F-W8 minted round 4, 2026-07-16; F-W9 minted post-ratification same day (doc §16); F-W10 minted at the §12.9 ratification same day (Gate-6 doc). Prefix `F-W` distinct from `F-`/`F-D` (no parse collision) | **All resolved (F-W1–F-W10, as of 2026-07-16) — round 4 RATIFIED: deletion-with-tripwire (doc §15.4); F-W9 rode the §15.5 hand-forward, ANSWERED at Gate-6 §12.9 (seam re-homed; §16.4 default accepted); F-W10 added at the §12.9 ratification (fourth attrition row — output-count phantom; Gate-6 §2.4 discipline retired; method note 5 adopted)**. UPDATE 2026-07-16 (F-W9, post-ratification): §13.3's repetition premise corrected by lifecycle enumeration (doc §16) — `m` = 1 mandatory observable crossing (`JoinMarket`; `bond_post.rs:540` one-per-life), 2 wallet-default-closeable optional classes (`HoldingsUpdate`-add, credit-bearing `Rebond`), 0 observable exits (voluntary refund CT-hidden in-tx; terminal slash emits nothing), rejoin unmodeled (self-harm, slot-indexed derivation); geometric-collapse clause and the residual's `σ_L` routing fall, S-2 routing survives narrowed (its composition question now finite); cross-persona intersection withdrawn (§16.3 — no linking key across `P_id`s; surviving surfaces are the T-4 counterparty ledger and the entry standoff's shared-trigger case); §16.4 GF-4b-filter proposal handed to R4 with its growth-gating cost; Gate-6 §11.8 method note 4 adopted (*enumerate the events that compose*); WI-4 §13.5 + FOLLOWUPS carry-4 citations swept. UPDATE 2026-07-16 (ratification): F-W7/F-W8 resolved — deletion ratified with the harness sequenced behind the §15.5 gate question (handed forward to Gate-6 R4, audit attached: does GF-4's exit seam re-home to §18.13, and what does the joint grade become without its timing axis); `RELEASE_CHECKLIST.md` seal entry removed; observer-as-code adopted as method posture (doc §15.6 + Gate-6 §11.8 method note 2). UPDATE 2026-07-16 (round 4): premise audit run (doc §2.1/§15) — no population for the principal-side re-appearance `T` within the seam (refund structural: in-tx hidden outputs, `bond_connect.rs`; rotation dead by S-5/T-A1 + in-place `HoldingsUpdate`; network spent as §10.9 conditioning; off-chain crossing re-homed to Gate-6 §18.13/S-2). §5–§14 outputs conditional; sentinel holds the frozen state by construction. UPDATE 2026-07-16: F-W5 resolved a-priori before the sweep grades (derivation doc §13) — **exit-seam `N_t = 10`, by derivation, not inheritance** (seam-variant facts land on `W`/graded arms/regime rows; the repetition asymmetry cannot be priced into a per-event anchor and routes to `σ_L` + the S-2 exposure ledger as a named residual; `EXIT_TARGET_ANON_SET` minted at the sweep-harness PR). Same day: `draw_exit_gap` **landed** against the sentinel (verified: `shekyl-standoff/src/exit.rs`). UPDATE 2026-07-15: F-W1–F-W4, F-W6 resolved by same-day amendments (F-W1's round-1 fix superseded by F-W3's sentinel reshape); F-W5 pinned, derivation owed pre-seal |
| **Round N / R N** | Per-doc adversarial design rounds; each design doc numbers its own | Owning doc's round table | Gate-6 **R1–R2 closed** (R2 network/transport closed 2026-07-11, `ARCHIVAL_FIREWALL_GATE6.md` §10.13; its subject = the 2d-2 transport track, still partial-built/inert per §4); **R3 designed + adversarial pass run** (timing/rotation, §11 — GF-10 claim-jitter pinned as mechanism + structural window; grading routed to the R4/GF-4 joint grade; §11.8 pass folded 4 findings incl. one retraction); **R4 drafted** (§12, the drain-event firewall — GF-7 graded PROVISIONAL-PASS; GF-4 value-out: F-D1 drain-amount taint-carve complete pre-code pin (a)+strip, F-D2 UI-default, F-D3/F-D4 exit standoff — **FSM gate FIRED 2026-07-15**, F-D5→~~§14.4~~ *(dangling — disposition round ran 2026-07-17, no grid; see the F-D row)*, F-D6 anti-drift; joint-graded); R5 planned (cross-layer sign-off — S-2 ledger + S-3 sim). UPDATE 2026-07-15: the F-D3/F-D4 activation criterion (Gate-6 §12.5: a verify path reads the cooldown as a spendability gate) FIRED — `release_cooldown_elapsed` is enforced consensus with two verify consumers (verified: `bond_post.rs:369` `HoldingsUpdate`-drop per-shard anchor, `:627` `Unbond` whole-record anchor; FSM landed via PR #303/#307). The R4 pre-seal blocker chain (FSM frictions → age-stratified sim reconciliation) was already discharged (`STAKER_ARCHIVAL_SIM.md` §L18; P2B-7 Pin-4/Pin-5 closed PR #303 2026-07-14). F-D3/F-D4 are open for build (`draw_exit_gap` unbuilt — `shekyl-standoff` `draw.rs` carries only `draw_entry_gap`); Gate-6 §12.5–12.8 + `PHASE_2B_FSM_RETOOL.md` P2B-7 residuals row swept same day. The F-D4 a-priori window derivation is committed (`ARCHIVAL_EXIT_STANDOFF_FD4_WINDOW.md`, 2026-07-15); rate-model review rounds 1–2 run same day — round 1: F-W1 (candidate failed its own X-1 bound), F-W2 (`σ_L` reclassified as design lever; §5.3 joint-constraint costing, lever vs queue); round 2: F-W3 (no pre-measurement value derivable — the bound is a ~19× planning box; `DEFAULT_EXIT_GAP_WINDOW` converted to a `K_COVER`-pattern provisional sentinel with the decision rule frozen, sealed by the Phase 7.7 stressnet rate read per `RELEASE_CHECKLIST.md`), F-W4 (joint-percentile conservatism committed), F-W5 (exit-seam `N_t` re-derivation owed pre-seal); round 3: F-W6 (X-3 anchor-merge bound `W ≥ 2 × SEB` derived from the quantization lemma, folded into the frozen rule as a two-term `max` with its queue-predicate cost stated). `draw_exit_gap` **unblocked**, written against the sentinel (doc §5.4/§9/§11–§12). UPDATE 2026-07-16: `draw_exit_gap` **BUILT** against the sentinel (`shekyl-standoff/src/exit.rs` — `ExitGapWindow`/`ExitGap` newtypes, compile-refusal wiring, golden vector + seal tripwire, shared-trigger negative control) and the F-D6 anchor derivation landed (`release_cooldown_anchor_height` in `shekyl-archival-retention`, boundary-tested against the consensus predicate; staking-sim `20_000` doc fossil purged). R4's timing channel now awaits the F-W5 `N_t` re-derivation, then the X-1/X-2 sweep into the joint grade. UPDATE 2026-07-16 (same day): F-W5 resolved (F-D4 doc §13, `N_t(exit) = 10` derived) — the X-1/X-2 sweep is unblocked and is what remains before the R4 joint grade; the seal stays a Phase 7.7 obligation. UPDATE 2026-07-16 (same day): the **§8 sweep instrument built and run** (`shekyl-staking-sim --exit-standoff`; `EXIT_TARGET_ANON_SET` minted; frozen rule encoded as `frozen_rule_window` with planning-box KAT; instrument never landed on `dev` — archived at `archive/feat/fd4-exit-sweep-2026-07-16`) — pre-seal findings at F-D4 §14 (X-1 delivered cover latency-gated `ρ_x · σ_L`; required-`σ_L` surface now prices the §5.3 lever-vs-queue decision; X-2 trough rows reported failing per GF7 §5.1 as the pre-committed costing surface). R4's timing channel has its joint-grade input; **R4 close awaits the four-axis joint grade + F-D1/F-D2 arms**; the seal stays Phase 7.7. UPDATE 2026-07-16 (later same day): **F-D4 round 4 run and RATIFIED — the premise audit (F-W7/F-W8, FD4 §2.1/§15)**: the exit-timing channel's correlated observable `T` is unpopulated within the seam (refund structural, rotation policy+economics, network conditioning, off-chain crossing re-homed); X-3's harm model retracted; **deletion-with-tripwire ratified** (no seal — the checklist entry is removed; the sentinel never shipped a value). R4's close condition re-forms: the FD4 §15.5 **hand-forward** (Gate-6 §12.8 — seam re-homing to §18.13 + the joint grade's axis set without its on-chain timing axis) now precedes the grade; F-D1/F-D2 arms pend the same answer. Method posture adopted: observer-as-code, pre-registered (Gate-6 §11.8 method note 2). UPDATE 2026-07-16 (post-ratification): second method posture adopted — **mechanism-vs-economics measurement filter** (FD4 §15.8 + Gate-6 §11.8 method note 3 + `RELEASE_CHECKLIST.md` queueing rule): two of the three FD4 seal inputs were economics numbers a testnet cannot produce in principle (true before F-W7); every future stressnet-seal candidate classifies first; PF-9 queue audited — `K_COVER` passes (mechanism), GF-7's cover-level residual caveated at Gate-6 §12.8. UPDATE 2026-07-16 (same day): the GF-7 caveat's follow-through — the effective-cover *level* reclassified as the **fifth WI-4 §13.5 standing conditional** (conditions the number itself, not a channel; the `1.86` is stacked marginals — pessimistic adversary panel × optimistic nominal cover), and its shape measured via the new `shekyl-staking-sim --gf7-breakeven` sensitivity arm (`gf7_breakeven.rs`, mechanism-class): worst-arm `r` clears the bound at every row of `N ∈ [2, 16]`, never trending toward the bar as cover thins ⇒ **`r < 2` is structurally blind to cover** (cover was never gated by it; the ~7% margin is relative-leak margin, not cover margin) and **no per-event monitoring threshold exists** — the candidate `P(link) ≤ 0.2` is the nominal-cover assumption restated by identity (parity `N ≈ nominal · r/bound` forced by flat `r`), refused as an absolute bar; the conditional's instrument is the unbuilt R5 S-2 lifetime exposure ledger (WI-4 §13.5 pins the register as the aspiration's ledger and records S-2's permanent deferral as itself a finding). UPDATE 2026-07-16 (evening — F-W9, FD4 §16): §13.3's repetition premise corrected before R4 takes the §15.5 hand-forward — `m` is a bounded lifecycle count (1 mandatory observable crossing = `JoinMarket`; 2 wallet-default-closeable optional classes; 0 observable exits; rejoin unmodeled), so the on-chain composition a re-homed grade would inherit is finite and GF-7-shaped (at `m = 1` the per-event grade *is* the on-chain lifetime grade, per observer class, at nominal cover); cross-persona `q^k` raised and withdrawn (no linking key — FD4 §16.3); the §16.4 GF-4b-filter wallet-default proposal rides the hand-forward with its growth-gating cost stated; third method posture adopted — **enumerate the events that compose** (Gate-6 §11.8 method note 4). UPDATE 2026-07-16 (night — **R4 DECISION ROUND run, Gate-6 §12.9**): the §15.5 hand-forward **answered** — (1) GF-4's exit seam **re-homes** to the principal↔user crossing (WI-4 §18.13; both halves reduce — timing phantom, amount already class-3; F-D1/F-D2 re-labeled as that seam's mistake-set closure, build/arms unchanged; the on-chain `P`↔principal surface is now the entry seam alone); (2) the four-axis joint grade **dissolves by axis attrition** — CB-3's obligation discharged (one live axis left), **GF-10 grades standalone** against its §11.5 pre-committed advantage claim (R3's close re-formed accordingly); (3) F-D4 §16.4 funding default **ACCEPTED** as F-D2-class (self-fund default via GF-4b lineage; external growth-funding = loud override routed through the entry standoff); (4) harness fork resolved **deleted** (rate model over counterparty arrivals fails method-note-3; never landed, not resurrected) and the mechanism-**deletion PR scoped** (`exit.rs` wholesale; F-D6 + `release_cooldown_elapsed` out of scope). R4's re-formed close: F-D1 + F-D2 (incl. funding default) + deletion PR + F-D5 §14.4 disposition; S-2 is R5's opening item with F-W9's finite domain. UPDATE 2026-07-16 (night — **§12.9 RATIFIED at review-at-source**): decisions 1, 4, 5 as drafted; the dissolution pair (2–3) **amended and ratified** — the draft's axis walk had silently omitted output-count, and ratification added it as the fourth attrition row (**F-W10**: phantom — the drain is not an identifiable transaction under FCMP++, so its output count is not an observable; Gate-6 §2.4's lump-sweep discipline retired as a CryptoNote-lineage carry never re-walked across the crypto change; §11.8 method note 5 adopted: *"carry from X" is a re-walk trigger, not an exemption*). The dissolution stands strengthened; the round is closed as ratified. UPDATE 2026-07-17: the decision-5 **mechanism-deletion PR landed** (`exit.rs` wholesale + features + acknowledgment lines + conformance/test arms; F-D6 anchor re-anchored, out of scope) — R4's re-formed close condition (iii) is satisfied; what remains of the close is F-D1 + F-D2 (incl. the funding default) + the F-D5 §14.4 disposition. UPDATE 2026-07-17 (later): **close condition (iv)'s "§14.4" was a dangling pointer** (WI-4 §14.4 = the closed partition arm; the phrase conflated WI-4's discharged strata/lifetime riders with its quantization GF-4-candidate routing) — re-worded by dated amendment (Gate-6 §12.9): the **F-D5 disposition round** (charter at Gate-6 §12.7) runs a structural-derivation attempt first (grid width from reward-curve structure, mechanism-class; fresh measurement round refused per method note 3), names the genesis-frozen consequence (no derivation ⇒ no grid at genesis), reconciles the §10.12 pass-4 banding rejection, and registers the lifetime-aggregate band as an R5 S-2 ledger row either way. UPDATE 2026-07-17 (same day): **the F-D5 disposition round RAN — width does not derive, NO GRID at genesis; condition (iv) DISCHARGED** (Gate-6 §12.7 OUTCOME: harm survives F-D1, derivation fails at three population entry points — `r_market` in `scarcity_milli`, the `budget(E)/Σwork(E)` spacing, reachable-collision ≠ delivered cover; cost side derivable, benefit side not). **R4 is open on F-D1 + F-D2 only**; the band is the first registered R5 S-2 ledger row | |
| **F-N** | Findings within a specific design doc's review (e.g. F40 `AlreadyInChain`, F41 `Conceal` constant-work in the daemon-submit series) | The doc that numbered them, e.g. [`DAEMON_SUBMIT_VERDICT.md`](DAEMON_SUBMIT_VERDICT.md) | F-numbers are doc-scoped, not global | |
| **DQ-N** | Design questions in the 2d-2 transport plan | [`ARCHIVAL_BOND_2D2_TRANSPORT_PLAN.md`](ARCHIVAL_BOND_2D2_TRANSPORT_PLAN.md) | — | |
| **SCE-N** | Serve-credit C++-decision equivalence-audit findings (D-SC-A/B/C mirror + standing KAT; SCE-1 = the `(P,s,E)` key BE/native-endian encoding split) | [`ARCHIVAL_SERVE_CREDIT_EQUIVALENCE_AUDIT.md`](ARCHIVAL_SERVE_CREDIT_EQUIVALENCE_AUDIT.md) | Registered 2026-07-09 at birth (rule 94 §1); prefix `SCE-` passes the uniqueness check against Phase/Stage/M/Bond-PR/SP/SP-T/PR-A/PR-B/PR-E/CT/GF/S/R/F/DQ/WI/M1 | |
| **WI-1…WI-4** | Post-#255 archival-bond work items: 1 `start_pscan` lifecycle wiring, 2 production bond assembly + pending-post persistence, 3 block-timed dispatch driver, 4 GF-7 measurement round | This index §4 (rows) + the carrying design docs per row | Registered 2026-07-05; prefix `WI` passes the rule-94 uniqueness check against Phase/Stage/M/Bond-PR/SP/SP-T/PR-A/PR-B/PR-E/CT/GF/S/F/DQ. **Ephemeral family (rule 21 retire criterion):** one plan's work breakdown over already-durable §5 gaps, not a durable cross-doc family — the row retires when WI-4 lands (its items live on as the §5 gap closures and per-doc rows they map to) | W2 Pending |
| **MS-1…MS-8** | V3.1 multisig **Rust engine integration** Round 1 design questions (trait identity, C++/Rust boundary, feature gate, receive/spend/transport, persistence; **MS-8 RETIRED** 2026-07-14 → MSW-2) | [`V3_1_MULTISIG_RUST_ENGINE.md`](V3_1_MULTISIG_RUST_ENGINE.md) | Registered 2026-07-14 (rule 94 §1); prefix `MS`. Track B only. | **Round 1 CLOSED 2026-07-16.** All blockers discharged: F-3 DELETE (Option A fossil) + F-6 CI (`--features multisig` green) in **#310** (merged 2026-07-15); **F-7** (`EngineSignerKind::SigningCeremony`, `SoloSigner = Infallible`) in **#317**. Track B (MS-1…MS-7) → Round 2 / MS-5 (Option E′) on explicit go-ahead. Pre-MS-5 gate no CI can turn green: the `MultisigSigner` `N`/`K` decision. |
| **DS-1…DS-7** | F-D2 drain-send subsystem design questions (assembly locus, GUI data source, destination pin, fee/change mechanics, UI default, F-D4 §16.4 funding-default rider, shape-era doc sweep) — Gate-6 R4's last open item (former `K_COVER` seal seat removed 2026-07-19, M1 §4/F-W10) | [`ARCHIVAL_DRAIN_SEND_FD2.md`](ARCHIVAL_DRAIN_SEND_FD2.md) | Registered 2026-07-19 at birth (rule 94 §1); prefix `DS-` passes the uniqueness check against the registered families (no family's tokens parse as `DS<digits>`; distinct from `DQ-`) | Round 1 DRAFT (2026-07-19) — scoping opened, review pending |
| **MSW-1…MSW-8** (+ **MSW-G=5**; product **Option E′**) | V3.0 wire: MSW-6 scheme_id; MSW-G=5; MSW-1…5; **MSW-8** delete vestigial address `hybrid_sign_pubkeys`. MSW-7 retracted. Product **E′** (§0.5). | [`V3_1_MULTISIG_RUST_ENGINE.md`](V3_1_MULTISIG_RUST_ENGINE.md) §0.3–§0.5 | **MSW-1/2/3/4/5/8 code LANDED 2026-07-16** (branch `feat/msw-wire-max5`, atop MSW-6): `MAX=5` across crypto-pq/wire/address/C++, F-1 fossil `2 + N·LEN` deleted at all four sites, crypto-pq↔address caps pinned at compile time, cross-language KAT `fcmp.cpp::msw1_pqc_constants_match_rust` green (F-1 catcher); MSW-3 was cleared by MSW-6. Per-item closeouts in the wire-work table. Product **E′** = Track B (pending). | MSW-1/2/3/4/5/6/8 landed; E′ (Track B) pending |

---

## 3. Phase map (engine layer, `WALLET_REWRITE_PLAN.md`)

| Phase | Scope | Status (per plan doc + code, 2026-07-05) |
| --- | --- | --- |
| 0 | Close out previous plan; align baseline | Closed |
| 1 | Wallet domain model (`shekyl-engine-core::Engine`) | Closed 2026-06-10 (`docs/completed/PHASE_1_ORCHESTRATOR_STATUS.md`) |
| 2a | Send path | Done for phase scope (2026-07-01) — curve-tree client wired into production sends; residue in `PHASE_2A_SEND_PATH.md` §10 |
| 2b | Stake lifecycle | Design rounds 0–4 closed / largely closed; implementation = Stage 3, **partially landed** (see §5) |
| 2c | Addresses / proofs | Per plan doc |
| 2d | Air-gapped flow (`UnsignedTxBundle`/`SignedTxBundle`) | Per plan doc — **not** the bond chain's 2d-1/2d-2 |
| 3 | `shekyl-cli` binary | Per plan doc |
| 4 | `shekyl-wallet-rpc` binary | **Phase 4b SPECIFIED methods landed except `rescan_blockchain` (as of 2026-07-09)** — lifecycle + read queries + `refresh` + send lifecycle (`build`/`submit`/`discard_pending_tx`) on `Engine::<SoloSigner>`; `rescan_blockchain` stays `-32601` pending Engine rescan API (`FOLLOWUPS.md`). Distinct from transitional `shekyl-engine-rpc` (wallet2 FFI). UPDATE 2026-07-09: Phase 4b send + refresh + reads (verified: `cargo test -p shekyl-wallet-rpc`; `git grep build_pending_tx` → `shekyl-wallet-rpc/src/send.rs`). UPDATE 2026-07-09: Phase 4b lifecycle. UPDATE 2026-07-09: Phase 4a scaffold. UPDATE 2026-07-06: OpenAPI contract landed #265 (decision-anchored) |
| 5 | C++ deletion (single commit) | Not started; gated on 1–4 completeness |
| 6 | Tests and docs | Per plan doc |

---

## 4. Bond-construction chain (Bond-PR track)

History (all landed): Bond-PR 0 `archival_p` derivation #152 → 1 bond builder +
`shekyl-rct-balance` #155 → 2a synthetic-tree KAT #156 → 2b StakeEngine
actor #157 (partly superseded by Model D) → 2c-1 real-tree KAT #158 → 2c-2a
Model D wiring (`feat/archival-stake-wiring`) → 2c-2b request path #163.

Current front:

| Item | Plan doc | Status (code-verified 2026-07-05) |
| --- | --- | --- |
| GF-7 scheduler seam (2c-2b re-scope) | [`ARCHIVAL_BOND_2C_GF7_HOOKS.md`](ARCHIVAL_BOND_2C_GF7_HOOKS.md) | **Landed (as of 2026-07-05)** — PR #255 merged (`6d5ecac5f`): `plan_entry_seam`/`EntrySeamPlan`, `BroadcastTimelineObserver` seam behind `gf7-hooks`, `--gf7-timeline` sim scenario, `gf7-no-emit-guard` workflow. UPDATE 2026-07-05: landed #255 (verified: git grep `plan_entry_seam`/`BroadcastTimelineObserver` on dev → `shekyl-staking-sim/src/gf7_timeline.rs`, `shekyl-standoff::gf7`; `.github/workflows/gf7-no-emit-guard.yml`) |
| 2d-1 `P`-scan layer | [`ARCHIVAL_BOND_2D1_PSCAN_PLAN.md`](ARCHIVAL_BOND_2D1_PSCAN_PLAN.md) | **Wired (WI-1, 2026-07-18)** — SP-0…SP-7 landed via #192/#194/#195 and PR-A #201 / PR-B #205; the embedder call site now lives in `shekyl-wallet-rpc`'s wallet lifecycle (`start_pscan_if_staker` on staker open, `PScanHandle::shutdown` on close). Read-side only; the bond-post/assemble path the dispatch tick drives stays compile-inert until SP-R0 mints the `SpentRecordsDurablyPruned` witness |
| 2d-2 `P`-isolated transport | [`ARCHIVAL_BOND_2D2_TRANSPORT_PLAN.md`](ARCHIVAL_BOND_2D2_TRANSPORT_PLAN.md) | **Partially built, inert** — SP-T1 circuit isolation #204/#209; SP-T4a `BroadcastSubmitter` #254; `shekyl-p-transport` (`PTorClient`) and `PBlockSource` on `dev` but `dead_code`; block-timed dispatch driver not built (`TODO(2d)` in `stake_engine.rs`) |
| WI-1 `start_pscan` lifecycle wiring | [`ARCHIVAL_BOND_2D1_PSCAN_PLAN.md`](ARCHIVAL_BOND_2D1_PSCAN_PLAN.md) (SP-5 completion, §"WI-1" note) | **Landed (2026-07-18)** — closes the §5 "Built, unwired" `P`-scan gap. The `PScanHandle` is **embedder-held**, so the production caller is `shekyl-wallet-rpc`'s wallet lifecycle, not the `engine` crate: `create`/`open_wallet` auto-start `Engine::start_pscan_if_staker` (a staker parks the handle in `Tenant`; a non-staker parks `None`), and `close_wallet` calls `PScanHandle::shutdown().await` **before** `Arc::try_unwrap` (the task holds its own clone of the engine arc; a live handle would fail the close "still in use", so the prior "close-stops-task by ownership"/Drop framing was insufficient). Closure is a **behavioral gate, not the `dead_code` lint** (a `pub` lib item cannot trip it — the lint was disarmed by `pub`, not satisfied by wiring): the `staker_open_parks_a_pscan_handle_and_close_shuts_it_down` lifecycle test fails if either the start or the shutdown call site is removed. Enabler: engine-core `__test_helpers::make_staker_for_test` (feature-gated) — the first `test-helpers` downstream consumer, the rule-21 re-export reopen the feature reserved. Seal-path note: WI-1 is **not** a `K_COVER`-seal input — the WI-4 §19.1 sealing form pins "live dispatch driver" to the production task+dispatch code path, harness-spawnable without the embedder wiring (§19.1 disposition note, 2026-07-18) |
| SP-R0 reconcile GC — arms #1/#2/#3 | [`ARCHIVAL_BOND_SP_R0_PLAN.md`](ARCHIVAL_BOND_SP_R0_PLAN.md) (Round-0 scoping: consolidation, locked decisions D-1…D-3, DQ records) | **Round 0 ratified (2026-07-18) — DQ-A…F; arm #1 BUILT + logic-discharged (2026-07-18, `feat/sp-r0-arm1-funding-prune`); arm #3 BUILT + logic-discharged (2026-07-18, `feat/stake-activation-entry` — open-time phantom GC, SA-DQ-3 co-designed crash fixture, pending-record-bridge wrongful-GC argument); arm #2 BUILT + logic-discharged at the task/accrual/lifecycle test level (2026-07-18, `feat/sp-r0-arm2-retire-gc` — done-side `RetiredPersonaRecord` ledger, `PSCAN_STATE_VERSION` 6→7 + snapshot, atomic retire-time prune under the DQ-D sweep-corroborated tip clamp, open-time records-driven hint clean; the ~270k-block claim-window reachability makes a not(test) lane impractical — disclosed deviation, ratification pending). Production-discharge: **the PR-4b gate CLEARED 2026-07-19 (#338)** — the regtest lane now enters through the production `first_stake` and **arm #1 is PRODUCTION-DISCHARGED** there (SP_R0_PLAN §4 UPDATE); arm #3 production-discharge leg **LANDED 2026-07-19** (`e2e_arm3_phantom_slot_collected_at_open` — the SA-DQ-3 persist-then-no-broadcast crash against the live chain, collected by the production open; SHEKYLD_BIN cadence like the arm-#1 lane); arm #2 production-discharge **corrected: NOT drivable** — the `Unbond` trigger has block-path verifies but a JoinMarket-only submit battery (§8.7.1) and no wallet constructor; named blockers = the non-JM submit battery (PR-4b sibling) + the wallet unbond entry. *(Prior clause "blocked on PR-4b" swept 2026-07-19 by the merging PR per the gate-decay method note.)* — per Guard 2: arm #1's status is **logic-discharged** — the CI fire lane (`shekyl-engine-core/tests/sp_r0_arm1_fire.rs`; harness `feature = "test-helpers"` + `not(test)`, Guard 1 compile-time) drives the production watch/prune/witness path and asserts both prune paths fire (cross-step + in-step, `spent_pruned_total` non-zero) with the production-witness sweep refusing post-prune; **production-firing gated on #332**. The sole production `SpentRecordsDurablyPruned` constructor (`arm1_watch_pruning_live`) is minted; the GF4b-5 structural gate is discharged (tripwire now asserts exactly-one). Three arms: **#1** spent-`funding_outputs` prune via key-image watch at ingest (D-2, no `PSCAN_STATE_VERSION` bump; mints the sole production `SpentRecordsDurablyPruned` constructor per D-1 disposition (a); no canonicity token — 720-deep ingest ceiling inherited), **#2** phantom `bonded_slots`/`p_slot` GC over the done-side ledger (consumes SP-6 `PReconcileSet`; canonicity token as corroboration, DQ-D), **#3** wallet-local full-scan phantom drop (domain includes activation-time crashes, DQ-C sub-question). All arms **production-inert until the staker-activation round lands** (§0 F-1); per the **DQ-F split** (precisified 2026-07-18): arms may land **logic-discharged** in parallel with #332 — a CI fire harness drives the production code path (sole stand-in: `make_staker_for_test` at the top; Guard 1: no `for_test()` on the path under test) and CI asserts the GC fired — while **production-firing** stays gated on #332; every status line states which discharge (Guard 2 — a bare "discharged" is forbidden). DQ-C order (activation front → #1 → #3 → #2) binds the production discharge, not the landing. DQ-A containment pinned: the key-image watch-set is `≤ view_sk`, transient, same-process — frozen structurally as redacting `Debug` + no `Serialize` from the first commit |
| Production staker-activation path (genesis front) | [`ARCHIVAL_STAKE_ACTIVATION_PLAN.md`](ARCHIVAL_STAKE_ACTIVATION_PLAN.md) (Round-0 scoping — PR #332, `docs/stake-activation-round0`) | **Round 0 + Round 1 CLOSED (ratified §5.7); FIRST IMPLEMENTATION LANDED (2026-07-18, `feat/stake-activation-entry`)** — wallet-rpc `stake` entry (Full-gated, `-29500..-29502` refusal taxonomy), SA-R1-a intent open (`open_full_with_first_stake_intent`, transient-by-construction with an executable abort-clean pin test), `Engine::first_stake` continuation (W1 preflight sweep → re-entrant persist → assemble → pending seal; SA-DQ-5 no-broadcast structural), SA-R1-c count gate (warn-on-multi-input at assemble), rule-21 dead-code half (b) retired on all five witness consumers. Guard-2: SP-R0 arm #1 stays **logic-discharged** — production-discharge needs the DQ-F CI lane driven through this entry (drivable now, not yet driven). Prior state: **SA-DQ-1…SA-DQ-5** — the missing front the whole archival-`P` constellation gates on (SP-R0 §0 F-1): nothing in production sets `staking_enabled` — `persist_bond_record` is the sole setter with zero production callers, and no stake RPC exists in `shekyl-wallet-rpc`, so scan (WI-1), drain (F-D1), assemble, and the SP-R0 GCs are all built-unwired behind this one path. Owns the first-stake chicken-and-egg with the source-anchored break (**first-stake collapses onto reopen**: derive from the transient seed outside the `staking_enabled` gate, sign, `persist_bond_record` flips flag + `bonded_slots` atomically, reopen spawns the engine for real — `stake_engine.rs:26-28`) and carries **two named genesis gates**: SA-DQ-4 (owns GF4b-2 — the first bond's funding-input-**count** discipline; funding-**source** stays deferred) and SA-DQ-5 (GF-7 — first-stake is the worst-case principal↔`P` linkage; the broadcast-before/after-reopen fork), plus SA-DQ-1 (restricted RPC method list + idempotency), SA-DQ-2 (bootstrap derive-**equivalence** — stake-under-A/scan-under-B loses the bond), SA-DQ-3 (the durable phantom window is persist↔broadcast-confirmation, co-designed with SP-R0 arm #3's DQ-F fixture). Landing retires the rule-21 dead-code half **(b) RPC stake entry** (`stake_engine.rs:1366` et al.); SP-R0's arms sequence behind it |
| WI-2 production bond assembly + pending-post persistence | [`ARCHIVAL_BOND_WI2_ASSEMBLY.md`](ARCHIVAL_BOND_WI2_ASSEMBLY.md) (the design addendum) + [`ARCHIVAL_BOND_2D2_SP_T4_BROADCAST.md`](ARCHIVAL_BOND_2D2_SP_T4_BROADCAST.md) (P-1/P-2 pins) | **Orchestrator wired (as of 2026-07-11)** — Engine-side `assemble_bond_post` (`bond_orchestrator.rs`) + `StakeEngineHandle::assemble_bond`; independent `PendingPostStore` over cloned `pending_write_lock` (F-1; gate-11 green, no allowlist edit); named `daemon_claimed_tip` for `anchor_t0` (F-2); `ReferenceBlock` via existing anchoring procedure (F-6); domain newtypes + `PSCAN_STATE_VERSION` 6 / `PENDING_POST_VERSION` 3. **Go-live still gated** on 2d-1 / SP-R0 `SpentRecordsDurablyPruned` production mint **and** RPC stake entry (sibling dead_code allows; rule-21). UPDATE 2026-07-11: verified `git grep assemble_bond_post` → `bond_orchestrator.rs`. Prior: design addendum D-A1..D-A5 (2026-07-05). |
| WI-3 block-timed dispatch driver | [`ARCHIVAL_BOND_WI3_DISPATCH.md`](ARCHIVAL_BOND_WI3_DISPATCH.md) (design rounds 1–2 + implementation status) + [`ARCHIVAL_BOND_2D2_TRANSPORT_PLAN.md`](ARCHIVAL_BOND_2D2_TRANSPORT_PLAN.md) (SP-T4b/T5 seam) | **Implemented — in review (2026-07-05, `feat/wi3-dispatch-driver`)** — gates 1–11 landed: driver + locked seal path (`pscan/dispatch.rs`), `PENDING_POST_VERSION` 2 (v1 fails closed), `DispatchTick` sweep wiring + production `WalletFile` store / `Local`-posture broadcast (`pscan/task.rs` / `pscan/start.rs`), per-submit `BondPostDispatched` emission-completeness test, gate-11 single-write-path grep gate (`scripts/ci/check_pending_post_write_path.sh`). Closes the §5 "Block-timed dispatch driver" gap; **GF-7 acceptance stays open** per the reconvergence gate below. Design rounds 1–2 (closed 2026-07-05): dispositions D-B1..D-B7 (D1 one-per-tick + dispersal draw, seal-before-send with `PENDING_POST_VERSION` 2, observed-not-claimed confirmation, late-is-noise/stale-is-terminal/re-anchor-is-re-assemble, live `BondPostDispatched`); round 2 (doc §7) pinned the clock's daemon trust as a **named invariant, sound under the recommended local-daemon posture** (R2-1: raw tip kept; the sweep-corroborated clamp `min(claimed_tip, verified_frontier + reorg_depth)` is pre-designed and held in reserve for the 2d-2 remote/untrusted-daemon reopen shared with D-B6) and pinned single-blob crash-atomicity / derived-reservation against the actual AEAD-envelope + atomic-rename seal path (R2-2/R2-4 — the round's load-bearing, posture-independent item). Closes the §5 "Block-timed dispatch driver: Missing" gap (`TODO(2d)` in `stake_engine.rs`) when the implementation lands; consumes WI-2's sealed pending posts. **Reconvergence gate (rule 21):** WI-3 may land in parallel with WI-4, but its GF-7 acceptance does **not close** until WI-4's threshold artifact exists — the D1 coordination is only validatable against a defined `P(link \| T_obs)` bound (no armed-gate-without-trigger). **D1 invariant (design-round scope):** the dispatch driver must not co-launch correlated events off a shared sweep tick — the entry-gap draw decorrelates bond-post-vs-funding *ordering* only, not the principal's lifecycle timeline (`stake_engine.rs` `SignBond` handler note), so D1 requires independent submitters/circuits **and** independently-anchored dispatch times (discharged in `ARCHIVAL_BOND_WI3_DISPATCH.md` §3.2); the adversarial check (submission-timestamp observer gains no better-than-prior linkage) is exactly what WI-4 measures |
| WI-4 GF-7 measurement round | [`ARCHIVAL_BOND_WI4_MEASUREMENT.md`](ARCHIVAL_BOND_WI4_MEASUREMENT.md) (a-priori threshold + correlator spec + §13 result) + [`ARCHIVAL_BOND_2C_GF7_HOOKS.md`](ARCHIVAL_BOND_2C_GF7_HOOKS.md) §5.1 (the binding constraints) | **Graded — PROVISIONAL-PASS, local-daemon posture only (2026-07-05; re-graded 2026-07-06 post-adversarial-review, `feat/wi4-gf7-measurement`)** — the §11 round landed the posture-conditioned S-3 correlator (§4.1.1: session/refresh markers excluded as direct anchors is a *posture theorem* — loopback under the local-daemon posture, D-B1 2d-2 reopen family; the **remote/non-isolated-daemon posture is a named unmet residual on the verdict**, expected re-grade shape `r ≈ 5`), the funding-seam-blind null, the oracle-panel stress arm (§4.3.1: MAP + density-corrected + exact seam-consistency-gated; `lr > s3` on every gate row), the D-B3 `resume` sweep group (session-lattice resubmit exposure, `r = 1.57`, under bound), both validity controls, and the pre-committed sweep in `shekyl-staking-sim/src/gf7_timeline.rs`. The indirect session channel is coupled in the generator and graded (blind `0.237` at window 0 → `≈0.13` under full jitter — not a refresh-free world). Controls valid; every gate-relevant row clears `r = P(link)·N < 2` on all three arms; worst gate row `r = 1.86` (panel; honestly thin margin). Threshold provenance settled a-priori (§13.4: spec commit predates correlator code; `r < 1` pre-rejected as grading the conceded floor). **Cold-start reclassified (§13.2):** low-activity **fails** (panel `r = 3.54`) and cold-start is the genesis regime ⇒ disposition is the **§14 founder-cover launch posture** (refuse the regime: production-path-identical staggered founder cover with permanent consented attributability, shard-schedule structural gate, and the §14.4 **partition-adversary arm** — a-priori bounds committed; review closed 2026-07-10 with §§17.1–17.6 ratified (R4 distinct-position round, §17.6): **implementation round released** at seven family members / five witness-typed controls, with the a-priori gating lemma as its first deliverable and two pinned premises (floor persistence — later withdrawn at R7; count `K`-independence) carrying reopen criteria to Gate 7; R5 (§17.7, same day) adjudicated the landing itself — tier channel promoted to named-structural with per-channel strip floor, snapshot-by-construction floor + a-priori thinning margin, premise-2 OR struck, witness type compile-time co-first-deliverable; R6 (§17.8, same day) adjudicated the R5 landing — cumulative trigger vs live property (`frozen_segment_count` has no bond-exit decrement path, verified at source; derive-don't-cache deliberate per M2), per-channel reachability with descriptor profile-commonality check (fingerprint-not-zero-cover routes to redesign), "fixed pre-gate schedule" pinned to {count, completion-before-gate}; R7 (§17.9, 2026-07-11) reopened R6's landing and **re-closed §17 load-bearing** — the R6 maintenance commitment was a ratified founder-differential mechanism born of a category error (Foundation-backstop properties applied to anonymous founder-users; §14-head terminology pin lands three referents / three tokens), withdrawn on two independent grounds (categorical: founders are users, no continuity owed; fails-in-its-own-design-case: masking is thinnest exactly where the floor is load-bearing), the claimed exit-correlated channel refuted at source (epoch quantization + cooldown decorrelation headroom + no repost mechanism), finding 1 re-homed to population-level churn calibration with the margin derived against **adversarial max net attrition over the full pre-gate window**, Q3-B re-seated on the same basis. **Final Gate-7 gates are §17.9's**: adversarial-max-attrition margin + per-channel reachability; §17 reopens only against the termination bar — a construction defeating both the categorical rule and the substrate's decorrelation). Block-unit `dispersal>0` is a coarse-tick counterfactual (real dispersal is sub-block ⇒ the emitted `at` is the due block, so the realistic gate sits at `dispersal=0`). **Reconvergence leg (b), severity sharpened:** the block-time sealing re-run is confirmatory by construction (live `BondPostDispatched.at` = due block, per the `gf7_emits_bond_post_dispatched_per_submit` engine test) ⇒ **no block-resolution re-run can close leg (b)**; the wall-clock sweep-phase channel — the one dispersal defends — is the **primary open uncertainty**; closing requires sub-block wall-clock emission + re-grade (FOLLOWUPS, pre-genesis). **Binding constraints (GF7_HOOKS §5.1, pinned 2026-07-05):** (1) threshold derived a priori from a stated adversary-advantage claim and committed in the acceptance doc **before** any grading runs — a failed sweep is a decorrelation-redesign signal, never a move-the-bar signal; (2) correlator adversarially specced: joint-axis fusion (never per-axis-multiplied) + a stronger-than-S-3 stress arm alongside the funding-seam-blind null; (3) known-linked / known-independent instrumentation controls gate the run's own validity; (4) pessimistic principal-lifecycle distributions, stated; (5) the pre-WI-3 pass is **provisional** — sealing measurement re-runs against WI-3's live `BondPostDispatched` emission. **WI-3 R2-3 obligation:** the synthesis must model WI-3's intra-tick dispersal draw and sweep its *distribution* as a parameter — a pre-live "dispersal range suffices" conclusion drawn from a dispersal-free timeline is invalid (`ARCHIVAL_BOND_WI3_DISPATCH.md` §5/§7). WI-3's GF-7 acceptance gates on this artifact (see WI-3 row). **R3 scope pin (§18, 2026-07-06):** the `1.86` is a bound on one seam (entry; exit/drain is GF-4, unmeasured), one scope (per-post; promotes to per-principal **only where GF-4 holds** — conditional now on the verdict line §13.1/§13.5), one channel (timing; the reward value channel is loud by design, verified at source), one instant (not lifetime-cumulative), one stratum (unstratified by `holdings`/`bond_floor`); GF-4 exit seam + value channel are co-equal sealing-path rounds; strata + lifetime ride the §14.4 round. **§18.7 addendum:** tiers-vs-continuous answered at source — accrual reads shard age not persona duration (`g(age)` linear), duration structurally epoch-quantized; four pins (curve shape, epoch width a-priori-derived, claim-timing seam, correlated-exit wargame) routed to the GF-4/§14.4 rounds. **§18.8 correction:** P-is-public reframing — only the `P`→user link is protected; §18.3 stratification narrowed to bridge-axis screening, §18.7 curve pin downgraded to economic-with-bridge-check; entry amount bridge verified **closed structurally** (bond amount consensus-forced to `bond_floor(holdings)`); surviving V-2a membership prior + V-2b exit lifetime-total match routed to GF-4. **§18.9 mechanism pin:** the bond term is loud-but-constant (transparent `amount·H` in the CT balance, `bond_ct_balance.rs`), not CT-hidden-above-floor; `commitment_mask` is HKDF-derived, never user-chosen — the mask-consistency wargame is unrepresentable-by-construction (reopen on any user-supplied blinding factor / above-floor bonding); on-chain bridges timing-only, V-2a/V-2b survive off-chain. **§18.10 R4 exit-seam inventory:** reward payout verified cleartext-by-disposition + exact (`REWARD_EMISSION_LEG.md` §5.5/§9; `reward_P(E)` ex-ante computable from public state) — R-1 dominant but severed at first spend by FCMP++/CT, all bits off-chain (V-2b sharpest form); CT-committing the payout foreclosed (priority-1 inflation audit); open defenses quantization (a-priori grid width, GF-4 candidate) + gate-6 routing; claim-cadence/amount/stratum graded jointly on GF-4. **§18.11 R-1 disposition:** FCMP++ hides amount AND linkage on-chain (no non-mint cleartext amounts; `P`'s spend set unenumerable) — R-1 a managed residual at the guaranteed off-chain boundary; layers: routing (primary, complete on-chain) > new gate-6 drain-amount-decoupling pin (exact-subsum match is wallet-policy, not structural) > quantization (defense-in-depth vs the approximate lifetime-aggregate band). **§18.12 R4 closure:** drain pin tightened to input-level (drain-amount computation must not read the reward sequence; acceptance test + source verification ride the gate-6 policy PR); why-loud recorded (priority-1 inflation audit — do not CT-commit the mint); inventory closed to its floor (fee-widened lifetime-aggregate band at the definitional off-chain crossing). **§18.13 arc closure:** theory ceiling; Tor-default trace resolved (persona side mandatory-by-construction, principal side opt-in — default flip is a named V3.0 FOLLOWUPS item); drain-inputs trace rides gate-6. UPDATE 2026-07-11: §14.4 partition-adversary arm implemented (`feat/sim-partition-adversary`; verified: `partition_adversary.rs` + `riders.rs` in `shekyl-staking-sim`) — co-first deliverables landed before grading (gating lemma `3.5393e-5` + compile-time witness-typed controls); family members 1–7, controls M-a…M-e, deployed bound 1 holds, whole-set controls bite; bound-2 under-specification for single-member-targeted controls (M-b/M-d) **pinned in §14.4** with disposition options — arm reports INVALID-with-pins pending that decision. Riders landed same slice: N-sweep (`r < 2` at `N ∈ {10,20,50}`), M6.2 coupling control (passes, `0.231 ≥ 2/N`), attribute-bridge grading (deployed at null, positive control bites), lifetime-accumulation sweep (52-event founder anchor). UPDATE 2026-07-11 (same day, post-review): the bound-2 pin **RESOLVED as a detector-architecture finding**, not a disposition wording choice — the T-site fact pulled at source per the review precondition (verified: `family_max` in `partition_adversary.rs` is a raw per-member max, no consensus, but its permutation null re-maximizes the family per relabeling, ≈ 0.66 at `N=10`, so lone member-6/7 Q2-A/Q2-B signals were structurally invisible and a `T`-only bound 1 potentially false-clean); fix armed the trigger rather than moving a bar: **any-member arm** (per member vs its own null-of-the-mean, exact rank p, Bonferroni `α/7` at a-priori `α = 0.05`, `perms ≥ 139` resolution guard, `trials = 500` power-sized a-priori to the M-b hypergeometric effect ceiling); deployed bound 1 requires both arms, whole-set controls retain family-max `+0.30` + aimed flag, single-member controls grade on the aimed flag; re-graded 500×200 — deployed clean on both arms (lift `+0.001`, min member p `0.264`), all five controls bite (M-b m2 / M-d m6 flag at the `0.005` p-floor, M-d members 1–5 at chance): **PARTITION-PASS, no pins** (§14.4 UPDATE block is the record; reopen on family-membership / `N`-`M` geometry change or persistent non-aimed flags). RATIFIED 2026-07-11 (decision-anchored: PR #291, incl. review-round dispositions — projection-form spectral deflation with bit-for-bit-identical re-grade, loud geometry guards; §14.4 RATIFIED block). The arm's verdict is now a gradeable `K_COVER`-sealing input; remaining seal inputs are leg-(b) sub-block wall-clock emission, the GF-4 exit-seam/value-channel rounds, and the §17.9 Gate-7 calibration (adversarial-max-attrition margin + per-channel thickness-reachability). UPDATE 2026-07-11 (leg-(b) harness-timestamp form graded, `fix/sim-wallclock-leg-b`; verified: `wallclock_leg.rs` in `shekyl-staking-sim`): the §13.3 closing requirement converted to committed spec §19 (a-priori bound `r = P(link)·(N−1) < 2`, doc commit ahead of code per §3.5) and graded in-model — production dispersal (`U[0,T)`, `T = 60 s`) at `r ∈ [0.84, 1.32]` over `m ∈ {1,4,12,52}` × `N ∈ {10,20,50}`, sub-`T` cliff measured (`T/4` fails at every `m`; `T/2` from `m = 4`), controls + §19.5 observer-strength tripwire bite, verdict folded into the `--gf7-timeline` criterion-9.9 conjunction: **PROVISIONAL-PASS**. Finer-hook alternative rejected (hooks §3 payload ban intact; §19.1 reopening criterion). Leg (b)'s open remainder narrows to the §19.1 sealing form (receipt-timestamped live-driver re-run) — the seal input is now that re-run, not the emission's existence. UPDATE 2026-07-18: the two seal-path items without prior rows registered per rule 94 rule 5 — the §17.9 Gate-7 calibration and the terminal `K_COVER` seal act each have their own front row below; this row remains the seal-input list's home. UPDATE 2026-07-18 (leg-(b) sealing form graded — **PASS, leg (b) CLOSED**; verified: `gf7_sealing_run.rs` in `shekyl-engine-core` + `gf7_seal.rs` in `shekyl-staking-sim`, record WI-4 §19.9): the §19.8 committed design executed against a live `shekyld --regtest` on the production task+dispatch path (24 graded runs + 8 positive-control, 2 560 receipts, 4 h 42 m); gate row `r = 1.275` (clustered SE 0.195) under the committed bound 2 — ≈ 3.7 SEs clear, escalation band untouched; both controls bite (positive 0.850 ≥ 0.80, negative 0.108 at chance); receipt-noise floor 474 ms of `T = 60 s` — the §19.1 reopening criterion not tripped. `K_COVER` seal input #2 discharged; remaining open inputs: GF-4/F-D2 drain-send subsystem + §17.9 Gate-7 calibration (rows below). UPDATE 2026-07-18 (same day, PR #337 review): **grade WITHDRAWN — leg (b) re-OPEN, hardened re-run pending (WI-4 §19.9.1)**. The review found the harness could not detect background-miner death (the §19.8.2 chain-advancement row is unverifiable from the artifact), the grader enforced neither the committed `R = 24 + 8` nor run-ordinal contiguity, the recording period was three untied literals across two crates, and the documented `--gf7-seal <path>` form silently ran the default simulation. Hardening landed same PR (fail-loud miner watch, `R`-floor + contiguity + per-row `cadence_ms == TICK_MS` enforcement, `Arm`-typed label/config binding, both CLI forms loud, CI clippy lane compiling the `gf7-hooks` harness); the §19.8 committed design is unchanged — enforcement, not motion of any bar. `K_COVER` seal input #2 returns to the open list until the re-run grades. UPDATE 2026-07-19 (hardened re-run graded — **PASS, leg (b) CLOSED**; record WI-4 §19.9.2, both sessions disclosed): session 1 (4 h 41 m, clean harness run) graded **INVALID** — the §19.8.3 negative control tripped at `P(link) = 0.167` vs ±0.05 around chance (the control catching shared-host cross-wallet phase coupling; grader arithmetic independently recomputed and confirmed); stopping rule stated before session 2 (a second INVALID ⇒ §19.8 decoupling design round, no re-rolling); session 2 (4 h 42 m, 24 + 8 runs, 2 560 receipts) **PASS** — gate row `r = 0.412` (clustered SE 0.121, ≈ 13 SEs under the bound), `m = 1` context `r = 1.050`, positive control 0.875 ≥ 0.80, negative 0.087 at chance, noise floor 413.9 ms (stable 406–474 ms across all sessions; §19.1 reopening criterion untripped). Inter-session gate-row dispersion (0.41 vs 1.28/1.31 in the non-counting sessions) disclosed in §19.9.2; every session ever observed sits ≥ 3 SEs under the bound. UPDATE 2026-07-19 (scope review, same PR — **leg-(b) seal-input status WITHDRAWN; WI-4 §19.10**): the form's `K = 2` concurrently-posting-persona premise is **design-foreclosed** — the persona-lifecycle enumeration (§19.10.1: wallet-timed `P`-attributed txs = `2 + E + H (+R)` per lifetime; funding + drain unattributable; slash is a consensus mutation, not a tx) shows no code path produces the graded behavior, and `stake_engine` has **no rotation scheme** (rotation is a caller-driven bundle operation; multiple concurrently-*bonded* personas supported for unbond reachability; nothing creates a second *posting* persona; `1/R` economics make one self-defeating). Same correction class as TM-1's own closure and F-W7/8/10. The §19.9.2 measurement records stand; the hardened instrument is retained as a **dispersal-regression tripwire** (broken draw ⇒ exact-phase corroborator, ~75–150× background filter on the few real posts) under the `gf7-hooks` CI lane — no further sealing re-runs required by anything. `K_COVER` seal re-dispositioned in M1 §4 (2026-07-19): predicate = partition-proof operand (DISCHARGED, §14.4) + value paired to the **measured** cover floor (`N ≈ 10` min / `20` with margin, from this doc's own entry-seam grades) via the §14 founder schedule — evidentiary predicate as discharged as design permits; remaining act = land the value + commitment, clear the sentinel (PF-9 unchanged) |
| M1 reward gate (consensus rule) | [`ARCHIVAL_REWARD_GATE_M1.md`](ARCHIVAL_REWARD_GATE_M1.md) (the spec + §9/§10/§11 round records) + `ARCHIVAL_BOND_WI4_MEASUREMENT.md` §16.2 (the pre-flight pin + three obligations; merged to `dev` 2026-07-06 via #262 — the merge-ordering dependency pinned in the spec's Provenance is satisfied) | **Design rounds 1–3 closed; IMPLEMENTED steps 1–5 (2026-07-06, spec §11.10 record)** — cold-start refusal in consensus form: zero accrual to every `(P, s, E)` for epochs where `shard_count < K_COVER`; uniform/global/blind (§16.1 partition trap). **Gate input re-anchored at round 3 (§11 M2-1, critical):** `shard_count(E)` = segment-table (`m_archival_shard_segment`) rows with `freeze_height ≤ H_close(E)` — **not** the credit-derived gather count (a participation sensor: non-monotone + withdraw-service griefing lever); threaded as `EpochCloseInputs::frozen_shard_count` + FFI param + C++ count pass. **Implementation conditioned on §1.3 substrate obligations** (segment-freeze pipeline unbuilt: O-1 determinism, O-2 per-branch monotonicity, O-3 pop-symmetry — landing writes without O-3 reopens §9.4). Spec pins: zero-at-top locus in `epoch_close_compute` (two-field `EpochCloseResult`, no per-`P` widening; **zero through the normal close path, never skip** — close-log revert symmetry, prune advance, stored-zero sigma), **wire-level positive-amount rule** (round-2 resiting of the round-1 positive-share rule: any `reward_amount_plain[i] == 0` rejected at `validate()`; the zero-row beacon unencodable; exactly **one** `K_COVER` comparison site, CI grep tripwire), zero-accrual/no-seniority invariant table + zero-accrual≠zero-accountability (serve credits / bad intervals / slashing live during gated epochs), lagged-`Σwork`-read pinned doubly load-bearing (a primaries-recompute refactor bypasses the gate; reopens the round; standing pre-flight check per §11 R2-1), activation-boundary KAT (G-1..G-10, incl. participation-independence) + C++ stored-shape and boundary-reorg tests as first-class deliverables, spec/constant split (**`K_COVER` finalization gates on the §14.4 partition run**; provisional sentinel = compile-time refusal in non-test builds per §9.3), reorg argument written (§9.4 — connect/revert pairing suffices, no new KAT case). **Round-3 amendment (§11.8, M3-1..M3-3):** the count pass armed to the predicate site's standard — single `count_frozen_shards_at_close` helper owns the `≤` filter (one call site; tripwire extended to refuse any other counting read over `m_archival_shard_segment`); C++ unit cases pin the filter boundary (equality counts) + decode-failure loud abort (lenient skip = consensus fork in the gating direction); G-1..G-3 rebound to `frozen_shard_count`; named pre-flight item: WI-4 §14.4 `K_COVER` calibration must derive against segment count, never a served/participation quantity. **Implementation gates decided (§11.9):** §1.3 second branch — gate implements now on fixture rows, O-1..O-3 blocking on the pipeline round which **opens before the gate PR merges**; pre-flight audit pin = dev head at pre-flight time; WI-4 doc merge = sequence slot 0; intra-PR order pinned (constants + compile-refusal armed before the `k_cover` identifier exists anywhere else → helper + threading → wire positivity + corpus fix same-commit → KATs + C++ store tests → tripwire scripts last). **Implemented (§11.10):** the pinned sequence executed on `feat/m1-reward-gate-design` (`a2292b23c`..`31f133e5e`) after the pre-flight pass held the §6 enumeration; M3-3 discharged against the merged WI-4; sentinel refined to gate-identity `0` (provisional ⇔ `0`, sealed ⇒ `≥ 1`; compile refusal remains the shipping guard); tripwire landed as `check_reward_gate_predicate_sites.sh` = consensus-invariants invariant 5. **§1.3 merge condition satisfied 2026-07-06:** the segment-freeze pipeline design round opened (row below). **§1.3 obligations DISCHARGED (2026-07-06):** the segment-freeze pipeline implemented (row below) — O-1..O-3 delivered by the landed connect/pop hooks, and the gate's M1 operand test now counts production-written rows (the §11.10 fixture caveat retired; discharge recorded in M1 §1.3). Pre-flight dispositions recorded: **PF-1** §11.9 sequencing breach → `FOLLOWUPS.md` breach record + halt condition in `26-sub-pr-design-discipline.mdc`; **PF-2** compile-refusal out-of-band-artifact residual accepted with the Guix + signed-tag dependency named (M1 §4); **PF-9** `K_COVER` finalization pinned as a **prerequisite of Phase 7.7 stressnet entry** (M1 §4 rule-21 entry + `RELEASE_CHECKLIST.md`). `K_COVER` sealing gates on the §14.4 partition run. **Landed: PR #263 merged 2026-07-06 (`6b6517bfc`)**. Genesis-frozen — first item on the WI-4 ranked implementation path |
| F-D2 drain-send subsystem (`P`-value-out; Gate-6 R4 close) | [`ARCHIVAL_DRAIN_SEND_FD2.md`](ARCHIVAL_DRAIN_SEND_FD2.md) (Round 1 draft) + [`ARCHIVAL_FIREWALL_GATE6.md`](ARCHIVAL_FIREWALL_GATE6.md) §12.4 (build notes) | **Design round 1 opened (2026-07-19) — scoping DRAFT, review pending; owner: maintainer.** F-D2's remaining half is a whole unbuilt subsystem (Gate-6 §12.4): a `P`-scan data source in the GUI process (staker-mode engine adoption, WI-1 lifecycle shape), a drain tx assemble→sign→broadcast path in engine-core (the claim-assembly analog; persona transport pin applies), and the round-number/random-split default on top (+ the accepted F-D4 §16.4 funding default). DS-1…DS-7 drafted with proposed dispositions; sub-PR slicing proposed (DS-PR-1…6, cross-repo per rule 10). Riders: the FOLLOWUPS "P-drain mechanism re-walk" items (a) fee/change, (b) shape-era sweep, (c) amount-channel-only confirmation land with this subsystem. Closing F-D2 closes Gate-6 R4. *Seal-seat note (2026-07-19, merged from PR #337's scope review):* the former GF-4/F-D1+F-D2 `K_COVER` seal seat is REMOVED (M1 §4 — the drain is not an on-chain observable, F-W10); F-D2 is a Gate-6 build item on its own track, still pre-genesis, no longer seal evidence |
| §17.9 Gate-7 calibration (`K_COVER` seal input) | [`ARCHIVAL_BOND_WI4_MEASUREMENT.md`](ARCHIVAL_BOND_WI4_MEASUREMENT.md) §17.9 (the final Gate-7 gates, R7) + [`ARCHIVAL_REWARD_GATE_M1.md`](ARCHIVAL_REWARD_GATE_M1.md) §4 (constant placement, sentinel mechanics, seal-input list) | **REDUCED to a recorded derivation (2026-07-19, PR #337 scope review; was: Registered 2026-07-18, not started); owner: maintainer** — the measurement content is already in hand (M1 §4 2026-07-19 update): the only lifecycle channel with a measured population dependence is the entry seam, floor `N ≈ 10` minimum / `≈ 20` with margin (WI-4 grades; thin-cover failure `r = 3.54` is the enforced fact); the calibration record collapses to one page — founder schedule `M ≥ ~20` staggered net of adversarial max attrition (no replacement backstop, R7 basis), `K_COVER` = the segment count whose crossing time covers that rollout. Per the `RELEASE_CHECKLIST.md` mechanism-vs-economics queueing rule the organic-adoption form is unsealable; the founder-schedule pairing is the sealable form. The two R7 gates remain the record's content, satisfied by that derivation: (a) the calibration **margin derived against adversarial max net attrition over the full pre-gate window** (the inducible-forfeiture adversary with no replacement backstop — never expected churn; population-level, no founder obligation), carried in the calibration record; (b) **per-channel thickness-reachability** against the union of per-channel floors with non-existence armed per channel, including the descriptor channel's profile-commonality classification (fingerprint-not-zero-cover routes to redesign, never a larger `K`). Deliverable: the calibration record + the derived `K_COVER` value, margin committed a-priori per the gating-lemma discipline. Expect a design round first (rule 26 — adversary-model artifact). Registered per rule 94 rule 5 ahead of implementation; M1 §4 pins the derivation input: segment count, never a served/participation quantity (§11 M3 named pre-flight item) |
| `K_COVER` seal act (terminal) | [`ARCHIVAL_REWARD_GATE_M1.md`](ARCHIVAL_REWARD_GATE_M1.md) §4 (sentinel mechanics + the 2026-07-18 seal-input UPDATE) + [`ARCHIVAL_BOND_WI4_MEASUREMENT.md`](ARCHIVAL_BOND_WI4_MEASUREMENT.md) §13.5 (sealing conditions) | **Re-dispositioned (2026-07-19, PR #337): evidentiary predicate discharged as designed — blocked only on the act itself; owner: maintainer** — per the M1 §4 seal predicate (stated once, 2026-07-19): (a) partition-proof operand — DISCHARGED (§14.4 PARTITION-PASS, PR #291); (b) value paired to the measured cover floor via the founder schedule — content in hand (row above). Former inputs removed on scope review: GF-7 leg-(b) WITHDRAWN (WI-4 §19.10 — premise design-foreclosed; instrument retained as tripwire); GF-4/F-D2 drain-send seat REMOVED (F-W10 — not an on-chain observable; stays a Gate-6 build item, not seal evidence). The act itself: land the calibration-derived `K_COVER` in `config/consensus_constants.json`, clear `k_cover_provisional` (the compile-time refusal disarms — M1 §4 sentinel mechanics), sweep the cleared-gate lines in the same landing (M1 §4, WI-4 §13.5, `RELEASE_CHECKLIST.md` PF-9 stressnet-entry pin — the 4-week clock starts at seal), and record the seal decision-anchored (PR number + hash). The two permanently conditional §13.5 clauses (isolation posture, effective-cover assumption) ride the sealed verdict line as named assumptions, never as inputs this act waits on |
| Segment-freeze pipeline (`m_archival_shard_segment` writer) | [`ARCHIVAL_SEGMENT_FREEZE_PIPELINE.md`](ARCHIVAL_SEGMENT_FREEZE_PIPELINE.md) (round 1 draft) | **Design round 1 opened (2026-07-06)** — the production writer/deleter the M1 gate, gate-2 challenge registry, and M1 §9.4 reorg argument consume. Freezing = **first-crossing rule** over the consensus curve-tree leaf count (`frozen = ⌊leaf_count / SEGMENT_LEAF_COUNT⌋`; `freeze_height` = first crossing; `R_k` = the layer-2 chunk hash the same-txn `grow_curve_tree` computed — MMR-final, no new crypto). O-1..O-3 discharge by inheritance from the tree machinery (drain journal + `trim_curve_tree` already consensus-atomic in the block write txn); pop hook is a derived delete, no freeze journal (doc §3). Pins `SEGMENT_LEAF_COUNT = 25 992` (level-2, `38·18·38`) into the constants pipeline; boundary division single-sourced in a Rust FFI entry point called by both hooks (M1-1 shape). Exercises the maintainer's pre-genesis schema/FFI authorization: **deletes `archival_shard_leaf`** (derived copy of `m_curve_tree_leaves`; challenge path reads the 38-leaf chunk directly, doc §6.2); gate-2 fixture `segment_leaf_count` placeholder `26 000` regenerates at `25 992`. **IMPLEMENTED steps 1–7 (2026-07-06, `feat/segment-freeze-pipeline`)** — constants + Rust first-crossing arithmetic with unit tests, FFI exports, connect/pop hooks in the block write txn (`process_archival_segment_freezes_at_height` / `revert_archival_segment_freezes`), `archival_shard_leaf` deleted with the challenge-path rewire, the §9 C++ suite (`archival_segment_freeze.cpp`: first-crossing, multi-segment catch-up, independent `R_k` recomposition, bit-identical pop/re-apply, pop-above-boundary no-op, missing-chunk loud abort, M1 operand on production rows), gate-2 fixture regenerated at `25 992`, tripwire extensions (writer one-site, cursor accounting, division one-site, `for_kat` containment), and docs (`LMDB_SCHEMA.md` 42 → 41; M1 §1.3 discharge). O-1..O-3 **discharged**. Pre-flight additions folded in: **PF-8** transposition-distinguishing epoch-close FFI fixture (exact-sigma pin over the three leading `u64`s); **PF-6a** `KCover` capability newtype (`consensus()` sole production constructor; `for_kat` behind the permanent dev-only `consensus-kat` feature). **Landed: PR #264 merged 2026-07-06 (`0d224faac`)**, incl. the O(1) pop-symmetric frozen-shard counter (LMDB `VERSION` 8) |

---

## 5. What is live vs. built-but-unwired vs. missing (Stage 3 slice + wallet submit-verdict path)

Verified at `dev` = `e22549a79`:

| Layer | State | Evidence anchor |
| --- | --- | --- |
| `StakeEngine` persona/bond/scan surface | **Live** — spawned by orchestrator lifecycle for staker wallets | `engine/lifecycle.rs` (`spawn_stake_engine_if_staker`) |
| Bond signing (`sign_bond` → `JoinMarketVin`) | **Live** (inert output — signed vin not yet assembled into a broadcastable tx in production) | `engine/stake_engine.rs` |
| `P`-scan pipeline (SP-0…SP-7) | **Wired (WI-1)** — `start_pscan_if_staker` called from `shekyl-wallet-rpc`'s wallet lifecycle (staker open/close); closure proven by a behavioral test, not the `dead_code` lint | `engine/pscan/start.rs`; `shekyl-wallet-rpc/src/lifecycle.rs` |
| `P` transport (`PTorClient`, `PRpc`, `PBlockSource`) | **Built, unwired** | `shekyl-p-transport`, `engine/pscan/` |
| `BroadcastSubmitter` / `BroadcastPosture` (SP-T4a) | **Live (`Local` posture)** — the WI-3 dispatch driver feeds `submit_bound`; `PerP` posture still awaits 2d-2 | `engine/transaction_submitter.rs` (`for_posture`/`submit_bound`), `engine/posture.rs`; landed #254, fed by `pscan/dispatch.rs` |
| Block-timed dispatch driver (consume seam plan → submit at height) | **Implemented — in review** (WI-3, `feat/wi3-dispatch-driver`; GF-7 acceptance open per the WI-3 row's reconvergence gate) | `engine/pscan/dispatch.rs`; WI-3 row above |
| Submit lifecycle driver (§5.3 watchdog + F40 re-scan / F40-R2 breaker) — *distinct from the staking dispatch driver above* | **Built + wired to `Engine`, no production cadence caller (as of 2026-07-05)** — `pub run_submit_lifecycle_tick` drives the watchdog kernel over held F14 locks (escape ladder, presence branching, F40 targeted re-scan, F40-R2 fruitless-rescan breaker); the post-refresh call site is documented but not yet invoked in production (tests drive `tick` directly). UPDATE 2026-07-05: landed #257 (verified: git grep `run_submit_lifecycle_tick` on dev → `engine/mod.rs`; driver in `engine/submit_lifecycle.rs`) | `engine/submit_lifecycle.rs`, `engine/mod.rs` (`run_submit_lifecycle_tick`), `engine/submit_watchdog.rs` |
| Production bond-tx assembly | **Missing** — full assembly exercised only in KATs | `local_pending_tx.rs` tests |
| GF-4b backing-lineage pre-join wiring (ladder + sweep + `BackingSet`) | **Built, unwired (as of 2026-07-08)** — the `REWARD_EMISSION_VIN_PLAN.md` §8.0.3 C-1 activation precondition is **satisfied**: `MintLineageOutput` + `spendable_height` classified/stored at the scan seam (`PSCAN_STATE_VERSION` 5), `sweep_funding_outputs` (sweep semantics, GF4b-6 spendability filter, `SpentRecordsDurablyPruned` witness gate — go-live fails to compile until SP-R0 mints the witness), `BackingSet` + zero-pre-bond-output test. C-1 residue enumerated with named criteria at the GF-4b doc §5; GF4b-2 genesis gate in FOLLOWUPS V3.0 queue | `ARCHIVAL_GF4B_BACKING_LINEAGE.md`; `engine/backing_set.rs`, `engine/bond_assembly.rs` (`sweep_funding_outputs`), `pscan_state.rs` (v5) |
| Reward-emission leg (PR-E1…E3, C-1 verifier) | **C-1 LANDED (as of 2026-07-09, PR #277 merged, `dev` `13c368707`)** — all six commit-blocks on `dev`; the emission leg is code-complete through the connect arm, with only the wallet-side claim builder + regtest e2e as named residue. UPDATE 2026-07-09: #277 merged (verified: `git grep txin_archival_reward_emission` → `cryptonote_format_utils.cpp:706` whitelist; `apply_archival_emission_claim` → `blockchain_db`). History: PR-E3 verify body + WS-2 write-side landed via PR #269 — the §7.1 steps 1–6 verify module (`emission_verify`, KAT-tested) and the LMDB emission-claim connect / pre-image journal / pop-restore path; the emission numerator FFI (`shekyl_archival_emission_epoch_work`) and the as-of-`E` snapshot gather live since then. The gating design round CLOSED 2026-07-07 (`REWARD_EMISSION_E3_GATING_ROUND.md`): policy trio Q3/Q11/Q12 resolved, **WS-1 ratified** (M-2 supply conservation under _bits-sourcing ∧ as-of-fire-height acceptance_; serve-credit bits as sole `work_P` source; one `at_height` accessor, two consumers), **WS-2 ratified** (three-layer `(P,E)` dedup; prune-straddle double-mint closed by journaled pre-image revert). The C-1 blocks: Block 1 — `budget(E)` production per `ARCHIVAL_BUDGET_SCHEDULE.md` (RATIFIED; F-C1a closed): redirect-the-write at the connect site (`HF_VERSION_ARCHIVAL_EMISSION`), `archival_budget_accrual` per-height row (burn-record idiom, pop-symmetric), frozen `archival_budget` close row in the sigma txn, snapshot gather carries `budget_atomic`/`has_budget_row`, KATs B1/B2/B3. Block 2 — `txin_archival_reward_emission` transport shim (dense tag `0x04` per F-C1b; opaque `canonical_bytes`, `emission_wire.rs` owns the codec). Block 3 — emission verify FFI + dispatch: the full `check_tx_inputs` emission branch (classification → PQC-slot binding → reference-block context → F-C1c signable hash [RATIFIED §9.6] → frozen snapshot gather → coarse verify → fee-input FCMP++). Block 4 — **activation cut**: whitelist flip (Q3 arity-1 / Q11 mixing gates), `verCtSemanticsEmission` CT balance, `check_tx_outputs` plaintext-reward exemption, block-level `(P,E)` uniqueness Rust-decided (`shekyl_emission_block_claims_unique`). Block 5 — connect arm: emission vin → `apply_archival_emission_claim` (WS-2 single writer, pre-image journal, pop-symmetric), coinbase-shaped vout storage. Block 6 — KAT B5 fee-coinbase foreclosure. In-PR findings fixed: F-B5a/b (connect/pop height operands), F-B1a/b/c (accrual ordering, `bl.major_version` redirect anchor + CI tripwire, verify-operand identity + genesis guard). Residue (FOLLOWUPS, V3.0): wallet-side claim builder + §9.5 item-8 regtest e2e (E4/E5 gate; also carries the conservation KAT's fee-leg e2e). UPDATE 2026-07-09 (`feat/emission-conservation-kat`): the end-to-end conservation KAT fast-follow LANDED — `archival_budget_conservation_boundary` (`tests/core_tests/archival_budget_conservation.{h,cpp}`, real connect path, labeled-row form, pop/reconnect boundary straddle; CI `conservation` lane of `run_economics_c2a_prime.sh`); the fee-free fixture leaves the fee-pool half of `staker_inflow` to the regtest-e2e residue above (disclosed in the test header). UPDATE 2026-07-09 (`chore/delete-emission-burn-leg`): the pre-activation burn leg + `HF_VERSION_ARCHIVAL_EMISSION` DELETED (rule 60 — unreachable-by-construction, emission is a genesis fact); accrual now unconditional per non-genesis block, raw-import guard refuses all non-genesis blocks, tripwire re-anchored, KATs B1/B2 + conservation KAT reframed burn-leg-free (`ARCHIVAL_BUDGET_SCHEDULE.md` §2.1 is the historical record) | `REWARD_EMISSION_LEG.md`, `REWARD_EMISSION_VIN_PLAN.md`, `REWARD_EMISSION_E3_GATING_ROUND.md`, `ARCHIVAL_BUDGET_SCHEDULE.md`; `shekyl-archival-retention/src/emission_verify.rs` |
| Principal stake/unstake/drain lifecycle | **Missing — not yet homed in any plan doc** | `WALLET_REWRITE_PLAN.md` timeline note |
| Serve-credit C++-decision equivalence audit (D-SC-A/B/C mirrors + standing KAT) | **Landed (as of 2026-07-09)** — *verification only, no consensus change*; mirrors the three C++ serve-credit decisions (`blockchain.cpp:4247`/`:4312` wide-gate/`:4889–4910`) as pure Rust + shared-fixture equivalence KAT; the behavior-preserving decision-site **flip is V3.1** (FOLLOWUPS). UPDATE 2026-07-09: implementation landed (verified: `git grep serve_credit_gate_decision` → `rust/shekyl-archival-retention/src/serve_credit_decisions.rs`; both KAT legs `serve_credit_equivalence_kat.rs` / `tests/unit_tests/archival_serve_credit_equivalence.cpp` over shared fixture `serve_credit_equivalence_kat_v1.json`; fuzz targets in the CI smoke gate). **No divergence found**; SCE-1 resolved by the post-equivalence unify commit (D-SC-C re-keyed onto `ArchivalServeCreditKey`). UPDATE 2026-07-09: design rounds 1–3 + post-closure pins closed, PR #274 (decision-anchored: the design-doc commits) | `ARCHIVAL_SERVE_CREDIT_EQUIVALENCE_AUDIT.md`; `FOLLOWUPS.md` V3.0 queue item |

---

## 6. Known stale statuses in other docs (to fix separately)

Recorded here rather than edited in place, per the scope of the PR that adds
this index:

- [`ARCHIVAL_BOND_PR2_CHAIN.md`](ARCHIVAL_BOND_PR2_CHAIN.md) §2 arc table
  lists **2d-1 as "not started"**; SP-0…SP-7 landed via PR-A #201 / PR-B #205
  (built, unwired — see §4/§5). The same row's "2d-2 not started" understates
  SP-T1/#204/#209 and SP-T4a/#254.

---

## 7. Doc directory (active fronts)

| Doc | Owns |
| --- | --- |
| [`WALLET_REWRITE_PLAN.md`](WALLET_REWRITE_PLAN.md) | Engine-layer phase plan; Stage architecture; Stage-3 gating |
| [`ARCHIVAL_BOND_PR2_CHAIN.md`](ARCHIVAL_BOND_PR2_CHAIN.md) | Bond-PR chain record + per-sub-PR statuses |
| [`ARCHIVAL_BOND_2C_GF7_HOOKS.md`](ARCHIVAL_BOND_2C_GF7_HOOKS.md) | GF-7 measurement-hook design (PR #255) |
| [`ARCHIVAL_BOND_WI4_MEASUREMENT.md`](ARCHIVAL_BOND_WI4_MEASUREMENT.md) | WI-4 GF-7 graded genesis gate: a-priori threshold, posture-conditioned correlator spec (§4.1.1/§4.3.1 incl. the pinned in-model ceiling), §13 result (PROVISIONAL-PASS, local-daemon posture only), §14 founder-cover launch posture + partition-adversary arm spec (§14.4 hypothesis class widened pre-implementation), §15 remote-daemon structural-refusal disposition, §16 mechanization addendum (M1 reward-eligibility-by-shard-count consensus rule pre-flight pin + M2–M7; global-and-blind constraint), §17 review-closure rounds R1–R2 (the three §16.10 attacks answered attack-shaped: N-sweep added, family widened to five members + two label-free channels closed structurally, `K_COVER` cycle traced to its breaking joint; §17.5 the r<2 mean-vs-max challenge run to land — exchangeability named as the load-bearing step, regime-splitting as the mean-to-max carrier), §18 distinct-position round R3 (five-qualifier scope pin on the `1.86`: entry-seam-only / per-post-not-per-principal (cross-seam GF-4 conditional landed on the verdict line) / timing-not-value (loud reward channel verified at source) / per-instant / unstratified; GF-4 + value channel named co-equal sealing-path rounds), §19 leg-(b) wall-clock sweep-phase arm spec (harness-timestamp disposition, `r = P(link)·(N−1) < 2`, §19.5 strength tripwire; graded 2026-07-11, PROVISIONAL-PASS; sealing form: first live run's PASS withdrawn 2026-07-18 (§19.9.1), hardened re-run graded PASS 2026-07-19 (§19.9.2), seal-input status withdrawn same day on scope review (§19.10 — premise design-foreclosed; instrument retained as dispersal tripwire)) — §§15–16 proposed review-gated; §§17–18 conducted, ratification pending |
| [`ARCHIVAL_BOND_2D1_PSCAN_PLAN.md`](ARCHIVAL_BOND_2D1_PSCAN_PLAN.md) | `P`-scan pipeline (SP-0…SP-7, PR-A/PR-B) |
| [`ARCHIVAL_BOND_2D2_TRANSPORT_PLAN.md`](ARCHIVAL_BOND_2D2_TRANSPORT_PLAN.md) | Transport slices (SP-T0…SP-T5, DQ-N) |
| [`ARCHIVAL_BOND_SP_R0_PLAN.md`](ARCHIVAL_BOND_SP_R0_PLAN.md) | SP-R0 Round-0 scoping: three-arm decomposition, locked decisions D-1…D-3, DQ-A…F dispositions, arm-#1 key-image-watch design |
| [`ARCHIVAL_STAKE_ACTIVATION_PLAN.md`](ARCHIVAL_STAKE_ACTIVATION_PLAN.md) | Staker-activation round (the genesis front): first-stake chicken-and-egg + reopen-collapse bootstrap, SA-DQ-1…SA-DQ-5 (incl. the GF4b-2 count gate and the GF-7 linkage fork) |
| [`ARCHIVAL_FIREWALL_GATE6.md`](ARCHIVAL_FIREWALL_GATE6.md) | Gate-6 rounds, GF-N findings, S-N seam scenarios |
| [`ARCHIVAL_GF4B_BACKING_LINEAGE.md`](ARCHIVAL_GF4B_BACKING_LINEAGE.md) | GF-4b backing-lineage design + landing record: sweep conversion (WI-2 §3.2 amendment), `MintLineageOutput` ladder + `spendable_height` (schema v5), `BackingSet` + zero-pre-bond-output test, `SpentRecordsDurablyPruned` witness gate, GF4b-1…6 review round, §5 C-1 residue criteria |
| [`REWARD_EMISSION_LEG.md`](REWARD_EMISSION_LEG.md) / [`REWARD_EMISSION_VIN_PLAN.md`](REWARD_EMISSION_VIN_PLAN.md) | Emission leg (PR-E0…E3, C-1) |
| [`REWARD_EMISSION_E3_GATING_ROUND.md`](REWARD_EMISSION_E3_GATING_ROUND.md) | E3 gating round: pre-flight audit + policy-trio (Q3/Q11/Q12) closure |
| [`EMISSION_CLAIM_BUILDER.md`](EMISSION_CLAIM_BUILDER.md) | Wallet-side emission claim builder (E4-gate part 1): assembly spec, CB-1…CB-5 round-1 questions, inherited-obligation discharge table, round-2 RPC surface (§7) + implementation PR chain (§8) |
| [`V3_1_MULTISIG_RUST_ENGINE.md`](V3_1_MULTISIG_RUST_ENGINE.md) | V3.1 multisig: Track A wire (MSW-1…MSW-8, MSW-G=5) + product **Option E′** (§0.5); Round 1 adversarial; MS-8 retired; MSW-7 retracted; protocol `PQC_MULTISIG.md` |
| [`DAEMON_SUBMIT_VERDICT.md`](DAEMON_SUBMIT_VERDICT.md) | Daemon submit-verdict series (F-N findings incl. F40/F41) |
| [`ARCHIVAL_SERVE_CREDIT_EQUIVALENCE_AUDIT.md`](ARCHIVAL_SERVE_CREDIT_EQUIVALENCE_AUDIT.md) | Serve-credit C++-decision equivalence audit: D-SC-A/B/C mirror spec, shared-fixture standing KAT + leg-responsibility split, mirror-then-fix discipline, SCE-N findings ledger (SCE-1 key-encoding split), V3.1 flip deferral |
| [`PRINCIPAL_STAKE_LIFECYCLE.md`](PRINCIPAL_STAKE_LIFECYCLE.md) | Principal-side stake lifecycle design |
| [`STAKER_ARCHIVAL_SIM.md`](STAKER_ARCHIVAL_SIM.md) | Staking/archival simulation harness |
| `docs/completed/` | Closed-out tracks (CT series, DAA/LWMA-1, RandomX v2 phases, …) |
