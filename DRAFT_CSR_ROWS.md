# DRAFT — DRS-P0f conformance rows, 16 non-K backlog rows

**HOLD: do not append to `docs/design/CONSENSUS_STORE_RECONCILIATION.md` §5.4.1
until steering clears it.** §5.4.1 has one authorised writer (`shekyl-core-7b`,
K family, slice 9) and it is not this lane. These rows are drafted text, held in
the worktree, ready to paste into the §5.4.1 register table.

**Reviewer:** `shekyl-core-24 [b27bcb]`, lane `docs/csr-conformance-16`.
**Pin:** `eb1b601982897f95081e3128ea77d5576376747a` (`dev`, merge of PR #699).
**Dated:** 2026-09-11.

## What this pass did NOT execute — read this before trusting a row

- **No build, no test run, no daemon.** Zero C++ compiles, zero `cargo test`,
  zero `ctest`. Every verdict below rests on a **code walk at the pin**, plus
  citation of tests that already exist. Where a row's evidence is only a walk,
  the row says so. This was a read-only phase by assignment; writing tests was
  out of scope, and no row claims a gate it did not run.
- **No pop/revert path.** Scoped out throughout as **P0b's** subject, per the
  register's existing convention.
- **Three rows carry a live exposure** (CEN-M3, CEN-G6, CEN-G6b): PR #701
  (`feat/fl-r20-relay-policy-2`) is **OPEN** at this pin and rewrites the very
  functions under them — `check_tx_inputs` (+211), two hunks in
  `get_dynamic_base_fee_estimate_2021_scaling`, and
  `update_next_cumulative_weight_limit`. Re-checked immediately before writing:
  still open (head `86ba9f9d5`, no merge commit). **An unmerged tree is not a
  pin**, so all three are reviewed at `eb1b60198` with the exposure and its
  falsifier written into the row. #701 also rewrites CEN-M3's *census row
  itself* (line anchors → symbol anchors, and a changed rule statement under
  FL-R20/FL-R23), so M3's row below reviews the **pre-#701 rule text**, which
  the incoming census row preserves verbatim as its *"Until PR B:"* clause.

## Findings this pass produced that are NOT conformance verdicts

Routed, not decided here:

1. **`docs/completed/CONSENSUS_C2_R1_REORG.md` §5.5 is headed "DRAFT — steering
   review, then Rick; nothing here is ruled"** — and that heading is **stale**.
   The doc's own banner records "R1c signed in full 2026-09-04 (seven rulings
   Q1a–Q3b)" and §5.7 records the row dispositions landing. Six of my rows cite
   §5.5 as their ratified spec. The rulings are real; the heading lies about
   them. A future reader consulting §5.5 directly is told nothing there is
   ruled. Census/doc-lifecycle question (rule 95), not a code verdict.
2. **CEN-H1's census row under-cites its enforcement sites.** It names two live
   sites plus the constant's getter; there are **three** live sites — the third
   is `src/cryptonote_protocol/cryptonote_protocol_handler.inl`
   (`make_block_connect_supplement`, the unpack guard). Census question.
3. **CEN-E5's census row says the chain-conflict rollback is
   `saturating_sub(pt.first, 2)`.** The shipped code is `pt.first >= 3 ?
   pt.first - 2 : 1` — floored at **1**, not 0, deliberately, because genesis
   cannot be popped. The row's phrasing understates the shipped floor. The code
   is the stricter and correct one; the row text is imprecise. Census question.
4. **CEN-M10's census row conflates two eviction mechanisms.** "stale-reference
   and weight-cap pruning … evict lowest fee/byte first" reads as one ordered
   sweep. They are separate: weight-cap pruning is fee/byte-ordered; the
   stale-reference arm is a predicate on FCMP++ reference age inside the
   receive-time sweep and has no fee ordering. Census question.

---

| Row | Conformance state | Evidence | Digest acceptance |
| --- | --- | --- | --- |
| **CEN-A1** | **CHECKED-CONFORMANT** | Reviewed at `eb1b60198` (2026-09-11, DRS-P0f backlog). Rule = a block whose hash is already known is recorded as `ALREADY_EXISTS`. **Three-store membership:** `Blockchain::have_block_unlocked` (`src/cryptonote_core/blockchain.cpp`) tests main (`m_db->block_exists`), alt (`m_db->get_alt_block`) and the process-lifetime invalid set (`m_invalid_blocks.count`) in that order, setting `*where` to `HAVE_BLOCK_MAIN_CHAIN` / `HAVE_BLOCK_ALT_CHAIN` / `HAVE_BLOCK_INVALID`; `Blockchain::have_block` is the locking wrapper. **Consumer:** `Blockchain::add_new_block` calls `have_block(id)` first, before any routing, and on a hit calls `record_block_ingest(bvc, SHEKYL_BLOCK_INGEST_ALREADY_EXISTS)` then returns false — **the outcome byte, not the inherited bool**, confirming the row's post-PWD-B7 claim: `block_already_exists` (`src/cryptonote_basic/block_ingest.h`) reads that byte through `shekyl_block_ingest_already_exists`, and `BlockIngest::AlreadyExists = 2` (`rust/shekyl-peer-policy/src/block_ingest.rs`) is documented *"Not invalid; not new."* **Invalid-set write sites walked:** the only insert is `add_block_as_invalid` and the only other mutation is `m_invalid_blocks.clear()` — no consensus verdict reads it. **One observation against the Q3a falsifier, recorded not charged:** the falsifier reopens the classification if "the invalid set [is] consulted for anything but dedup", and there IS a second consumer outside this row's cited sites — `src/cryptonote_protocol/cryptonote_protocol_handler.inl` (`handle_response_chain_entry`) switches on the same `where` and treats `HAVE_BLOCK_INVALID` — **and its `default:` arm, which is the same label** — as grounds to `drop_connection(context, true, false)`. That is peer management keyed on membership, not a consensus verdict on a block, so it does not trip the falsifier as written; it is flagged because it is the nearest thing to it in the tree and a future reading of "dedup only" should know the site exists. *Falsifier:* the invalid set acquiring any consensus-verdict role (a block refused *because* its parent is in the invalid set, say), or `add_new_block`'s membership hit ceasing to record `ALREADY_EXISTS`. **Scope: the ingest dedup path only; pop/revert is P0b's.** **Evidence is a walk, not behavioural** — no test was run this pass. Spec: `docs/completed/CONSENSUS_C2_R1_REORG.md` §5.5 Q3a | Digest identity **required**; a match on this row **is** correctness evidence |
| **CEN-A2** | **CHECKED-CONFORMANT** | Reviewed at `eb1b60198` (2026-09-11). Rule = prev_id-equals-tail routes to the main-chain path, anything else to the alternative path, and main-chain connect re-checks and fails closed. **Routing leg:** `Blockchain::add_new_block` (`src/cryptonote_core/blockchain.cpp`) tests `!(bl.prev_id == get_tail_id())` and, on mismatch, stops the read txn guard and tail-calls `handle_alternative_block` with the comment *"never relay alternative blocks"*; otherwise it tail-calls `handle_block_to_main_chain`. Both arms release `rtxn_guard` before the call, so neither path runs under a stale read txn. **Fail-closed re-check:** the four-arg `Blockchain::handle_block_to_main_chain` recomputes `top_hash = get_tail_id(blockchain_height)` under `m_blockchain_lock` and refuses `bl.prev_id != top_hash` via `reject_block_internal(bvc)` + `return false` — a genuine second read, not a cached value passed down from the router, which is what makes it a belt rather than a restatement. The two reads straddle the router's `rtxn_guard.stop()`, so a tip that moved between them is caught here. **Classification note:** the re-check reports `reject_block_internal` (`BlockIngest::Rejected`, drop decided separately by `DropVerdict`) rather than a form/state rejection — correct for a race against our own tip, which is not peer misconduct. *Falsifier:* the connect-path re-check being removed or rewritten to consume a tip hash computed by the caller — that collapses the belt into the routing test and the row becomes a restatement of A2's first clause. **Scope: routing + connect entry. Reorg/pop is P0b's.** **Walk-only evidence; no test run.** Spec: `docs/completed/CONSENSUS_C2_R1_REORG.md` §5.5 Q3a | Digest identity **required**; a match on this row **is** correctness evidence |
| **CEN-A4** | **CHECKED-CONFORMANT** | Reviewed at `eb1b60198` (2026-09-11). Rule = a block whose parent is in neither main nor alt storage is marked orphaned and **not stored**, and the p2p layer re-requests ancestry. **Marking + non-storage:** in `Blockchain::handle_alternative_block` (`src/cryptonote_core/blockchain.cpp`) the `else` arm of the parent-lookup records `SHEKYL_BLOCK_INGEST_ORPHANED` and logs parent-in-alt / parent-in-main / current-top; **no store call appears on that arm** — the alt-store write (`SHEKYL_BLOCK_INGEST_ALT_STORED`) is the sibling branch only. `BlockIngest::Orphaned = 3` is documented *"Parent unknown. Not invalid; the sync path re-requests history."* **Both consumer arms walked, and the Q3b fix is present.** *Live arm:* `src/cryptonote_protocol/cryptonote_protocol_handler.inl` routes `block_announce_request_history(announce)` to `request_chain_history(context)`. *Sync arm:* the in-loop orphan branch is the **built Q3b fix** — it does **not** punish. It logs *"re-syncing without penalizing the origin"*, treats a `cleanup_handle_incoming_blocks` failure as loud-but-non-aborting (an early return would recreate the synchronizing-and-silent stall the arm exists to close), removes the span, clears `m_last_response_height` **and** `m_last_known_hash`, then calls `request_chain_history`. The `m_last_known_hash` clear is load-bearing and the code says why: `request_missing_objects` PREPENDS it to later chain requests, which would restart the walk from the chain just rolled off. **The `:1407`-class bookkeeping-mismatch drop keeps its teeth** — it is a separate, earlier arm and was not softened. *Falsifier, two:* (i) the orphan arm acquiring a store call — that makes orphaned blocks retained state and reopens the storage question; (ii) any path from an in-loop orphan back to a score-drop or `drop_connection` on the origin — that is the punish arm returning, which Q3b ruled a defect. **Scope: ingest marking + both p2p consumers.** **Walk-only evidence at this pin;** the fix's own rig-pinning is recorded in the round (`docs/completed/CONSENSUS_C2_R1_REORG.md` §5.6), not re-run here. Spec: `docs/completed/CONSENSUS_C2_R1_REORG.md` §5.5 Q3b | Digest identity **required**; a match on this row **is** correctness evidence |
| **CEN-D5** | **CHECKED-CONFORMANT** | Reviewed at `eb1b60198` (2026-09-11). Rule = alt-chain difficulty is LWMA-1 over a window stitched from main-chain prefix + alt-chain suffix ending at `bei.height−1`, with a height-0 candidate sentinel-returning difficulty 0. **The ruled crossing is real, and it is the SELECTION that crosses.** `Blockchain::get_next_difficulty_for_alternative_chain` (`src/cryptonote_core/blockchain.cpp`) calls `shekyl_difficulty_alt_window_plan(bei.height, alt_chain.size(), first_alt_height, &plan_main_start, &plan_main_stop, &plan_alt_take)` and then performs **only the fetches the plan names** — `get_block_timestamp` / `get_block_cumulative_difficulty` over `[plan_main_start, plan_main_stop)`, then the newest `plan_alt_take` alt entries oldest-first so the assembled window ends at the candidate's parent. C++ computes no window boundary itself; both regimes (short-alt stitch, alt-covers-the-window tail) live behind the one function, exactly as Q1b ruled. **Both fail-closed legs present:** the height-0 precondition is `CHECK_AND_ASSERT_MES(bei.height > 0, false, …)` with the rationale in-comment (a silent ternary fallback would mask the violation), and a plan refusal — including discontiguous ancestry — is `CHECK_AND_ASSERT_MES(plan_rc == SHEKYL_DIFFICULTY_OK, false, …)`. Both return `false`, which converts to `difficulty_type` **0**, the sentinel CEN-D6's guard rejects. A third belt re-asserts the assembled window is exactly `min(N+1, bei.height)` entries. **The regtest lever is ordered first** (`m_fixed_difficulty` returns before any of this), so the plan is not consulted on that path — matching CEN-D7's carve-out rather than contradicting it. *Falsifier:* any C++ site re-deriving a window boundary (a second `first_alt_height` arithmetic, a locally computed `plan_alt_take`) — that is the selection rule crossing back and reopens Q1b; or a plan refusal being mapped to anything but the 0 sentinel. **Scope: alt-path difficulty selection + assembly.** **Walk-only; the `FORK_CHOICE_V1.json` vectors cited by the census row were not executed this pass.** Spec: `docs/completed/CONSENSUS_C2_R1_REORG.md` §4b Q1b | Digest identity **required**; a match on this row **is** correctness evidence |
| **CEN-D6** | **CHECKED-CONFORMANT** *(one classification gap recorded — behaviour conforms; the verdict CHANNEL does not carry it)* | Reviewed at `eb1b60198` (2026-09-11). Rule = a zero next-block difficulty **rejects the block**, via log-and-return-false, not a process abort. **The macro claim is part of the rule and it verifies:** `CHECK_AND_ASSERT_MES(expr, fail_ret_val, message)` expands to `do{if(!(expr)) {LOG_ERROR(message); return fail_ret_val;};}while(0)` (`contrib/epee/include/misc_log_ex.h`) — `LOG_ERROR` then `return`, **no abort, no `LOCAL_ASSERT`** (the sibling `CHECK_AND_ASSERT` is the one carrying `LOCAL_ASSERT`; D6's sites use the `_MES` form). **Both ratified call sites present, as the marshaling belt on the FFI result:** alt path — `Blockchain::handle_alternative_block` guards `current_diff` from `get_next_difficulty_for_alternative_chain` (*"!!!!!!! DIFFICULTY OVERHEAD !!!!!!!"*) **before** `check_hash(proof_of_work, current_diff)`, so a 0 target can never be handed to the PoW comparison; main path — `Blockchain::handle_block_to_main_chain` guards `current_diffic` from `get_difficulty_for_next_block` identically, also before the PoW check. A sentinel-eating shim at either site would convert a difficulty-function failure into a silently accepted block, which is the harm Q1b named; neither site eats it. **RECORDED GAP — the reason this row carries a parenthetical.** Both guards `return false` **without recording an outcome byte**, unlike every neighbouring rejection in the same functions, which pair `reject_block_form` / `reject_block_state` / `reject_block_internal`. `bvc.m_outcome` therefore stays at its value-init **0 = `BlockIngest::Unclassified`**, which `rust/shekyl-peer-policy/src/block_ingest.rs` documents as *"Nothing recorded… and any new arm that forgets to classify itself. **Not a rejection.** P2P must not drop on it."* The consensus behaviour is still correct — the block is refused and not added, because callers branch on the **bool** — and the composition is benign today: the only byte-consumers are the mined-block path (`src/cryptonote_core/cryptonote_core.cpp`, `handle_block_found`), where `CHECK_AND_ASSERT_MES(!block_rejected(bvc), …)` passes vacuously but the adjacent `block_added(bvc)` is independently false, so nothing relays. **It is recorded because the byte cannot distinguish "deliberately unclassified" from "an arm that forgot"** — and for a self-inflicted 0-difficulty these are different statements. *Falsifier, two:* (i) the guards being hoisted after the PoW check, or either being replaced by a value-substituting shim — that is the ratified belt gone; (ii) **the gap going live:** any consumer branching on `block_rejected(bvc)` (or the byte generally) to make a relay, drop or consensus decision on the main-chain path would silently mishandle a zero-difficulty refusal. That second falsifier is the one to re-test after any PWD-B7 consumer is added. **Scope: both connect-path difficulty call sites.** **Walk-only; no test run.** Spec: `docs/completed/CONSENSUS_C2_R1_REORG.md` §4b Q1b | Digest identity **required**; a match on this row **is** correctness evidence. The recorded gap is a **verdict-channel** defect, not a state defect — it does not reach the digest, and a rewrite is free to classify the arm properly without failing this row |
| **CEN-E1** | **CHECKED-CONFORMANT** | Reviewed at `eb1b60198` (2026-09-11). Rule = in the checkpoint zone, the block id at a checkpointed height must equal the loaded checkpoint hash. **Main path:** `Blockchain::handle_block_to_main_chain` (`src/cryptonote_core/blockchain.cpp`) runs `m_checkpoints.is_in_checkpoint_zone(blockchain_height)` and, inside the zone, `m_checkpoints.check_block(blockchain_height, id)`; failure logs *"CHECKPOINT VALIDATION FAILED"* and takes `reject_block_form(bvc)` + `goto leave`. **Alt path:** `Blockchain::handle_alternative_block` runs `m_checkpoints.check_block(bei.height, id, is_a_checkpoint)`, and the `is_a_checkpoint` out-param is what forces the reorg downstream, as the census row states. **The ratified "NO crossing" claim holds:** `checkpoints::check_block` (`src/checkpoints/checkpoints.cpp`) is derivation-free hash equality over epee-fed C++ state — there is no arithmetic to own and nothing for Rust to hold, which is exactly Q2's reasoning for leaving it in place. The row's semantics are ratified **while the mechanism exists**; existence is HELD for C2-R0 and this review does not disturb that. **Re-checked the reopening trigger and it is still not live:** DRS/R8 has not moved checkpoint state (the register's own trigger note from #595 stands at this pin). *Falsifier:* the zone test and the equality test drifting apart (a block inside the zone reaching connect without `check_block`), or `check_block` acquiring a derivation — either reopens the crossing question Q2 closed. **Scope: main + alt connect. The operator surface that populates these points is CEN-E5; the alt-height floor is CEN-E2.** **Walk-only; `tests/unit_tests/checkpoints.cpp` exists but was not run this pass.** Spec: `docs/completed/CONSENSUS_C2_R1_REORG.md` §4b Q2 | Digest identity **required**; a match on this row **is** correctness evidence |
| **CEN-E2** | **CHECKED-CONFORMANT** | Reviewed at `eb1b60198` (2026-09-11). Rule = an alternative block at or below the last checkpoint preceding the current height is refused. **Implementation:** `checkpoints::is_alternative_block_allowed(blockchain_height, block_height)` (`src/checkpoints/checkpoints.cpp`) — returns false for `block_height == 0` (no alternative genesis), returns **true** when `upper_bound(blockchain_height) == m_points.begin()` (the chain is before the first checkpoint, so there is no floor to enforce), and otherwise steps back one to the last checkpoint at or below `blockchain_height` and returns `checkpoint_height < block_height`. That final comparison is the rule verbatim: an alt candidate **at** the checkpoint height is refused (strict `<`), as is anything below it. **Caller:** `Blockchain::handle_alternative_block` (`src/cryptonote_core/blockchain.cpp`) calls it with `get_current_blockchain_height()` and the candidate's derived `block_height`, and refuses on false — the admission fence sits on the alt path, before the expensive tier, consistent with the Q1a tier contract. **Empty-checkpoint behaviour walked explicitly** because it is the live configuration pre-genesis: with `m_points` empty, `upper_bound` returns `begin()`, so the function returns true and the fence is inert — it is a fence that arms itself when checkpoints exist, not a no-op with a dead branch. *Falsifier:* the comparison relaxing to `<=` (admitting an alt block exactly at the checkpoint height, which is the one height where the checkpoint is a statement), or the caller passing a claimed rather than derived height — the derived height is CEN-K1a's subject and this row depends on it being the one used. **Scope: alt admission only.** **Walk-only; no test run.** Spec: `docs/completed/CONSENSUS_C2_R1_REORG.md` §4b Q2 | Digest identity **required**; a match on this row **is** correctness evidence |
| **CEN-E5** | **CHECKED-CONFORMANT** | Reviewed at `eb1b60198` (2026-09-11). Rule = loading a checkpoint JSON can add checkpoints at runtime — operator-supplied consensus pins — wired uniformly, conflict-fail-stopping, with a bounded rollback. **All four ruled clauses hold.** **(1) Rule-71 uniformity, both guards gone.** `checkpoints::init_default_checkpoints(network_type nettype)` (`src/checkpoints/checkpoints.cpp`) discards its argument — `(void)nettype` with the comment that the per-nettype no-op arms were structurally divergent — and `core::update_checkpoints` (`src/cryptonote_core/cryptonote_core.cpp`) records the deleted `!= MAINNET` guard **and why it was wrong**: it *"returned TRUE — reporting success for work it never did."* The reload now runs wherever a `checkpoints.json` exists, so an operator can rehearse on testnet. **(2) JSON-internal conflict fail-stops with named output.** `checkpoints::add_checkpoint` refuses a second, different hash at a height it already holds, and the message carries height, existing pin and new pin plus the remedy (*"two different pins for one height; fix the checkpoint source"*) — the named output Q2b required. It propagates: `ADD_CHECKPOINT` (`src/checkpoints/checkpoints.h`) is `CHECK_AND_ASSERT(add_checkpoint(...), false)`, so `load_checkpoints_from_json` returns false, `Blockchain::update_checkpoints` returns false, and `core::update_checkpoints` calls **`graceful_exit()`** — *"if anything fishy happened getting new checkpoints, bring down the house."* A missing file is `return true` (not an error), which is correct: absence is not a conflict. **(3) Chain-conflict rollback, bounded twice.** `Blockchain::check_against_checkpoints` (`src/cryptonote_core/blockchain.cpp`) fail-stops outright on a **genesis** conflict (no rollback can resolve it — the reports-success shape, named as its fourth instance), and otherwise computes `rollback_target = pt.first >= 3 ? pt.first - 2 : 1` and pre-checks `m_db->pop_target_allowed(rollback_tip_index)` — **Q1c's prune watermark** — failing closed to the same `graceful_exit` when the rollback would cross it. The height-vs-index conversion (`rollback_target - 1`) is explicit and its review-round provenance is in-comment. The loop `break`s after a rollback because later checkpoints would be read against a pre-rollback height. **(4) Q2c deletions executed:** `m_difficulty_points` / `difficulty_points` — **zero hits across `src/`**; no weekly full-chain recompute remains. **ROW-TEXT CORRECTION, recorded not charged:** the census describes the rollback as `saturating_sub(pt.first, 2)`, which would floor at **0**; the shipped floor is **1**, deliberately, because `pop_block_from_blockchain` throws at `height() == 1` and a target of 0 aborted mid-rollback on the genesis guard (review round 4). The code is stricter and correct; the row text understates it. *Falsifier:* the watermark pre-check being dropped or relaxed (the rollback then reaches the belt's exception instead of the documented fail-stop), a nettype guard returning anywhere in this path, or a conflict being logged-and-continued rather than fail-stopped. **Scope: the operator JSON surface + its rollback. Whether the mechanism should EXIST is HELD for C2-R0 and is not this row's question.** **Walk-only; no daemon run, no conflict rehearsal executed.** Spec: `docs/completed/CONSENSUS_C2_R1_REORG.md` §4b Q2a/Q2b/Q2c | Digest identity **required**; a match on this row **is** correctness evidence |
| **CEN-F14b** | **CHECKED-CONFORMANT** | Reviewed at `eb1b60198` (2026-09-11). Rule = the subsidy penalty curve: median soft-raised to the 300 000-byte free zone; `weight ≤ median` ⇒ full subsidy; `median < weight ≤ 2·median` ⇒ quadratic `base·(2m−w)·w/m²` in u128, fail-closed on overflow. **Implemented in Rust as ratified** — `apply_weight_penalty` (`rust/shekyl-economics/src/emission.rs`), reached from `paid_block_reward`, with C++ a marshaling shim. **Every clause walked.** *Soft-raise:* `let median = median_weight.max(full_reward_zone)` — the free zone is a floor on the median, not on the weight. *Full-subsidy arm:* `current_block_weight <= median` returns `amount` unchanged. *The 2·median boundary:* `double_median = u128::from(median) * 2` computed **in u128 before the comparison**, with the reason in-comment — the comparison must not wrap for medians above `u64::MAX / 2`, which consensus never produces but the FFI edge cannot assume away; `> double_median` returns `Err(EmissionError::BlockTooBig)`, so **exactly** 2·median falls through to the curve and yields zero subsidy rather than rejecting, which is CEN-F14's recorded inclusive-bound divergence and is consistent here. *The curve:* `multiplicand = (double_median - current) * current`, then `amount.checked_mul(multiplicand).ok_or(EmissionError::Overflow)?`, then `/ median / median` — the two sequential divisions are the `m²` denominator without ever forming `m²`. **Fail-closed on overflow is a real `checked_mul`, not a saturate**, matching the rule. The unguarded subtraction is justified in-comment and the justification checks out: past the `current <= median` return, `double_median - current` is below `median` and `current <= 2·median`, so the product stays under `2·u64::MAX² < u128::MAX`. *Cap:* `reward.min(u128::from(amount))` — the penalty can never raise the subsidy. **One benign observation:** the tail `u64::try_from(capped).unwrap_or(amount)` has an unreachable fallback — `capped ≤ amount ≤ u64::MAX` by the `min` immediately above, so `try_from` cannot fail; the `unwrap_or` is dead but harmless, and returning `amount` would be the wrong direction if it ever became live. *Falsifier:* the `checked_mul` becoming saturating or wrapping (a silently capped subsidy is a consensus divergence no test on the happy path sees), the soft-raise moving from the median to the weight, or the `> double_median` comparison being computed in u64. **Scope: the curve between median and 2·median. The reward-side rejection at >2·median is CEN-F14; the release multiplier is CEN-F15.** **Walk-only — the 81-vector cross-language KAT cited by the census row EXISTS and is the row's real behavioural evidence, but was NOT executed this pass.** Spec: `docs/completed/CONSENSUS_C2_R2_WEIGHT_FEES.md` Q4 | Digest identity **required**; a match on this row **is** correctness evidence |
| **CEN-H1** | **CHECKED-CONFORMANT** *(census under-cites its sites — see note)* | Reviewed at `eb1b60198` (2026-09-11). Rule = serialized tx size ≤ `CRYPTONOTE_MAX_TX_SIZE` (1 000 000 bytes). **Single constant, single getter, three live enforcement sites — all walked.** The constant is hand-maintained at `src/cryptonote_config.h` (not `config/`, as the census row correctly flags) and is read through exactly one accessor, `get_max_tx_size()` (`src/cryptonote_basic/cryptonote_basic_impl.cpp`), which is the only place the macro is referenced outside its definition — so there is no second spelling of the bound to drift. **Sites:** (1) `ver_non_input_consensus` (`src/cryptonote_core/tx_verification_utils.cpp`, "Rule 1") checks `get_transaction_blob_size(tx) > get_max_tx_size()` and sets `m_verifivation_failed` + `m_too_big`; this is the NIC-set site and therefore runs on **both** relay admission and pool-supplement connect, per CEN-M2/CEN-G4. (2) `core::handle_incoming_tx_blob`-side check (`src/cryptonote_core/cryptonote_core.cpp`) refuses an oversized blob **before parse**, via `reject_form(tvc)` — a parse-bound guard, which is the job Q6 states for this rule. (3) **NOT CITED BY THE CENSUS ROW:** `src/cryptonote_protocol/cryptonote_protocol_handler.inl`, in the block-connect-supplement builder, refuses `tx_entry.blob.size() > get_max_tx_size()` with *"too large to unpack"* before unpacking. All three refuse rather than clamp. **Census note:** the row names sites (1) and (2) plus `src/cryptonote_basic/cryptonote_basic_impl.cpp`, but that third citation is the **getter's definition**, not an enforcement point — so the row cites two of three live sites. Routed to the census; it does not change the verdict, since all three enforce the same bound through the same accessor. *Falsifier:* any path reaching parse or unpack with an unbounded blob (a fourth entry point added without the guard), or the bound being read other than through `get_max_tx_size()`. **Scope: relay admission, connect, and the p2p unpack boundary.** **Walk-only; no test run.** Spec: `docs/design/GENESIS_TX_WIRE_FORMAT.md` §10 (frozen limit); `docs/completed/CONSENSUS_C2_R2_WEIGHT_FEES.md` Q6 | Digest identity **required**; a match on this row **is** correctness evidence |
| **CEN-H3** | **CHECKED-CONFORMANT** | Reviewed at `eb1b60198` (2026-09-11). Rule = tx weight ≤ half the minimum block weight minus the coinbase reserve (= 149 400). **The arithmetic resolves to the stated constant at this pin:** `get_transaction_weight_limit(hf_version)` (`src/cryptonote_core/tx_verification_utils.cpp`) returns `get_min_block_weight(hf_version) / 2 - CRYPTONOTE_COINBASE_BLOB_RESERVED_SIZE`; `get_min_block_weight` (`src/cryptonote_basic/cryptonote_basic_impl.cpp`) returns `CRYPTONOTE_BLOCK_GRANTED_FULL_REWARD_ZONE_V5` = **300 000** and **ignores its version argument** (the parameter is commented out in the signature — the vestigial-arg fact CEN-G6b also records), and `CRYPTONOTE_COINBASE_BLOB_RESERVED_SIZE` = **600** (`src/cryptonote_config.h`). 300 000 / 2 − 600 = **149 400**. ✓ **Enforcement:** `ver_non_input_consensus` "Rule 4" compares `get_transaction_weight(tx, blob_size) > tx_weight_limit` and sets `m_verifivation_failed` + `m_too_big`. **Both paths are covered by ONE implementation, which resolves the census row's open half.** The row records that "the block-connect side of the same limit was never separately examined"; at this pin there is no separate block-connect implementation to examine — the NIC set is the single site, and it runs at relay admission **and** at pool-supplement connect (CEN-M2 / CEN-G4). The only other consumer of `get_transaction_weight_limit` is `src/rpc/daemon_submit_ffi.cpp`, which reports it as a *fact* to the submit verdict rather than enforcing it — a read, not a second gate. That is the healthy shape: one bound, one enforcer, one reporter. **Dead-branch note:** Rule 4 is gated on `hf_version >= HF_VERSION_PER_BYTE_FEE`, and the limit function's `else` arm returns the un-halved bound; `HF_VERSION_PER_BYTE_FEE` is **1** (`src/cryptonote_config.h`), and Shekyl is v3-from-genesis at HF1, so the gate is always true and the `else` is unreachable rule-60 deletion residue — the same class the census tracks for CEN-H2's collapsed dispatch. It cannot change the shipped bound. *Falsifier:* a second site re-spelling the limit arithmetic (the grep-gate candidate), the version argument becoming live in `get_min_block_weight` (which would make the "always V5" claim false and is CEN-G6b's coupled subject), or the connect path acquiring its own weight check that disagrees. **Scope: the NIC-set weight bound on both paths. Block-level weight limits are CEN-G6/G6b.** **Walk-only; no test run.** Spec: `docs/completed/CONSENSUS_C2_R2_WEIGHT_FEES.md` Q7; `docs/design/DAEMON_SUBMIT_VERDICT.md` §8 row N3 | Digest identity **required**; a match on this row **is** correctness evidence |
| **CEN-M4** | **CHECKED-CONFORMANT** | Reviewed at `eb1b60198` (2026-09-11). Rule = **relay** cap on `tx.extra` ≤ `MAX_TX_EXTRA_SIZE` (24 576), `kept_by_block` exempt, with no consensus-side `tx_extra` bound beyond CEN-H1. **Enforcement:** `tx_memory_pool::add_tx` (`src/cryptonote_core/tx_pool.cpp`) tests `!kept_by_block && tx_extra_size > MAX_TX_EXTRA_SIZE`, logs the size and the limit, sets `m_verifivation_failed` + `m_tx_extra_too_big`, and returns `reject_state(tvc)`. The `kept_by_block` conjunct is **first in the predicate**, so a tx arriving inside a block is never measured — the exemption is structural, not a later override. Constant at `src/cryptonote_config.h` = 24 576. ✓ **The "no consensus floor" half is the clause that needed checking, and it holds.** `MAX_TX_EXTRA_SIZE` has exactly one other referent in `src/`: `construct_tx_with_tx_key` (`src/cryptonote_core/cryptonote_tx_utils.cpp`), which asserts `tx.extra.size() <= MAX_TX_EXTRA_SIZE` **after** `sort_tx_extra`. That is a **builder precondition on transactions we construct**, not an acceptance rule on incoming ones — it can refuse our own tx, never a peer's — so it does not constitute a consensus bound and the census row's claim survives. (A third hit is a comment in `src/cryptonote_basic/cryptonote_format_utils.cpp` reasoning about log amplification; not a gate.) **This is a policy row and its scope statement matters:** the bound is relay-only by construction, so a block carrying a larger `tx_extra` connects, bounded only by CEN-H1's 1 MB and the block weight limits. That is the ratified posture, not a gap. *Falsifier:* the `kept_by_block` conjunct being dropped (which would silently make a relay policy into a consensus rule and could fork the chain), or a connect-path site acquiring an independent `tx_extra` bound. **Scope: relay admission only.** **Walk-only; no test run.** Spec: `docs/completed/CONSENSUS_C2_R2_WEIGHT_FEES.md` Q10; `docs/design/GENESIS_TX_WIRE_FORMAT.md` §10 | Digest identity **required**; a match on this row **is** correctness evidence — but note this is a **relay-policy** row: a rewrite diverging here changes propagation, not chain state |
| **CEN-M10** | **CHECKED-CONFORMANT** *(census row conflates two eviction mechanisms — see note)* | Reviewed at `eb1b60198` (2026-09-11). Rule = pool lifetime and eviction: non-`kept_by_block` expire at 3 days, `kept_by_block` at the longer alt-block lifetime; stale-reference and weight-cap pruning evict lowest fee/byte first. **Lifetimes:** `tx_memory_pool::remove_stuck_transactions` (`src/cryptonote_core/tx_pool.cpp`) applies `(tx_age > CRYPTONOTE_MEMPOOL_TX_LIVETIME && !meta.kept_by_block) || (tx_age > CRYPTONOTE_MEMPOOL_TX_FROM_ALT_BLOCK_LIVETIME && meta.kept_by_block)` — one disjunction, both arms, each with its own guard, so the two populations cannot be confused. Constants at `src/cryptonote_config.h`: `86400*3` = **3 days** and `604800` = **one week**. ✓ **Weight-cap pruning and its ordering — the clause that needed proving:** `tx_memory_pool::prune` defaults `bytes` to `m_txpool_max_weight`, then walks `m_txs_by_fee_and_receive_time` **from `--end()` backwards**, stopping once `m_txpool_weight <= bytes`. That evicts the **lowest** fee/byte first only because `txCompare` (`src/cryptonote_core/tx_pool.h`) sorts **greatest-first** — its own comment says *"sort by greatest first, not least"*, `a.first.first > b.first.first` — so the container's tail is the cheapest tx. **The ordering claim is correct, and it depends entirely on that inverted comparator**; reading the loop alone suggests the opposite. Ties break on earlier receive time, then tx hash, so the order is total and deterministic. `kept_by_block` entries are **skipped** by the prune loop (*"they're likely added because we're adding a block with those"*), consistent with the exemption pattern across the M-family. **CENSUS NOTE, recorded not charged:** the row's phrase *"stale-reference and weight-cap pruning … evict lowest fee/byte first"* reads as one fee-ordered sweep covering both. They are separate mechanisms: weight-cap pruning is fee/byte-ordered as described; the **stale-reference** arm is a predicate inside the receive-time sweep — `ref_stale` = not `kept_by_block`, `max_used_block_height > 0`, and the FCMP++ reference aged past `FCMP_REFERENCE_BLOCK_MAX_AGE` — with **no fee ordering at all**. Its own bounds are documented at the site: `max_used_block_height == 0` covers serve-credit-only txs and `kept_by_block` entries stored on a failed input check, so a genuine reference at height 0 is not swept (bounded residue, template-time re-check excludes it, receive-time sweep eventually evicts). It also keeps no eviction memory, deliberately, so a resubmit re-enters full admission rather than hitting a false-terminal shortcut (`DAEMON_SUBMIT_VERDICT.md` D3). All three mechanisms are present and correct; only the row's sentence merges two of them. *Falsifier:* `txCompare` losing its greatest-first inversion **without** the prune loop being re-oriented — that silently evicts the highest-fee txs first and no test on a uniform-fee pool would see it; or the `kept_by_block` prune skip being dropped mid-reorg. **Scope: relay-side pool retention. The 3-day livetime and the weight cap are consumed as given — never ratified as policy choices (census §10 R2/R7), which this review does not change.** **Walk-only; no test run.** Spec: `docs/completed/CONSENSUS_C2_R2_WEIGHT_FEES.md` Q11 | Digest identity **required**; a match on this row **is** correctness evidence — **relay-policy** row: divergence changes retention, not chain state |
| **CEN-M3** | **CHECKED-CONFORMANT** *(reviewed against the PRE-#701 rule text; re-review OWED at #701's merge sha — see exposure)* | Reviewed at `eb1b60198` (2026-09-11). **Exposure first, because it bounds this row's shelf life.** PR #701 (`feat/fl-r20-relay-policy-2`) is **OPEN** at this pin (re-checked immediately before writing: head `86ba9f9d5`, no merge commit) and it rewrites **both** this row's subject and this row's census entry — `src/cryptonote_core/blockchain.cpp` `check_tx_inputs` (+211 lines) and two hunks in `get_dynamic_base_fee_estimate_2021_scaling`, plus a new relay-floor ring (`relay_floor_at`, `rebuild_relay_floor_ring`) and the FL-R23 constants `SHEKYL_RELAY_ADMISSION_SLACK_BP` (0) and `SHEKYL_RELAY_FLOOR_LOOKBACK` (5) in `src/cryptonote_config.h`. An unmerged tree is not a pin, so this review is against the rule as ratified **today**, which the incoming census row preserves verbatim as its *"Until PR B:"* clause. **Every clause of that rule verifies at this pin.** *Relay-only + exemption:* `tx_memory_pool::add_tx` (`src/cryptonote_core/tx_pool.cpp`) computes `fee_good = kept_by_block || m_blockchain.check_fee(tx_weight, fee)` — short-circuit, so a block-sourced tx is never measured; `get_tx_fee` throwing is caught and routed to `reject_internal`, not silently treated as a low fee. *Unpayable-reward state rejects all:* `Blockchain::check_fee` returns **false** when `fee_per_byte == 0`, and `get_current_fee_per_byte` returns 0 on exactly one arm — `get_block_reward` failing — with the disambiguation stated in-comment and verified: `get_dynamic_base_fee` floors at 1 (`return lo == 0 ? 1 : lo`), so **0 is unambiguously the reward-failure arm** and cannot arise from arithmetic underflow. *Fee-per-byte derivation:* `get_dynamic_base_fee` computes `mul128(block_reward, DYNAMIC_FEE_REFERENCE_TRANSACTION_WEIGHT, &hi)` then `div128_64` **twice** by `median_block_weight` — the `m²` denominator without forming `m²` — then `lo -= lo / 20`, which is the **0.95 factor**; `DYNAMIC_FEE_REFERENCE_TRANSACTION_WEIGHT` = 3 000 (`src/cryptonote_config.h`), and the median is soft-raised to `get_min_block_weight(version)` first. The caller passes `std::min(median, m_long_term_effective_median_block_weight)`, so the floor tracks the long-term effective median as the row states. *Quantization and the 2 % buffer:* `check_fee` rounds **up** to the mask — `needed_fee = (needed_fee + mask - 1) / mask * mask` — then accepts when `fee >= needed_fee - needed_fee / 50`, i.e. **≥ 98 %**, with the no-overflow reasoning in-comment. ✓ *No consensus fee floor:* the only enforcement is the pool site; `check_fee` has no connect-path caller. **One deliberate-design note worth carrying:** `get_current_fee_per_byte` uses the **M_r-neutral** reward overload on purpose, and says why — the relay floor must not start tracking demand as a side effect of the FL-R12′ cutover, and no per-tx `tx_volume_window` scan lands on the admission path. *Falsifier, two:* (i) ordinary — the `kept_by_block` short-circuit being dropped (a relay policy silently becoming a consensus rule), or `get_dynamic_base_fee` losing its floor-at-1 so that 0 stops being unambiguous; (ii) **the exposure falsifier — #701 merging.** At that moment this row is stale by construction: the rule statement changes (lookback-min over G=5 at zero slack; the 0.95 and the 2 % buffer **deleted**), and the code under it changes. **This row must be re-reviewed at #701's merge sha and its verdict re-derived — not carried forward.** Multiple pins within one slice is this register's existing convention (CEN-B5 at `e54e5b983`, CEN-L11 at `ab4693d0e`, CEN-I12 at `667817d47`). **Scope: relay admission only.** **Walk-only — the `check_fee` KAT cited by the census row was NOT executed this pass.** Spec: `docs/completed/CONSENSUS_C2_R2_WEIGHT_FEES.md` Q8 + Q9; `docs/design/DAEMON_SUBMIT_VERDICT.md` §8 row P2 | Digest identity **required** *while the pin holds*; a match **is** correctness evidence for the pre-#701 rule only. **Relay-policy** row. On #701's merge this row's oracle status lapses until re-review |
| **CEN-G6** | **DIVERGENT** | Reviewed at `eb1b60198` (2026-09-11). **The divergence is the short-term surge factor, and it is exactly the one the census row pre-announces.** Rule as ratified: long-term window **100 000** blocks (C2-R2 Q2) and short-term surge factor **S = 4** (C2-R2 Q3, d24 denomination — re-derived and SIGNED after the GAP-7 floor measurement **refuted** the inherited ×50). **Shipped:** `#define CRYPTONOTE_SHORT_TERM_BLOCK_WEIGHT_SURGE_FACTOR 50` (`src/cryptonote_config.h`), consumed at its single site in `Blockchain::update_next_cumulative_weight_limit` (`src/cryptonote_core/blockchain.cpp`) as the upper clamp `CRYPTONOTE_SHORT_TERM_BLOCK_WEIGHT_SURGE_FACTOR * m_long_term_effective_median_block_weight`. **50 ≠ 4. The C++ does not implement the spec it was ratified against, and this row is therefore DIVERGENT — which is a success of this pass, not a failure of it.** **The window clause CONFORMS:** `CRYPTONOTE_LONG_TERM_BLOCK_WEIGHT_WINDOW_SIZE` = **100 000** (`src/cryptonote_config.h`), used both as `m_long_term_block_weights_window` and as the rolling-median cache size, and the median is taken over `min(window, db_height)` blocks with the V5 zone as the pre-history default. One clause conformant, one divergent ⇒ the row is DIVERGENT. **The divergence is KNOWN, NAMED and has a named fixer:** the census row already records it as *"a known divergence until the store port implements the signed value"*. This review's contribution is to **confirm it at a pin** rather than inherit the claim, and to establish it is not closed by work in flight. **Checked against work in flight, at source:** PR #701 is OPEN and **does** touch `src/cryptonote_config.h` — but its hunk adds **only** the two FL-R23 relay constants (`SHEKYL_RELAY_ADMISSION_SLACK_BP`, `SHEKYL_RELAY_FLOOR_LOOKBACK`) and leaves the surge factor at 50; #701 also rewrites `update_next_cumulative_weight_limit`, so this row's **function** is exposed even though its **constant** is not. **Method note, deliberately recorded:** an earlier reading of this question reasoned from #701's file list and got the right answer from wrong evidence (the file list *does* contain `src/cryptonote_config.h`). The verdict above rests on reading the constant and the diff hunk, not on the file list. *Falsifier — what flips this row to CHECKED-CONFORMANT:* the constant becoming **4** in a d24-consistent denomination at the clamp site, with the clamp arithmetic re-verified at the new value (this is not a pure constant swap — the register must confirm the clamp still means `S × long-term` after the denomination change). *Falsifier the other way:* C2-R2 Q3's signature being withdrawn or re-derived back to ×50, which would make the shipped value conformant without any code moving. **Scope: the frozen bounds only — the clamp composition, the zone floor and the 2× limit are CEN-G6b.** **Walk-only; no test run.** Spec: `docs/completed/CONSENSUS_C2_R2_WEIGHT_FEES.md` Q2 (window) + Q3 (surge, struck and re-derived) | **Regression only.** A rewrite reproducing ×50 reproduces a **known, ratified-against divergence** — digest identity here would confirm the port copied the refuted value, which is the opposite of correctness evidence. Do not promote this row on a digest match; promote it only on the falsifier above |
| **CEN-G6b** | **DIVERGENT** | Reviewed at `eb1b60198` (2026-09-11). **DIVERGENT for the same single cause as CEN-G6 — the ratified `S = 4` versus the shipped ×50 — and for that cause only. Every other clause of this row conforms, and they were each walked rather than assumed.** **The clamp composition is structurally exactly as ratified**, in `Blockchain::update_next_cumulative_weight_limit` (`src/cryptonote_core/blockchain.cpp`): `effective_median = min( max(m_long_term_effective_median_block_weight, short_term_median), S × m_long_term_effective_median_block_weight )` — lower-bounded by the long-term effective median, upper-bounded by S×long-term, with `short_term_median` the median of the last `CRYPTONOTE_REWARD_BLOCKS_WINDOW` block weights. **Only the value of S is wrong** (50 shipped, 4 ratified), so this row is DIVERGENT while its structure is sound — a distinction that matters for the fix, which is a constant and a re-verification, not a rewrite. **Clauses that CONFORM:** *Zone floor* — `if (m_current_block_cumul_weight_median <= full_reward_zone) m_current_block_cumul_weight_median = full_reward_zone`, where `full_reward_zone = get_min_block_weight(hf_version)` = `CRYPTONOTE_BLOCK_GRANTED_FULL_REWARD_ZONE_V5` = **300 000**; the long-term effective median is independently floored at the same constant. *Limit = 2 × median* — `m_current_block_cumul_weight_limit = m_current_block_cumul_weight_median * 2`, applied **after** the zone floor, so the floor propagates into the limit. *Long-term weight clamp* — `Blockchain::get_next_long_term_block_weight` bounds the stored weight to `[median·10/17, median·1.7]`, implemented as `max(block_weight, effective_median * 10 / 17)` then `min(..., effective_median + effective_median * 7 / 10)`; the 1.7× upper bound is expressed as `m + m·7/10` rather than `m·17/10`, which is the same value in integer arithmetic for all reachable medians and avoids an intermediate overflow. *Vestigial version argument* — `get_min_block_weight(uint8_t /* version */)` (`src/cryptonote_basic/cryptonote_basic_impl.cpp`) has its parameter **commented out in the signature** and unconditionally returns the V5 zone, confirming the row's "ignores its version argument" clause literally; CEN-H3 depends on this same fact and agrees. **A STALE COMMENT SITS ON THE DIVERGENCE and will outlive it:** the clamp line carries `// effective median = short_term_median bounded to range [long_term_median, 50*long_term_median]` — it hardcodes **50** in prose. When the constant moves to 4 the comment becomes false, and it is the sentence a future reader trusts to explain the arithmetic. **Fixing the constant without fixing this comment converts a known divergence into a silent contradiction** — flagged here so the fix lane inherits it. *Falsifier — what flips this row to CHECKED-CONFORMANT:* the same one as CEN-G6 (S becomes 4, d24-consistent, clamp re-verified), **plus** re-verification that the zone floor and the 2× limit still compose correctly at the smaller clamp ceiling — a tighter upper bound changes which of the three bounds binds in normal operation, and this row asserts all three, not just the clamp. *Independent falsifier:* the V5 zone value (300 000) acquiring a derivation record would not change this verdict but would close the row's separate provenance gap — its arbitration was punted "to the economics doc" and never landed anywhere, and the 1.7× clamps ride along unexamined (census §10 R2, FOLLOWUPS row owned by the census). **Scope: the weight-limit remainder at connect. The reward-side 2·median rejection is CEN-F14; the penalty curve is CEN-F14b; the tx-level half of the same zone constant is CEN-H3.** **Walk-only; no test run.** Spec: `docs/completed/CONSENSUS_C2_R2_WEIGHT_FEES.md` Q1 (zone) + Q2 (clamps) | **Regression only.** Same reasoning as CEN-G6: a digest match on a port that reproduced ×50 would confirm the refuted value was carried across. The *structural* clamp composition is sound and a rewrite may safely reproduce it — it is the constant that must not be copied |
