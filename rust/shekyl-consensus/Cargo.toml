[package]
name = "shekyl-consensus"
description = "Modular consensus system for Shekyl (PoW/PoS/hybrid pluggable proofs)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
thiserror.workspace = true
serde.workspace = true
# RTN-5: leftover `Difficulty(pub u128)` is deleted in favour of the
# transform-shaped types owned by shekyl-difficulty.
shekyl-difficulty = { path = "../shekyl-difficulty" }

[lints]
workspace = true
