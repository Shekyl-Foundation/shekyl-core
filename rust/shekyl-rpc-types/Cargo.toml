# Copyright (c) 2025-2026, The Shekyl Foundation
#
# All rights reserved.
# BSD-3-Clause

[package]
name = "shekyl-rpc-types"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Wallet-daemon RPC wire-contract types: the SubmitVerdict/RejectCause submit contract (docs/design/DAEMON_SUBMIT_VERDICT.md §2), single Rust definition consumed by shekyl-daemon-rpc and shekyl-rpc-client"

[dependencies]
serde = { workspace = true }
# The `.bin` half of this crate's job. RK-D1 always said one definition per
# method, "serde for JSON; shekyl-portable-storage for `.bin`"; RK-4a is the
# first method that speaks the second. The earlier claim that this crate's
# production surface stays serde alone (RK-D10) predates it and is superseded.
shekyl-portable-storage = { path = "../shekyl-portable-storage" }

[dev-dependencies]
serde_json = { workspace = true }
# Only to render a byte diff when a `.bin` parity test fails.
hex = "0.4"
# The frozen-hex txid KAT (tests/txid_kat.rs) computes the engine-side txid
# over a C++-captured oracle blob. Dev-only: the contract crate's production
# dependency surface stays serde alone.
shekyl-wire = { path = "../shekyl-wire" }

[lints]
workspace = true
